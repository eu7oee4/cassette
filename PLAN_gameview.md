# PLAN_gameview：游戏视图（画面＋点评同步的直播/回放）

2026-08-31 定稿（前情：08-30/31 与机主两轮讨论——先否掉 adb 轮询投屏，后随
PLAN_sdk 设计稿三「三轮改判」把定位钉死）。机主想看他玩如鸢：打开是直播
（游戏画面＋他的点评气泡同步上屏），关掉回正常聊天；睡醒能往回翻（回放）。

## 0. 定位与出发点

- **视图=「风险工具三件套」的第三件**（PLAN_sdk 设计稿三·三轮改判）：批准门＋
  插话通道＋**过程直播**。前两件已有（game_start 门/聊天框插话+急停⏸），
  这份 plan 补齐可观测性——看不见他在干嘛，就不知道该不该插话、插什么。
- **画面源=他自己的眼睛，不做独立采集**。曾比过 adb 每秒轮询截屏（1fps 投屏）：
  会截到打字机半句话，且是第二套采集链。他的截图走 double-tap 全文显示，
  帧只在他动作时更新——**你看到的就是他停下来读的那一屏**，帧率=他的操作
  节奏（几秒到几十秒一帧），POV 直播的正确形态，不是缺陷。
- **数据侧零新增**：PR13 活动事件账本已经是 feed 的全部——`save_shot` 落
  `state/activity/shots/<seg_id>/NNNNN.jpg`＋shot 事件引用；comment/user_msg
  事件由 deliver 闭包/泵在写；区间账收摊落章节志标题。本 plan 只做**读面**：
  两三个路由＋一个 iOS 页。
- **与 §5.5 会话卡的关系：本 plan 是会话卡 game 展开态的先行件**（§5.5 落地
  批次写的「game 先上当预演」就是它）。§5.5 给 code 设计的展开态是工具卡
  事件流；game 的正确展开态是**大图＋点评气泡**——同一账本、同一路由，只是
  渲染形状不同。iOS 视图做成独立组件，会话卡上线时从卡内展开复用，不另起炉灶。

## 1. 已定决策

1. **点评一根线不变**（结构铁律：过程不进消息史）：正文照旧 outbox→消息史，
   视图是同一条流的「带画面透镜」，**不是第二条消息通道**。聊天里的折叠形态
   （活动卡→摘要卡）归 §5.5 会话卡批次，本 plan 不做。
2. **视图只看，没有输入条**：unified 下泵开着时她说话就走聊天框（PR13 欠账⑤，
   /code/send 有声拒）；急停⏸在顶栏现成。
3. **直播=轮询不上 SSE**：帧率本来就低（他的操作节奏），live 时 app 每 2s 拉
   一次 feed 增量足够；省掉一条常驻连接的生命周期管理。
4. **回放窗口=30 天**，跟账本清理口径走（RETAIN_DAYS，不为视图另设）；30 天后
   只剩章节志摘要——与 §5.5「与人的记忆衰减同构」同款，不补档。
5. **图片路由按 seg+序号取，不透传盘路径**：`/game/view/shot/{seg}/{n}`，服务端
   拼 `SHOT_DIR/<seg>/<n:05d>.jpg`（`_safe_id` 同款清洗）——账本里的 ref 是
   绝对路径，直接透传=路径注入面，不开这个口。
6. **事件渲染面（一期）**：shot（主轴）＋comment（挂在最近一帧下方）＋user_msg
   （她的插话，气泡另色）。**tool 事件不渲染**——点评+画面就是全部叙事；
   tap 坐标点按标记叠加留二期彩蛋。comment 挂帧有已知误差（shot=False 连点时
   点评对应稍早的帧），接受，不做精确对齐。
7. **鉴权与隐私**：三个路由全部 X-Auth（jobhunt_pdf 同形）；截图可能含剧情/
   账号信息，只走鉴权路由，不进 uploads、不进任何公开面。

## 2. 后端（PR-A）

| 路由 | 干什么 |
|---|---|
| `GET /game/view/segments?char=` | 场次列表：`read_intervals`(scene 含 game) ＋ `open_segments()` 标 live。返回 seg_id/start/end/note（=章节志标题）/live |
| `GET /game/view/feed?seg=&since=` | `read_events(seg)` 过滤 kind∈{shot,comment,user_msg}，shot 行换成 `{kind:"shot", n, ts}`（n 从 ref 文件名算）；`since`=行号偏移（jsonl append-only，行号即游标，确定性） |
| `GET /game/view/shot/{seg}/{n}` | FileResponse image/jpeg；不存在/已清理→404（app 显示「画面已淡去」帧位） |

- feed 高频全量读一场 jsonl（几百行）没有性能问题，不建索引不上缓存。
- 直播判活就看 seg 在不在 `open_segments()`，不新增状态。
- 30 天清理与 FileResponse 的竞态：清了就 404，app 按缺帧渲染，不加锁。

## 3. iOS（PR-B）

- **入口挂 GamePage**（现成页面，改动最小）：live 场次置顶带「LIVE」直接进；
  下面历史场次列表（30 天内，note 当标题）。会话卡上线后同组件从卡展开复用，
  入口不冲突（一个从游戏页进、一个从聊天卡进，同一视图）。
- **视图布局**：上=当前帧大图（AsyncImage，新帧淡入；左右滑逐帧回看，滑回最
  新帧恢复跟播）；下=点评气泡流（comment/user_msg，自动滚到最新）。
- live：Timer 2s 拉 feed 增量；页面消失即停（onDisappear，别挂后台轮询）。
- 回放：底部时间轴拖动+逐帧；**图按需拉不预载**（一场可能 500 帧×~100KB）。

## 4. 验收

- 单测：feed 过滤与 since 游标/shot 序号映射/路径清洗（`../` 进不来）/清理后
  404/open 段标 live。
- 真机：开一局观战页 3s 内见新帧+点评同步；她在聊天框插话→视图里 user_msg
  出现；game_end 后场次转历史可回放、逐帧拖动流畅；30 天外场次不在列表。
- 铁律核对：全程零写操作（feed 是账本纯读面），消息史一条不多不少。

## 5. 坑与施工核对项

- **`game_watch` 连拍是否每张都过 shot_sink**（game_loop.py 连拍工具一个往返
  2-6 张）：动工先核——若只 sink 末张，直播会丢中间帧，把 sink 点挪进连拍循环。
- N 张滚动重铸不影响视图：账本独立于 transcript，重铸边界对 feed 无感——
  这正是「账本当经历权威」的红利，顺带当一条验证。
- 非 unified 回退路（STORY_ENGINE=sdk 独立 loop）同样走 shot_sink 落账，
  视图两条引擎路通用；tmux 回退路没有账（没有 sink），观战页对那种场次自然
  为空，不补。
