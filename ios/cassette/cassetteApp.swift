import SwiftUI

@main
struct cassetteApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .tint(Color.userAccent)   // 全 app 默认控件色＝机主人物色（PLAN_chatui §1.4）
        }
    }
}
