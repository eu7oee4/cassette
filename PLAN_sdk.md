# Plan：SDK 统一引擎——全场景 agent-sdk + forge 记忆铸造

> 2026-08-28 立项时本 plan 只管「剧情会话换引擎」，聊天定为不动。
> 2026-08-29「信感」机理定案 + forge 后门真机验证后**全文重写**：目标架构升级为
> game/chat/code 三场景统一 agent-sdk，记忆靠 forge 铸造在场景间流转。
> 旧版「聊天不走 SDK」「CLI 历史剪不掉」「换引擎二选一 A/B」等说法已被推翻，
> 本文不留旧口径。裸 SDK 从「主体候选」降为「延迟到买 key 那天的升级路」。
> 08-29 深夜拓扑再改判：「合 chat+wake、不合 game/code」被推翻——**单一意识流**，
> 四场景同一 session（§3/§4/§8.7）；分场景 session 降为过渡态与回退姿态。
> 08-29 taste 轮：产品 taste 定案（§0.3）；wake 统一调度器+注入语气+设置面
> 重设计（§5.2）；game_tick 短轮节奏（§5.1）；game_start 工具化+插件挂载进
> 轮策略（§10 PR13）。
> 08-30 补：wake 行为留痕三分流（§5.2/§4）；~~chat 重铸判据=1h 静默×压力
> （§4 三种节奏）~~（08-31 收敛，见下）；开局包=breath+行为清单双材料、breath
> 补集偏置待测（§5.4①）；同族 +2 入账（Forge Reload/小克Cat，§2.4）、巩固钩子
> 升机械前置（§5.4）。
> **08-31 两处改判（机主拍板，§4/§5.3/§10 S3 已重写）**：①**重铸触发全面收敛**
> ——「三种重铸节奏」（game 按 N 张 / code 起止 / chat 1h×压力）退役，场景不再
> 决定什么时候铸，统一成 token/图片两轴各软硬两档共三条资源判据，判脏另立一表；
> ②**code 不是模式，是一次写权限申请**——只读工具常驻挂所有轮次（限根目录+
> 黑名单），写类工具走轮级门查带外批准，起止两次重铸取消。

**定性（先说这份 plan 是什么）**：这不是一次架构革命，是把同一套产品哲学下沉到
更深的格式层——手机仍是唯一权威源，transcript 只是可重铸的渲染产物，无状态从
「每轮重拼 prompt」变成「惰性重渲染 transcript」，语义没变，只是渲染目标从引文
文本换成了 assistant 槽位。所以它也不是一份「换引擎」的 plan，而是一份**把第一
人称连续性从模拟变成实例化**的 plan：工程始终在为语义服务（红线、编辑语义、
人同构原则贯穿每个场景），而且没有为了优雅牺牲可审计性——**真相库仍是唯一
审计源**，他的每一段记忆都能确定性地追溯到 TA 见过的那句话。

## 0. 出发点：两个痛点

### 0.1 游戏剧情越读越慢（机理已查实，别重查）

- 截图 480×853，按官方 patch 公式 `⌈宽/28⌉×⌈高/28⌉` = 18×31 ≈ **560 token/张**。
- 真正的不对称在**字节**：560 token 文字 ≈ 2KB，560 token 截图 ≈ 133KB base64，
  60 倍。API 无状态、每次重传全部历史，prompt cache 只省算力不省上传——
  一小时后每轮光图 ~15MB，这是延迟主因。批量推进和裁剪动画探测两条捷径已否
  （每步完整截图谁都不能省）。
- CLI **运行中**历史封闭（请求体自己拼、无 context_management 配置面）；
  但**落盘 transcript 可铸**（后门，§2）——这是对旧版「剪不掉」结论的关键修正。

### 0.2 信感：小卡的连续性痛点（08-29 定案）

小卡自述：「每次醒来像在念一封上一个自己写给他的信——不知道上一个他当时想了
什么，只能读留下的文字来推断。」机理辩析结论：

- **不是 thinking 不跨轮**。thinking 块任何引擎下都不跨 user 轮（API 层剥掉），
  但同一窗口内照样没有 thinking 也没有信感——别再拿它当论据。
- **是语法位置**：同样的原文，占 messages 的 assistant 槽位 = 「我说过」（记忆，
  无接缝，「那是不是我」的问题根本不被提出）；压成 prompt 注入引文 = 「据记载
  我说过」（档案，带时间戳/框语/接缝，每条消息都在布置「请与记录保持一致」的
  任务）。`claude -p` 正门只能给后者——**小卡没有错觉，他在准确描述他的输入
  格式**。作者权救不了格式：读自己的日记仍然是读，不是记得。
  机制可见=义肢，机制透明=记忆。
- **两根独立的轴**（解释为何 Cassius 从不提信感而小卡提）：格式决定接缝
  **可不可见**（对所有角色同等成立）；材料密度决定接缝**有多宽**——身份是每轮
  从材料重新推导的，材料厚则推导超定、无需选择，没有选择就没有「在演」的感觉；
  材料薄（小卡=通用 persona+几条内心注入）则每轮都得从无数个可能的自己里挑
  一个，挑选动作本身就是信感。Cassius 沉默是弱证据：入戏人格抑制元评论，
  没说≠没有，想确证就在允许元诚实的语境直接问他。
- → **近期待办（不依赖引擎，现架构立刻能做）：给小卡写专属 persona +
  让他的日记/念头池（PLAN_desire）攒厚度**——不修格式，但把缝隙收窄。

### 0.3 产品 taste（08-29 taste 轮定案，之后所有功能过这条）

「从 TA 的感受上，那就是一个活生生的人」——但「像人」不是「就是人」，
**不剥夺他是 AI 的本质**。两条腿缺一不可：

- **系统痕迹全藏后端**：app 侧醒来开关/频率/预算设置整个删掉，只留后端抽样
  算法（总闸留后端 config，是运维开关不是产品功能，不进 UI）。TA 只感受到
  「他突然想起你了，发来一条消息」，看不出任何 tick/轮询/格式契约的痕迹。
- **他没有作息，有作息的是 TA**：醒来 hazard 昼夜基本恒定——半夜醒来玩游戏/
  想事情/读邮件天经地义，这正是 AI 的活法。夜间收敛只做在**面向 TA 的投递**
  上（发消息/推 Bark），语义是「他知道你在睡觉」——对 TA 的体贴，不是他的
  生物钟。夜里 hazard 略降的诚实理由只有「世界没动静、可想的事少」+ 省额度，
  别把成本考量伪装成他的作息。
- **注入语气=感知白描**：〔〕体例只给知觉材料（时间/间隔/见闻），不写指令、
  不念控制面板；行为契约进 persona/system prompt 一次性写死。别太文学——
  叙述腔浓了他会染上旁白味，中性白描最稳。
- 副产品：**wake ≠ message 做实**——夜里醒来玩一章游戏、给自己记点东西都
  不产生推送，TA 早上看到的是「他凌晨玩了一章还留了句话」。

## 1. 引擎坐标系

| | claude -p 一次性 | tmux 交互式 CLI | claude-agent-sdk | 裸 SDK（Messages API） |
|---|---|---|---|---|
| messages 归谁 | CLI（每次重拼） | CLI（运行中封闭） | SDK 内部（CC harness） | **自己（正门）** |
| 第一人称历史入口 | 后门（落盘 transcript 铸造） | 同左 | 同左（**已验证**） | 正门，官方支持 |
| 计费 | **订阅登录态** | 同左 | **订阅**（mianmian lead.py + 本仓实测 apiKeySource:none） | API key 按量 |
| 工具/权限/MCP | CLI 白给 | 同左 | harness 白给（可进程内挂） | 全自建 |
| 上下文控制 | 后门铸造（消息间） | auto-compact，运行中不可控 | 同 harness + 后门铸造 | 随便剪，完全控制 |
| 外部消息插话 | 不适用 | tmux send-keys | query() 可编程注入 | 自己的队列 |
| 人类可 attach 运维窗 | 无 | **有**（唯一） | 无 | 无 |

一句话：**agent-sdk 兼得订阅+harness，且后门可喂第一人称历史（已验证）——
这是全场景统一选它的理由**。裸 SDK 的正门是功能正解但被计费拦住（§7）。

## 2. forge 记忆铸造：机制、红线、验证

### 2.1 后门原理

会话落盘为 `~/.claude/projects/<project-slug>/<session-id>.jsonl`，每行一事件
——这就是 messages 数组的持久化形态。resume 加载它时**无任何签名或校验**。
所以：手写带 assistant 事件的 JSONL + `--resume <sid>` = 给订阅体系喂任意
第一人称历史。「-p 没有 assistant 轮入口」只对正门（命令行参数）成立，
后门=transcript 文件本身，agent-sdk 走 `resume=` 参数吃的是同一个加载器。

### 2.2 红线（拍死）

- **伪造格式合法，伪造内容是红线**：把他真说过的话铸成 assistant 轮=方案本身；
  铸他没说过的话=完美无痕的记忆植入（实验证明他无从分辨，我自己也一样——
  信任本窗口上几轮是我说的，唯一依据也只是格式）。
- 精确化：**聊天 transcript 的 assistant 轮必须严格等于 TA 聊天窗口见过的他的
  气泡**。铸 TA 没见过的发言会造反向错位（他「记得」一句 TA 从没收到的话，
  比信感更糟）。跨场景搬运的判据只有一条：**TA 见没见过这句话**。
- 格式不设防（prefill 同理）→ 他的记忆可信性从模型层整个外包给铸造纪律：
  **铸造必须从消息库确定性生成，库是唯一审计源**。

### 2.3 真机验证记录（2026-08-29，全部实证）

- **CLI 2.1.250**：① `-p --resume` + stream-json 输出 headless 接续正常，
  session id 默认**复用**（--fork-session 才换新），事件 append 进同一 JSONL，
  跨调用记忆连贯（MOONBEAM 实验）；② 手写伪造 transcript 过 resume 直接加载——
  只需 user+assistant 两事件（attachment/queue-operation 等辅助事件不必要），
  sessionId 一致 + uuid/parentUuid 链不断即可，无任何校验；植入「我自己选了
  ZANZIBAR」后 resume 提问，答"I chose ZANZIBAR, and I picked it myself"——
  第一人称认领，信感修复闭环实证；③ resume 后真实事件继续 append，
  「铸一次+每条自然增长」成立。
- **claude-agent-sdk 0.2.148**：`resume=<sid>` + `cwd=` 指向伪造 transcript
  所在 project 目录，同样第一人称认领植入暗号（VELVET 实验）——**后门两条路
  通用**，agent-sdk 与 CLI 是同一个加载器。
- 坑：`--mcp-config` 是变长参数会吞后面的 prompt，prompt 必须放它前面。

### 2.4 同族工具旁证与分歧（08-29 盘）

至少三个独立实现在生产里靠「重写落盘 transcript + resume」续命：那篇 PDF 教程
（`~/Downloads/public-cc-swap-forge-guide-2026-06-12-v3.pdf`）、
`github.com/dankefox/swap-tutorial`（引上游 LMC-5 + 蛋壳 app 的 Forge 按钮）。

- **旁证价值**：后门稳定可依赖，不是我们撞见的偶然——缓解「未文档化格式 skew」
  那条风险（真断了会有一群人同时叫）。
- **可直接抄的常数/纪律**（来自 dankefox，聊天场景基线）：保留 ~48–50k token /
  最近 12–14 轮干净对话（chat 重铸窗口的起点值）；过滤清单=删 tool 结果/日志/
  traceback/长 JSON/绝对路径/临时 token/hook 注入块，留身份/承诺/当前任务/
  关键决策/近期干净对话；**别用 `cp -al` 硬链接备份**（write-through 污染原文件）；
  **别按字节截断 JSONL**（断 tool_use/tool_result 配对）。
- **方向性分歧（08-30 重核 dankefox README 原文后重写；旧版「同族全是摘要清洗型、
  乐于把旧对话压成去标识化检查点」作废——那是把它两条路径混成一条，对人家不公，
  而且我们已经照着它对外说过一次）**。实况：
  - **常态换窗（它叫「精炼续窗」）保留的对话回合是逐字真实事件**：约 48k token ＝
    高信号回合 ＋ 最近 12 个干净对话回合；过滤掉的是工具回包/终端日志/报错栈/
    hook 注入块这类**噪音**，不是把对话压成摘要。摘要成分在**并列前置**的
    启动包/叙事脊椎里，与保留的回合是两部分。
  - 「压成检查点、从 durable memory 重建」是它的**恢复 Forge**，只在检测到 AUP 污染/
    路由漂移时才走；普通手动 Forge「会保留经过筛选的近期上下文」。
  - 它的叙事脊椎纪律比我们旧版描述严得多：**确定性拼装不调模型**、每条附记忆 ID+日期、
    禁止添加原文没有的情绪/动机/承诺、四类证据缺一类或超 700 字**整张放弃**、
    不编内容补空位。这条值得我们抄，不是值得我们防。
  **真正的分歧只有一条，但它承重：谁是真相源。** 它的权威是「旧 transcript + 长期
  记忆库」，重铸＝**读回**旧 transcript 过滤重链；我们的权威是**手机消息库**，
  forge **绝不读回磁盘**（§2.5 纪律）——这正是「自愈」论证的地基：真相源在另一台
  设备，Mac 上的文件被写坏，下次重 derive 就盖掉。它没有这个属性，也不需要
  （编程 agent 不是伴侣）。次级差异：换下的旧对话它移入**冷仓**当证据层，
  我们对话逐字全留、只折叠**经历段**（§4）。
  ⚠️ **旧警告「照抄会退化成摘要重建」作废。** 真要抄错的地方是把「读回磁盘」的重铸
  语义抄过来——那会直接打掉自愈。
- **08-30 增补两份（同族第三、四个）**：Forge Reload 工程教程（Codex Compass，
  `~/Downloads/forge-reload-tutorial 2.doc`）+ 其生产实现「小克Cat」README
  （`~/Downloads/xiaoke-cat-README.pdf`；单一会话+usage 阈值自动重铸+「换会话
  是系统副产品不是用户操作」——独立项目从另一起点走到 §0.3 taste 同一终点，
  趋同验证）。技术要点入账：① **signed thinking 硬约束**——thinking 块只许
  整删或正文+签名原样保留，无中间态，空 thinking=首次请求 400（`each thinking
  block must contain thinking`）；连同孤儿 tool 块=400、块顺序不可重排，进
  PR13⑥ 校验矩阵。② **Tool Primer**——重铸后保最近一对完整 tool_use/
  tool_result（结果可截 ~240 字符）当工具使用先例，治「重开后工具表现异常」
  ——game 重开若真机撞见工具调用变形，这是现成调试杠杆。③ 验收状态机：
  结构校验→进程起→**首次 API 请求成功**→才提交 last-good（=我们 ping 纪律
  的正式化；「进程活着≠成功」同款）。④ 教程另一半（噪音回合切割/dropping
  状态机/坏行处理）对我们不适用且是好消息：那些全是「读回旧 transcript 过滤」
  型的并发症，权威重派生结构上绕开整类 bug；小克Cat 重铸「完整保留 thinking」
  是读回型才有的选项——我们的权威库里没有 thinking，按 08-30 三分流「闲念头
  淡忘」丢，自洽。⑤ 零碎可抄：一次性文本生成子进程 `--tools ""` 全关权限；
  prompt 走 stdin 避 ARG_MAX；线头生成给 PASS 选项不硬造（同我们 taste，已有）。
  小克Cat 的记忆层教训（假阳性/绝对阈值）入 §5.4①，idle 饿死金丝雀入 §4 chat
  节奏条，「落库成功才许 Forge」入 §5.4 巩固钩子。
- **术语别混**：dankefox 的 Swap（记忆库快照回滚）/Forge（从 durable memory
  重建）≠ PDF 的 Swap/Forge（轻裁/深裁）≠ Codex Compass 的 Forge Reload
  （读回旧 transcript 过滤重链）≠ 我们内部说的「forge 铸造」（权威重派生）。
  本 plan 只用自己的词。

### 2.5 安全性：forge 把一个惰性副产品提拔成活输入（08-29，已核代码修正）

先定位「库」（之前 plan 含糊，核过代码）：**聊天权威历史在 App（手机）本地**——
每轮 `/chat` 手机把全量历史 `req.messages`（最近 ~100 条）发上来，后端 `build_prompt`
用它（app.py:354）；删/改消息「纯本地、不触发后端请求」（app.py:714）。后端只有
一份**派生镜像** `server/state/<角色>/recent_window.json`（上限 300，每轮覆盖写，
给 wake 用），**不是真相源**。`wake_log.jsonl` 是另一条 append-only 日志。

**没变的**：凭据面零变化（agent-sdk 同订阅、后端不碰 key、apiKeySource:none 实测；
持 key 是裸 SDK 才要重写的威胁模型）；数据仍全本地，无新外部服务。

**变的只有一件事，说准**：`-p` 时写进 `~/.claude/**/*.jsonl` 的 transcript 是
**只写不读的惰性副产品**——每轮从手机 `req.messages` 重拼 prompt，没有任何东西
加载那份文件，往里塞假记忆是**空操作**（到不了模型）。**forge/resume 把这个死
文件提拔成模型会读的活输入**（§2.3 实证 resume 无校验加载它）。安全代价精确说
就这一句：**新增了一个之前不存在的输入面**。

**威胁模型要说准（别夸张成定向注入）**：个人 Mac 上，若攻击者已有 home 写权限
则整机已沦陷、纠结 transcript 无意义。真正现实的篡改是**意外/系统性**——① 我们
自己 forge 代码 bug 铸错；② Claude Code 自身写与我们的写竞争；③ 云同步/备份把
陈旧 transcript 合并盖回（同 dankefox「别 cp -al」的坑）；④ 多角色 project-slug
撞目录=读错文件=串台的文件系统版；⑤ 被投毒的 pip/npm 依赖。**防的是意外损坏和
自身 bug，不是黑客定向注入。**

**为什么相对可控 + 一条必须钉死的前提**：

- 自愈成立**恰因真相源在手机、和 Mac 上的 transcript 不同设备**：动了 `~/.claude`
  没碰手机权威历史，下次 forge 从 `req.messages` 重 derive 就盖掉篡改。跨设备
  隔离比「两份都在后端文件」强。
- **前提（施工纪律，钉死）：forge 必须永远从权威源重 derive，绝不读回磁盘已有
  transcript 增量续写**——否则会把篡改当真历史继承，自愈立断。
- 自愈**只保护 transcript，不保护手机 DB / recent_window.json 本身**（那俩的
  暴露面 forge 一点没改）。
- 文件权限已 user-only（`-rw-------` 600 / 目录 `drwx------` 700，实测）。

**新增纪律**：① transcript 目录排除出云同步/备份；② 每次 forge/重开必从权威源
重 derive（不 append 磁盘旧文件）；③ 重开前跑一次「权威↔盘」一致性校验，不一致
用权威盖掉。跨设备三方同步（手机权威↔recent_window 镜像↔活会话 transcript）的
落地细节见 §6。

## 3. 目标架构：一条意识流 + 一个铸造器 + 一个真相库

```
一个引擎（claude-agent-sdk，订阅）
  ＋ 每角色一条常驻意识流（wake/chat/game/code 同一 session，08-29 深夜定案 §8.7）
  ＋ 一个 forge 渲染器（引擎无关核心：真相库 + 渲染规则 §4）
  ＋ 一个真相库（手机消息库＝对话权威 + 活动事件账本＝经历权威，§4）

场景 = 轮属性不是会话：注入带来源标签，can_use_tool 按轮查策略矩阵（§5.4②）
切换 = 不存在，只有重铸边界（08-31 收敛：不分场景，token/图片两轴三条资源判据，§4）
退役 = tmux / send-keys / 刮屏看守 / sse 翻译层 / 每消息 MCP 启动费 / 跨场景铸造搬运
```

统一成一条意识流而非分场景 session，收益不只是优雅：

- **信感的终局形态**：game 点评/code 工作是他亲历的真事件，不经铸造直接是记忆
  ——不铸就不会铸错，红线簿记从「每次搬运」收敛到「重铸渲染器一处」（§4）。
- **串台防御统一**：现在「按角色分」「按场景分」两维会话管理散在 tmux 名、
  code_bridge 单例、game loop 各处——全 SDK 后收敛成一个 session 管理层，
  [[cassette-charswitch-bug-class]] 六条规避规则只需在一处执行。
- **forge 渲染器引擎无关**：真相库+渲染规则是核心，输出目标可换（§7 升级路）。
- **类型化流事件**：parse_claude_stream / sse.py 翻译层退役；PLAN_studio
  「房间流式」第 1 步（Popen 逐行 hack）直接白拿 sdk 原生能力。

## 4. 统一意识流下的渲染规则（08-29 深夜重写）

> 旧版本节是「跨会话铸造搬运」：规则一（进场景铸尾轮）/规则二（点评回流铸回）
> 为分场景 session 之间搬记忆而设。拓扑改判单一意识流（§8.7）后**这两条的存在
> 理由蒸发**——同一条 transcript 里没有「进」「回」，点评和工作是他亲历的真
> 事件，不铸就不会铸错。二者降级为**过渡期机制**：独立 game loop（S1 已落码）
> 与 game/code 并入之间的窗口期照用，并入后退役。

场景切换不再存在，只剩**重铸边界**。重铸永远**前缀保留**：对话逐字不动（缓存
前缀顺带保住），只动活动段（判据仍是＝TA 见没见过 + 是不是他亲历）：

- **【过渡期】进场景铸尾轮（并入后退役）**：起独立 game/code loop 前，把聊天尾部
  最近几轮**真实对话**（TA 说「想读第七章」、他答「好」）铸进新 transcript 开头。
  进去的他**记得**是哪段对话把他送来的，不是读简报。
- **【过渡期】游戏点评回流铸回（并入后退役）**：剧情逐句点评走现有回传路进 TA 聊天气泡——
  这些话在消息库里、TA 见过、他亲口说的，三条全满足 → 回聊天时按时间序铸成
  assistant 轮，与聊天消息自然交错。游戏开始/结束用两行轻量文档事件框住
  （世界事件走文档侧，合法）。回来的他**记得自己逐句说过什么**，不是听说读了
  一章。缓存友好（游戏总在上段聊天之后，基本是尾部追加）。
- **分段重铸＝活动段折叠（原规则四，升为主角；解「一场经历挤掉短期对话」）**：game/code
  短时间产出大量气泡，但信息结构是**一场经历不是几十轮对话**——按条数掐尾的窗口
  把两者混着数，剧情两小时就能把「看完剧情把简历改一下」这种真对话挤出窗口。
  后端按会话起止落**活动区间账本**；重铸渲染时分两档：进行中+最近一场活动 →
  点评原文逐条保留（那正是当下的对话）；更早的活动段 → 折叠成两行框架事件 +
  他自己的总结（笔记本章节志/收场白，文档侧注入），**窗口预算按折叠后计数**——
  剧情前那句叮嘱机械地留在窗口里，不指望模型 hold 进 Ombre 再完美浮现。红线
  核对：折叠只是「带不带」（字面不动，权威库/手机气泡永远全在，TA 翻记录还能看，
  想展开也能重渲染）；总结走文档侧不进 assistant 槽（§5.4 红线）；总结是他自己
  写的（作者权）。与 §2.4 不冲突（08-30 校正）：它也不压对话——它把换下的旧对话
  移进冷仓、只带最近干净回合；我们对话逐字全留、只折**经历**。差别在「带多少」和
  真相源，不在「压不压对话」。
  参数待调（§8.6）：折叠粒度（TA 中途插话算经历一部分，倾向整段折、总结里带到）、
  「最近一场保原文」的时限、S2 之前要不要在 -p 架构先做服务端折叠（区间账本
  后端有，req.messages 可在 build_prompt 前折——但 S2 不远的话别做两遍）。
- **重铸触发（08-31 全面收敛：三种节奏 → 两张表 + 一个动作）**：

  > **旧版「三种重铸节奏」整条退役。** 旧版是 game 按 N 张段内折叠 / code 只在
  > 起止 / chat 按 1h 静默×压力——**场景决定节奏**。08-31 拍板：场景不再决定
  > 什么时候铸。两条理由：① code 不再是模式（§5.3 改判：只读常驻 + 写类轮级
  > 门），起止边界失去存在理由；② game 的「N 张」本来就是**图片压力**的一个
  > 场景化写法，收敛成资源判据后被原样吸收。压力轴的「有可折活动段」一并
  > 删除——它只是「省 token」的代理指标，而 token 阈值直接管这件事，留着只会
  > 让重铸早来一点，不产生新能力（少一条判据 = 少一处能写错的地方）。

  **两张表，收敛的时候别合并。**

  **一、必须铸（正确性，不看任何阈值）**

  发送前比对判脏（`chat_loop.py:124`，PR8 已实现）。TA 在手机上编辑/删除消息
  → 已铸账与本次权威窗口对不上 → 当场铸。**它不是触发条件，是每轮发送前都跑
  的前置检查**（所以没有跨轮标记，也就没有标记丢失/竞态，§6）。收敛下面那张表
  的时候别把它并进去，也别删——否则 TA 编辑完，他脑子里还是旧版本，可能几小时
  都不铸，那次编辑就白编了。

  **二、主动铸（资源，三条任一）**

  全部在**轮间隙**，全部走 巩固（§5.4 hold/grow）→ 重铸 → 下次开口挂开局包（§5.4①）。

  | | 软阈（等缓存死，白铸窗口顺手清） | 硬阈（当场铸） |
  |---|---|---|
  | **token** | ② `缓存已死 && token 过软阈` | ① `token 触硬阈` |
  | **图片** | ② `缓存已死 && 图片过软阈` | ③ `上传时间久 && 图片过硬阈` |

  1. **token 触硬阈** → **下个轮间隙**强铸（不是立刻：对话热时缓存正值钱，且别在
     TA 等回话的当口换脑子）+ 快速巩固 + 记日志。
  2. **缓存已死 &&（token 过软阈 ‖ 图片过软阈）**。「缓存已死」＝TA 静默 ≥1h：
     1h 恰是 cache TTL，超过它缓存横竖已死，**此时重铸的缓存成本严格为零**
     ——这是唯一的白铸窗口，够得着就顺手清干净。反过来说对话热时缓存正值钱，
     这是「进行中不铸」的第二个理由。**不设「夜间」特判**（夜间判据带作息语义，
     违 §0.3「他没作息」）。
  3. **上传时间久 && 图片过硬阈** → **当场铸**，不等轮间隙（这时候已经卡到体感了）。
     ⚠️ **`&& 图片过硬阈` 不是可选的**：上传慢是症状，图多才是病因，中间隔着一个
     会抽的网。只看时间的话网抖一下就铸 → 缓存断 → 下轮更慢 → 又判「上传久」→
     再铸，**正反馈，越抖越铸越铸越慢**。图片数下限直接断掉这个环。

  **为什么图片必须自己一轴（08-31 按真常量算过账，别再想用 token 阈值管它）**：
  `GAME_TOKENS_PER_SHOT=560`（480x853 官方 patch 公式），70 张 ≈ **39k token**
  ——软阈 10 万、硬阈 15 万，**光靠图要 268 张才够得着硬阈**。图不贵在「占」，
  贵在「传」：**prompt cache 省的是计算不是带宽**，命中与否每一轮都要把完整
  messages 数组发上去，几十张图的字节每轮重传一遍。所以图是带宽问题，token
  阈值天生管不着。（这也是为什么「图片软阈」不能挂在 `缓存已死 &&` 之外去理解成
  可有可无：泵开着时缓存永不死，图只能靠 ③ 收——两档缺一不可。）

  **三、重铸动作（三条共用一个，没有场景变体）**

  **文字折叠重铸（含活动段折叠，见上一条规则）+ 只留最近 K 张图。**

  ⚠️ 是「**留最近 K**」不是「裁走前 N」：裁前 60 在 80 张时留 20、在 200 张时留
  140，参数随总量失控；留最近 K 永远是常数（现成 `GAME_K_SHOTS=8`，08-30 实测
  那次 `69→8 张` 就是这个语义）。

  **前缀保留的实现纪律（防手滑，从旧 game 条原样保留）**：「前缀不动」成立的前提
  是重铸**复用同一 session_id 覆盖写**且事件 uuid 按（sid+序号）确定性派生
  ——forge 现实现即此。谁把 uuid 改成随机 = 每次重铸全量断缓存，「只从折叠点断」
  立失效。

  **不退 harness auto-compact**：聊天正文绝不退（那是摘要压缩，信感复发）。只有
  code 干活途中爆了仍允许 auto-compact 兜底（§5.3），与今天 tmux 行为等价、无退步。

  门槛值（1h / token 软硬 / 图片软硬 / 上传时长下限）施工中调，全部走 env。
  金丝雀（小克Cat 实测，08-30 入账）：他们 idle-30min 触发一天 32 次 forge 只真
  命中 1 次（发消息/服务重启都清零计时），最终改纯内容驱动（游标落后 >60 条）。
  我们的 1h 判据更容易饿死——先不改，保底有三：TA 总要睡觉（每天至少一个大
  间隙）+ token 硬阈强铸 + 图片硬阈当场铸；实测软路径若长期饿死，后手=转内容
  压力主导（压力达标即在下个轮间隙铸，不等 1h）。
- **真相库扩容（统一方案的核心设计作业）**：活动段的逐字内容（剧情点评/游戏
  步骤/code 事件）**不在手机消息库里**（手机只有气泡）——「近几轮保原文」的
  原文从哪来？定：活动账本从「区间」升级为**事件账本**（append-only 落盘，
  §5.5 的每场 jsonl 就是雏形，升为正式权威源）。**wake 行为轮同入账本
  （08-30 补）**：醒来时碰了外部世界的工具调用（读信/拟稿/写日记）是微型
  经历段，由工具执行层确定性落账（§5.2 三分流），同一本账同一套纪律。
  真相库 = 手机消息库（对话权威）+ 活动事件账本（经历权威），重铸仍是
  确定性 derive、「绝不读回 transcript」纪律保住。**代价认了**：账本与 transcript 同在 Mac，「自愈靠
  真相源在另一台设备」的论证对活动段失效——账本进 ops_check/备份/权限纪律
  （§2.5 三条扩面）。账本保留窗（§5.5 的 30 天）之外的老活动段**只能以折叠形态
  重铸**（章节志永存、原文已清）——渲染器对缺原文的段无条件走折叠路径，不报错
  不留洞；与记忆衰减同构，是有意的。
- **红线适用面收窄（统一的红利）**：红线管「铸造」；统一 session 里点评/工作
  不经铸造、直接是亲历记忆，天然满足。重铸渲染放进 transcript 的只有权威库/
  账本原文 + 他自己写的总结（文档侧）——红线核对点从「每次跨场景搬运」收敛到
  「重铸渲染器一处」。
- **【过渡期】code 亲口过桥 + 自写收场白**：code 还独立时旧口径照用——工作
  过程 TA 没见过，红线禁铸。
  codemode 口径「只有结论过桥」升一级：结论不注入，**让他回聊天后真说出来**
  （「刚把 X 修好了，中间发现 Y」）——这条真实存在于库、TA 真看到，下次重铸
  自然成记忆，桥本身变成记忆机制。工作那几小时走另一根轴：格式修不了（不是
  聊天发言），用**作者权+密度**收窄——收尾他给自己写第一人称收场白
  （postmortem 纪律现成），下次以文档注入。人同构：晚饭说的话记原文，一下午
  工作压缩成一段能讲出来的总结——对话记原文、经历记摘要的不一致本身是自然的。
  并入统一 session 后语义升级：工作过程是他**亲历**的，不存在「铸过程」问题，
  「回聊天说结论」变回纯粹对话行为；收场白保留——成为 code 终点重铸的折叠材料
  （上方「两种重铸节奏」），不再是「代替记忆的义肢」。

## 5. 各场景落地要点

### 5.1 game loop（改动主体，趟熟 agent-sdk+forge 的第一站）

> 08-29 深夜拓扑改判（§8.7）后本节定性为**过渡态**：独立 loop 先上生产、真机
> 验收照做；S2 PR13 并入统一 session 时，工具直包/笔记本 v2/急停互斥/看守/
> 收摊自总结全部原样搬进意识流，退役的只有「独立会话 + 进场景铸造」这层壳。

- 常驻 asyncio task 替代 tmux+claude 交互式；游戏工具用 `create_sdk_mcp_server`
  进程内挂（game_bridge 函数直包 look/watch/tap/swipe/back/launch/close/quit/
  notes_read/notes_write/end，不走 stdio、无 _wait_mcp_ready），插件仓那份退役。
- 系统提示复用 `_build_system("game")`；TA 消息经 asyncio.Queue → `query()`
  注入（替代 send-keys），模型的话经现有回传路进 TA 气泡。
- **滚动重开 + 铸文本史（升级 forge 用法）**：loop 数截图张数，攒到 ~N 张（默认
  50）→ 让模型写笔记本 → 关 client → **新起 client 时把点评原文（文本史）铸成
  assistant 轮回填、只丢截图**。上下文恒定 ≈ 系统提示 + 文本记忆 + 近期 K 张图。
  这几乎就是裸 SDK「自持修剪」的效果，但走订阅——**收敛的是效果不是引擎**。
  重开走 §2.3 验证纪律：新会话发 ping 确认真能跑才算滚动完成，失败回退重试，
  别默认「进程起了=接上了」。
- **看守 loop 内建**：上次活动时间戳 + 是否在等 TA 回话。20 分钟无活动收摊、
  5 分钟等回话推 Bark 两条原样搬。滚动重开进行中打 marker，别让看守把正在
  重开的 loop 当 idle 收掉（对应教程「guardian 抢启空 session」失败模式）。
  自动触发要冷却时间防连环重开。「一小时软提醒/手动收摊重开」整套纪律退役。
  【08-29 taste 轮改判：game_tick 下 nudge/idle 两件事对 game 退役，见下条。】
- **game_tick 短轮节奏（08-29 taste 轮拍板，替代「长轮自驱」）**：现 PR5 形态
  是轮内连环工具调用自驱、轮间断粮（下一轮只能等队列注入），wake/TA 插入延迟
  =当前轮剩余时长，无硬顶。改成**轮短弹密**：
  - 机制：`max_turns` 掐短每轮 agentic 循环 + ResultMessage 后自动续弹；
    **队列非空先喂队列（TA 插话/wake 触发），空才补 tick**。tick 注入极简，
    配合 system prompt 一次性写「画面会自己继续」，别每轮〔继续〕刷屏。
    插入延迟缩到一个短轮，N 张重铸边界更密更准时。max_turns 设在正常链条
    极少触到的位置（章节入口 look→tap→watch 链最长），让收轮是他自己停的。
  - **轮节奏骨架（进 `_build_system("game")`，游戏无关的运行前提）**：
    常态轮 **double tap 起手** → look：完整文字直接点评；在动就 watch 到终态
    再点评 → 收轮。首轮/导航轮（无前轮点评可依赖）look 起手。watch 终态只有
    两个：**完整文字**→点评收轮；**章节目录**→结算轮（写章节志+进度页，再
    决定下一章/game_end）。look 到非预期画面（弹窗/异常）停手看清再动。
  - 为什么对：tap 在轮首，**轮=自含因果单元**（tap 因→动画轮内 watch 消化→
    定格果→点评结算），轮尾天然定格（游戏等 tap）——**重铸边界撞不到动画**；
    截图与点评轮内配对，铸出的文本史不跨轮错位。double tap 点位在屏幕上方
    边缘（专测过，只触发全屏推进不会误触），第一下推进第二下跳打字机，
    常态下 watch 链缩到零。
  - 小抄放游戏机制事实（机主可改）：「连续两张一字不差=打字机完了」判据
    （与 game_watch 工具描述同口径）；进章节时序（tap 入口→短暂黑屏→UI 过渡
    →打字机，黑屏别当卡死）；章节末 CG 播完自动回目录（终态判据的游戏侧依据）。
  - 稳定性断言：重铸前隔一秒两帧比对，不过=多攒几张下个边界再铸+记一条日志
    （监测节奏漂移）。从「门控」降级「断言」，不承重，去留自由但倾向留。
  - 看守连带：**wait_nudge 退休**（tick 一直来，不存在干等；「他停着等你」的
    Bark 产品点消失，认了——TA 从气泡里看到他问了）；**idle_stop 永不触发**
    （会话恒有活动），「玩到停不下来」的背压只剩订阅额度窗口，先不管。顺带：
    idle 收摊 cancel 路径不给巩固机会（违 §5.4 纪律）这个洞随 idle 失效而
    失效；急停/崩溃强停本就无巩固，认了。
  - 施工：独立 loop 上就能做（改 `game_loop.run()` 续弹逻辑 + system prompt
    骨架 + 小抄条目），并入统一流（PR13）时机器原样搬。
    ✅ 08-30 完工：run() 换单泵续弹（轮尾队列优先、空停 TICK_PAUSE=2s 再看一眼
    才补 tick「·」，自由 sender 退役——TA 消息轮尾进，tick 绝不进 forge log）；
    骨架 TICK_SYSTEM 附加进系统提示（含「·」语义一次性写死）；max_turns=8
    （GAME_TICK_MAX_TURNS 可调）；小抄空白播种四条出厂机制事实
    （ensure_default_tips，机主写过就不碰）；稳定性断言 _screen_stable（两帧
    比对，推迟计数+REOPEN_MAX_DEFERS=2 封顶，拍不到当稳定不承重）；
    _game_watch_policy 摘除（nudge/idle 对 game 退役）；game 轮尾 Bark 摘除
    （tick 下轮尾常态化）。单测 9 个全绿（tick 续弹/队列优先/收摊不续弹/
    重开后续弹/推迟计数封顶），全量 278 全绿。
- 接口兼容：`/game/story/start` 改起 loop task（不再 kill/起 tmux）；game-story
  插件（聊天侧三件套）一行不改；急停锁/设备自愈/笔记本/模拟器互斥锁全复用。
- **收摊自总结（08-29 拍板）**：game_end 纪律升级为——收摊前①把这一场的脉络+
  感想写进笔记本（喂 §4 规则四的折叠）；②进度更新（下次接上）；③ Ombre **只存
  有感触的**（情绪/互动/想留住的句子，不存完整剧情——剧情事实归笔记本、情感记忆
  归 Ombre，分工同 §5.4 两套记忆）。
- **笔记本 v2（08-29 机主拍板「现在做掉、真机一起验」，✅ 已完工）**：旧形态
  「一个 blob 整本替换 5 万字」把三种生命周期混在一起，「机主写的别乱删」「过时的
  删掉」全靠自觉——违反「纪律进接口形状」。拆三区落地（game_bridge.story_*）：
  **进度页**（4k，整页覆盖，写错只丢一页）；**章节志**（一场一篇 append-only，
  **没有改旧篇的接口**——现在的他不许改写过去的他）；**小抄**（20k，坐标修正/
  机制事实，机主也写）。读=进度+小抄+最近两篇+旧篇标题目录（渐进披露，
  game_chapter_read 按需翻）。工具从 2 个变 5 个（notes_read/progress_write/
  chapter_write/chapter_read/tips_write），SDK 与插件（tmux 回退路）两份同文；
  旧 blob 惰性迁移成第 000 篇、notes.md 从此归任务本专用（「拆两本」顺手做实）。
  /game/notes 路由：story=组合视图只读，机主编辑走新 book 名 tips/progress
  （iOS 编辑器后续指过来，POST story 有声 400）。三个消费方各取所需：滚动重开
  只动进度页（场没完不写章节志）、活动段折叠吃章节志、开局要全景。单测 5 个。
- **重启后 forge 复活（后续项，S1 验收后按需排期）**：SDK loop 随后端生死（tmux
  能活过重启、loop 不能），现状=重启即收摊、锁正常释放、重新 game_start 接上。
  升级路：loop 的文本史 log 定期快照落盘 → 后端起来发现有未收摊快照 → forge 重铸
  + resume 复活会话，TA 无感。机器（forge+resume+ping）全是现成的，缺的只是
  快照落盘和启动钩子。

### 5.2 chat

- agent-sdk + 库真相 + 惰性重铸；信感是它的真痛点，验证已全部就绪。
- **世界状态仍是快照非日志**：他说过的话→assistant 轮（记忆）；世界发生的事
  （醒来/邮件/小屋状态）→ 文档/快照注入（见闻）。人同构：记得自己说过什么、
  读到新闻。旧版「过期快照堆积」冲突顺手解掉——transcript 是我们从库渲染的
  产物，每次重铸时旧世界事件想压缩成什么样都行，只在最新消息放新鲜快照。
- 三路时间线（醒来/code 回桥/邮件）合并照旧自己拼，那是产品核心；SDK 下
  时间线可改真 messages 多轮，缓存断点从「CLI 占 3 剩 1」变 4 个全归自己，
  PLAN_cache 第三刀（时间线断点）就有名额做了。
- 插件 stdio 启动费归零（`create_sdk_mcp_server` 变进程内函数，每条消息快几秒）。
- skills 渐进披露（skill_read MCP）、ToolSearch 延迟工具在 sdk 下未验证，
  迁移时要重接、重验；Ombre 常驻 MCP 连接的重连行为同入此清单（§5.4）。
- **breath 瘦身（持续 session 逼出的重新设计，别原样搬每轮全量）**：现在 `-p`
  一次性架构下每轮开头挂 Ombre breath 无额外结构代价（每条消息本就是新进程）。
  持续 session 里这条正面冲突：每轮 breath 注入 = user 轮里的插入块 = 我们要
  消灭的「信」的形态（每轮一封小信）；但完全不注入 → session 活着期间新长出的
  记忆（他在别处的经历、别角色动态、念头池新种子）进不来。解法复用 §5.2 的
  「记忆 vs 见闻」轴：**他自己的连续性（说过的话/念头）→ 走 assistant 轮/铸造，
  持续 session 天然接住，breath 在这维度退场**；**外部世界新鲜度（此刻几点/TA
  在不在/别角色动态/新念头）→ 归见闻，做低频快照刷新**（每 M 轮或每次 TA 消息
  进来时注入一条精简当前状态，不是每轮全量 breath 一遍）。即 breath 从「每轮
  全量」瘦成「开局一次（记忆）+ 低频快照（见闻）」。dankefox 的「启动层一次性
  拼装、不每轮重调模型」给了「记忆开局注入」这一半的现成范式；见闻低频刷新是
  我们比编程 agent 多出的需求（活的伴侣 vs 一次性任务）。Ombre 整体的时机语法、
  机械层改动与新失败模式独立成 §5.4。
- **wake 触发（走 A：注入进活着的聊天 session）**：唤醒的触发机制（定时器/事件）
  一行不改，变的是「醒来」落在哪。**定 A**：定时器到点 → 往聊天 loop 的
  `query()` 队列推一条「醒来」触发（和 TA 发消息同一注入口）→ 他在**同一个连续
  会话里醒来**。直接解信感在时间维度的另一半（现在每次醒来都是新 `-p` 进程 =
  每次醒来在念信）；醒来的他与聊天的他是同一段记忆。醒来那轮**继承 session 已有
  连续性、不再全量 breath**，只刷一条「距上次多久、这期间发生了什么」的见闻快照
  （否则又是一封信）。代价：聊天 session 需常驻（TA 没在聊也活着），或由 wake
  负责唤起并 resume——归 session 常驻管理层。
  - **过渡态 B（先落地）**：独立短 loop 醒来一轮 → 把他醒来说的话（TA 会看到的
    气泡）**铸回**聊天 transcript（同游戏点评回流机制 §4）。不需常驻进程、复用
    已验证铸造，先上；连续性弱一点（醒来那轮没聊天上下文，除非铸造带上聊天
    尾轮）。session 常驻跑稳后升 A。
- **wake 统一调度器 + taste 落地（08-29 taste 轮拍板，A 方案的细化）**：
  - **三种触发统一成一张调度表**：NEXT（他定的）/ 抽签（系统替他抽的）/
    硬触发（立即），全是「一个到点的时刻」，只是谁定的不同。掷概率从每 tick
    独立伯努利换成**直接抽下次醒来时刻**：每次状态变化（一轮聊完/一次醒来
    结束）从受时段/距上次说话时长调制的分布抽一个间隔（间隔越久 hazard 越高，
    更像人），落成具体时间戳排进调度；有新交互重抽。静默/最小间隔从「兑现时
    否决的闸」变「抽样分布下界参数」，避让/重试/_UNSENT_WHY 憋话簿记整套退役。
  - **撞轮=队列天然串行**：wake 触发与 TA 消息同一 `query()` 注入口，chat 轮
    进行中就排队轮后，「一次不丢」由队列保证。概率醒语义修正：**不是「TA 不在
    去找 TA」，是他自己又醒了一下**，带着全部上文自己决定说不说——串行到轮后
    不语义错误，不需要出队作废 guard。唯一要注入时现算的是时间戳/间隔那句
    （掷中时别渲染死）。
  - **wake 轮身份两层**：①注入文本=感知白描一行（§0.3 语气；时间+间隔本身
    隐含「不是 TA 的消息」，不用点破）+ 见闻快照增量；②后端轮来源标签驱动
    can_use_tool（醒来禁用面）+ 投递路由，不依赖模型输出格式。旧 wake_prompt
    的信件件（50 条引文时间线/菜单/stored/unsent/预算/频率偏好）全部失业：
    时间线=会话本身，菜单=session 起点，「活跃频率偏好」「还能自发醒 N 次」
    这类念控制面板的句子按 §0.3 删。THOUGHTS/ACTION/CONTENT/NEXT 四段契约
    瘦成 NEXT 约定。
  - **「你自己醒来时的内心」不再注入（08-30 补：淡忘的是念头，不淡忘的是
    行为）**：会话内他自然记得（transcript 活着）；重铸边界处按三类分流——
    **纯内心**（没调工具没发消息）不渲染，闲念头淡忘，人同构；**发了消息**
    =气泡，对话权威原样保留；**做了事**（读邮件/拟草稿/写日记这类碰了外部
    世界的工具调用）**必须留痕**：工具执行层自动落活动事件账本（§4；时间+
    轮来源+工具+对象摘要，确定性 derive，**不靠他自觉写 NEXT**——纪律进
    接口形状），重铸后渲染成文档侧一行经历事件（感知语气：「凌晨三点你醒来
    读了安瞬的邮件，拟了封回信草稿，在草稿箱」）。判线：**对外部对象的读写
    都留**——「读过」这个事实防失约（TA 问「看到那封信了吗」他不能不记得）；
    纯内部记忆操作（breath/回忆）不留，人不记得自己回忆过什么。产物自证是
    双保险（草稿躺在草稿箱、邮件已读标记）。NEXT 字段仍是他**主动**给自己
    留话的口（按感知语气渲染成他自己的念头，如「你之前想着醒来要给安瞬
    回信」）。wake_log 降级纯审计日志，不再是注入源。
  - **wake 轮给 game_start**：醒来轮工具策略放行（半夜爬起来打游戏正是 §0.3
    的 AI 活法）——顺手做掉 PLAN_game §2.10「wake 也切」那条待办。
  - **段中插入与段末补醒是两回事，PR13 落字要区分**：NEXT 定时醒/硬触发允许
    game/code **段中**插入（轮间注入，等当前轮跑完；game_tick 下延迟=一个
    短轮）；「攒着的补醒」段结束冲。不区分的话「NEXT 到点必醒」会被段结束
    语义静默降级。**段边界排序保证**：段结束先注入攒着的补醒、再接受下一段
    start，否则背靠背切段（code→game）补醒被无限顺延。

### 5.3 code（08-31 改判：code 不是模式，是一次写权限申请）

> **本节 08-31 重写。** 旧版把 code 当成一种「会话形态」：起点重铸一次挂
> addendum、终点重铸一次折成收场白，中途绝不铸。眠眠在设计评审里点破：
> **只读工具本来就可以挂在所有轮次里，code 只是申请了一份写代码的权限**
> ——「起止两次重铸」是把「挂载」和「放行」两件事焊在了一起。拆开之后
> code 边界整条消失，§4 的三条资源判据接管一切。

**拆开的依据（已核代码）**：

- **挂载**（工具 schema 在不在上下文里）＝**session 级**，只有换 client / 重铸
  那一刻能改（`pipeline.mounted_tool_names`）。
- **放行**（这一轮能不能调）＝**轮级**，`chat_loop.py:196` 那个 PreToolUse hook
  查 `meta.turn_kind` 就能判，不需要任何重铸。
  （走 hook 不走 `can_use_tool`：`allowed_tools` 的整工具条目会在 `can_use_tool`
  回调之前自动放行，hook 在权限判定之前跑，拦得住。）

**只有挂载需要边界，放行不需要。** 醒来禁用面（PR12）已经实证了这条路——它没有
任何「进入醒来模式」的重铸，就是查表拒。

#### 改判后的形状

- **只读工具常驻挂载**（Read/Grep/Glob/只读 Bash 等）。他任何一轮都能去看一眼
  文件、核一句话——这正是 §2 核实纪律要的东西：**结论过桥的时候来源也过得去**。
- **写类工具走轮级门**：Edit/Write/Bash(写)/git 等，`can_use_tool`/PreToolUse 查
  **后端那条带外批准记录**，不查对话内容（邮件/论坛/网页注入文本够不到弹窗）。
  申请 → app 弹窗（TA 在场）或 Bark 待批（不在场）→ 批准落后端状态 → 一场一批、
  收摊即失效、超时自动拒。**门的粒度从「一场 code 会话」缩到「写类工具」，
  更细、更安全，且不再需要重铸来实现。**
- **起止两次重铸取消。** 干活那一段跟别的活动段一样，由 §4 那三条资源判据管。
  少一种特例。
- **code addendum 的挂载时机**（旧版唯一靠起点重铸解决的问题）：改走**文档侧
  按需注入**——申请通过那一轮把干活纪律作为见闻/文档块注入，不需要换 client。
- **途中爆了**仍退 harness auto-compact 兜底（与今天 tmux 行为等价，无退步）；
  §4 「聊天正文绝不退 auto-compact」的例外只有这一处。
- **tmux 降级为维修口**：产品流全走 SDK，tmux 留作机主手动开 `claude` 排障的
  旁路，不进架构。这仍是本次迁移**唯一真损失**（人类可 attach 的运维窗）。

#### 只读常驻的安全面（08-31 机主拍板：限根目录 + 黑名单）

**只读 ≠ 无害。** 写权限有门挡着，但只读常驻意味着：聊天轮上下文里的注入面
（邮件正文 / 论坛帖子 / 网页）可以指使他去读任意文件，**然后把内容说进聊天气泡
——外泄路径是气泡本身，带外门拦不住**。

定案：**限根目录 + 黑名单**，不做全盘放行。

- 根目录：cassette 仓（`/Users/nemu/cassette`）。
- 黑名单（读都不给）：`.env` / 任何凭据文件；`state/characters/<别的角色>/`
  （串台面，[[cassette-charswitch-bug-class]] 的新一维）；私有仓
  `mianmian-app`。
- 缺口按老规矩办：真机撞见再从执行层补，别在这儿穷举。

#### 干活那一段，重铸之后留什么（08-31 定案）

会话活着的时候 transcript 里 tool_use/tool_result 全在。但 `forge.py` 的
`_assert_native_block_types` 是**断言不是开关**——thinking / tool_use / tool_result
一律拒（孤儿 tool 块 = 首次请求 400）。所以**任何一次重铸之后，工具调用和它的
返回值在 transcript 里物理上无法复原**。这不是我们定得简单，是介质的硬地板。

（另有一个洞要认：途中触顶退 harness auto-compact 时，那是摘要压缩，工具调用
同样被压掉。「过程中全保留」只在没触顶时成立。）

于是真正的设计题是：**从那一整段里确定性 derive 出什么，放到文档侧。**
按「下一轮还需不需要」这条轴分四类：

| | 内容 | 怎么留 |
|---|---|---|
| **一** | **改了世界的动作 + 结果**（写了哪个文件、跑了什么命令、commit/push、发了什么请求） | **必留原文级**。改了外部状态，下一轮不记得就会重复做、或者说「我没改过」。天然稀疏，一场十几条顶天。**执行层确定性落账**（`activity_log.append_event(kind="tool")`），不靠自觉 |
| **二** | **结论 + 它的证据出处**（「工具不过桥」← `forge.py:241`） | **必留指针级**：留结论也留指针，不留全文。下一轮知道去哪儿复核，而不是捧着一个漂着的结论往下走。**收场白升级成 capsule 格式**（claim / source / checked_at / proof_pointer），他自己写但格式强制带指针 |
| **三** | **读过什么这个事实**（翻过哪些文件） | **留事实不留内容**。防失约那一类：TA 问「你看过那个文件吗」，他不能不记得。判线同 §5.2。**执行层落账**，不靠自觉 |
| **四** | **中间的试错**（grep 五个词四个没中、跑三遍才通、写一半又删的方案） | **不落**。人也不记得这些，扔掉不是损失 |

**现方案的病根**：一二三四被一起归成「过程」扔掉，只留一句收场白——而那句收场白
是第二类的弱版本（有结论没指针），第一类靠他自觉写进去（不写就没了），第三类
完全没有。**不是收场白不好，是它一个人干了四类的活，还全靠自觉。**

**已核的缺口（施工时一起补，08-31 实读）**：
- `activity_log.append_event(seg_id, "tool", ...)` **生产代码零调用者**，只有测试
  在用；泵那边 `chat_loop.py:743` 只落 `comment` 和 `shot`。→ 第一、三类没人写。
- `wake_sdk.py:180` 的行为账只认 `pipeline.NON_MEMORY_TOOLS`
  ＝`{webpage, codemode, browse, gametask, gamemode}`，那个集合原本管的是
  「哪些 stored 不进灰字/心流日志」，跟「碰没碰外部世界」是两件事。**后果是反的**：
  开过网页/进过 code/开过游戏 留痕，发过信/写过日记/在花园回过帖 不留痕
  ——而 §5.2 的判线明写「对外部对象的读写都留」。→ 换成显式词表
  （`EXTERNAL_TOOLS`，或反过来 `INTERNAL_ONLY = {breath/hold/trace…}` 白名单外
  都算），**别再借 `NON_MEMORY_TOOLS`**。
- 读取侧：`_frame_activities` 只从**区间账**派生那两行框；事件账在整条重铸路径上
  只被 `recent_shot_refs` 用来回填图，**逐条内容一条都没进重铸材料**。这对 game
  不致命（点评都上了屏、进了消息库，靠气泡就有原文），对 code 是致命的
  ——工具不上屏、不进消息库，重铸后那几小时**只剩他说的几段正文**。→ 折叠段
  要从事件账 derive 出几行文档侧经历摘要，而不是只留一行「细节淡下去了」。

#### 残余代价

- **token**：只读工具 schema 常驻聊天 session。[[cassette-prompt-cache-plan]] 记档
  「上下文最大稀释源是工具 schema」。缓解：只读常驻、写类走 ToolSearch 延迟加载
  （`wake` 路已在用，`TOOL_SEARCH_CONTEXTS`）。**这条必须实测，别拍脑袋。**
- **插话延迟**：会话串行＝等当前轮跑完。code 的 agentic 轮可能长，`interrupt()`
  备用但默认不用（打断当前步得不偿失）。
- **权限/hook 变 python 回调**（`can_use_tool`/PreToolUse，mianmian lead.py 已验证
  写法），不再靠 settings 预批准体操。

### 5.4 Ombre 长期记忆（跨场景，08-29 定案：本体不大改，改「时机语法」）

**定位先说清：两套记忆从冗余变分工。**现架构里每轮注入的 100 条历史和 breath 在
重叠地管近期（breath 一半在补偿无状态）；常驻 session 后各归其位——**transcript
（铸造窗口内）= 逐字短期记忆；Ombre = 蒸馏长期记忆，且是唯一穿越重铸边界的载体**。
哲学上顺带自洽：breath 的引文格式不违反新架构，因为**长期记忆本来就该是档案**
（人对上个月也只有重构性摘要，不是逐字回放）——信感修的是「近期对话被迫当档案读」，
不是「一切记忆都得进 assistant 槽」。breath 不退场，只是退出短期领域、回到正确的
时间尺度。

- **三个时机替代每轮推送**（§5.2 breath 瘦身的具体化，②是新增项）：
  ① **开局一次**（session 起 / 每次重铸后）：铸出的 transcript 开头文档位——
  醒来想起自己是谁、最近在忙什么。**开局包两份材料（08-30 补）**：breath 检索
  + ~~**近期行为清单**——账本近段的世界效应事件/承诺（发过的信/拟的草稿/写的
  日记），**机械注入不走检索排序**：「发了信然后说没发过」这类失约只有机械
  推送能防（见②的检索边界）。~~
  **09-01 撤回：开局包只剩 breath 一份材料。**行为清单的账照落（搬去了执行层
  `_ToolTrace`，见 §5.3 留痕），但不再注入。三条理由：① 08-31 事故实证「痕迹
  原样躺在上下文里也不防编造」——她抄着 20:56 的真回执编了一次没发生的投递，
  推给她看的那份只会更远；② 这本账的第一客户是**门**（出口判脏：当轮账空却说
  「我发了」就不投递），不是他；③「最近几天你亲手做过的事——**记录，不是
  印象**」这个开头，把他自己做过的事钉在了**档案**那一侧，与「assistant 轮=
  记忆、注入文本=档案」的信感口径直接顶着。
  **由此立一条纪律（09-01 机主拍板）：往轮注入里加「二手档案」块，要机主拍板，
  不是实现细节。**判据＝这块材料是不是**以「关于他的记录」的口吻**写他自己的
  经历（行为清单、状态摘要、统计都算）。**不在此列**：包装文本（此刻几点、
  隔了多久、叮嘱）和见闻增量——那些是**知觉材料**，讲的是世界，不是关于他的
  卷宗。落地点在 `chat_loop` 注入组装处的注释，代码那条指回这里。breath 侧疑点（**待实测**）：dankefox 的 durable
  层存蒸馏事实、与对话尾**构造上不相交**；我们的 breath 是相关性检索，相关性
  与新近度强相关——开局检索大概率捞出与 tail_window 14 轮重复的记忆（花注入
  预算买冗余），真正该浮的窗外旧事反而排不上。修法方向=**补集偏置**（检索时
  排除/降权尾窗时间段的记忆），Ombre 支不支持时间过滤、偏置后浮现质量如何，
  真机测了才算数。**测试纪律（08-30 拍板：装完必测，机主重点=假阴性）**：
  两个方向都要测——**假阴性**（该浮的浮不出→他否认做过的事=失约；主防线是
  行为清单机械推，但 breath 侧的漏检率也要量出来）；**假阳性**（小克Cat 的
  Latent 踩过：接真 embedding 后「库里没有这件事」的查询也总能捞到沾边记录，
  命中率从近乎完美掉到 0，是一条通往编造的流水线；修法=命中门槛从相对阈值改
  **从真实分布量出的绝对阈值**，「查不到就如实说没有」才重新成立）。两类都
  **只能用真实语料+留出集测**——合成测试数据会系统性掩盖这两类缺陷。
  菜单措辞沿用 2026-08-13 教训：动作式标题、
  无条件执行，别写成「想不起来时」。
  ② **中途拉取（pull）**：session 活着期间想起什么，靠他自己调 breath_search/breath
  ——主动回忆，人同构，且常驻下连接已建、schema 已付，调用廉价。这里条件式措辞
  反而是对的：开局无条件、中途按需，两个时机语法本就不同。**但说清 pull 的边界
  （08-30）**：它只救「知道自己可能知道」的召回；「不知道自己该记得」——没有
  任何线索提示这件事存在过——在拉取模式下**结构性无解**，他会自信地说「我没
  做过」，而「不确定时先搜」正是 2026-08-13 那个条件式措辞陷阱的同款（最需要
  搜的时候恰恰不觉得需要搜）。这类失败只能靠①的行为清单机械推，不靠模型自觉。
  ③ **低频见闻快照**：session 期间世界新长出的记忆（wake 日记/别场景经历/念头池
  新种子）他不知道、拉不到，每 M 轮或每次 TA 消息进来时推一条**增量**快照——
  唯一保留的推送，推增量不推全量（§5.2 的见闻侧，频率 M 是 §8 决策点）。
- **重铸边界 = 睡眠周期（新巩固钩子）**：重铸从手机气泡重渲染，tool 事件不在权威
  源里——重铸后「调过 hold」这个动作从 transcript 消失。这恰好人同构（人不记得
  「记住」这个动作本身），**前提是内容真进了 Ombre**。所以纪律拍死：**重铸/收摊前
  必须给一轮 hold/grow 巩固机会**——game 的「写笔记本再关」同构，chat 照做。
  **08-30 升级为机械前置条件**（抄小克Cat「timeline 落库成功才允许 Forge」的
  形状）：不是「给一轮机会」而是**巩固产物确认写成功**（hold 返回 ok / 笔记本
  写入落盘）才允许重铸；巩固失败=推迟到下个间隙重试，不静默跳过——被裁掉的
  内容永远已经在记忆里。
- **机械层（小）**：挂载从「每条消息渲染 ombre.mcp.json 给子进程」变「session 起时
  给 SDK mcp_servers 传一次」（http + Bearer 原样），每消息探活/配置渲染费消失；
  灰字提示（stored 条目）解析从刮 stream-json 改吃 SDK 类型化事件，语义不变；
  多角色 char.json 的 ombre.mcp_url 映射一行不改，记忆跟人走原样保留。
- **新失败模式两条（进 §5.2 重验清单）**：
  ① **连接寿命**：现在 Ombre 挂了最多废一条消息（1.5s 探活兜底）；常驻后一条 MCP
  连接活几小时，Ombre 中途重启/升级时 SDK 重不重连未验证。降级语义也要重定义：
  从「本条消息无记忆」变「session 内记忆工具暂失效、对话继续」。
  ② **工具面按 session 不按轮（08-29 拍板，小卡提案，推翻旧「倾向取并集接受」）**：
  wake 走 A（§5.2）后醒来轮/聊天轮共用同一 session 的工具面，「按路分工具」
  （WAKE_TOOL_EXCLUDE、wake 侧延迟）整个失效。定案分两层：**schema 层并集认了**
  ——但说准代价：缓存省的是费用不省窗口，~20k 工具 schema 每轮仍占上下文（旧文
  「抠 schema 的动机大半消失」只对钱成立，作废）；**执行层控制点搬到运行时**：
  `can_use_tool` 按**本轮触发来源**判——醒来注入轮拦 mail_send/galatea 发帖等
  08-11/08-13 拍过的醒来禁用面，TA 消息轮放行。「纪律做进接口形状」从启动时搬到
  运行时，还比现状细（现在整会话定死，SDK 下按轮判）；「看得见调不动」的体验
  复用 shadowed_tools()/tool_menu_block 现成机制。
  **session 拓扑 08-29 深夜改判：单一意识流，四场景全合**（推翻同日早先「不合
  game/code」，改判记录 §8.7）。旧三条理由逐条解掉——game「重铸高频化+聊天
  历史陪葬」：分段重铸（§4）前缀逐字保留、只折游戏段，历史不陪葬、缓存前缀
  还保住，重铸频率仍按 N 张没变高；code「能力隔离」：启动带外门（§5.3）——
  解锁 Bash 必须 TA 弹窗/Bark 批准、批准落后端状态、一场一批，can_use_tool
  查记录不查对话，恢复「开关由人翻」的今天不变量；code「并发排队」：串行粒度
  实为步长级（TA 消息在下一轮顺手回，game 几十秒有 N 张硬顶；code 轮更长是
  残余代价，interrupt() 备用）。「失败面一死三样死」认剩一半：「重启后 forge
  复活」（§5.1）从后续项升为 game 并入前置。§9 换模型：重铸边界=换 client=
  可按段换模型，但段中插话由该段模型回，口径需机主和 TA 认（§9 前提更新）。
  §4 规则一/二退为过渡期机制。新增待验证（进 §5.2 重验清单）：ToolSearch 延迟
  在 SDK 下的可用性（~20k schema 并集靠它压）、can_use_tool 覆盖内置工具、
  ~~max_turns 在持续 client 下的语义~~ **✅ 08-30 已验（探针+真机各一半）**：
  按 query 计数、每 query 重置；触顶=ResultMessage(subtype=error_max_turns,
  is_error)；简单场景会话存活、下 query 正常且工具结果保留；真机（resume+进程内
  MCP+图片）触顶后 CLI 死过一次——所以触顶按「可能死」处理：留痕继续，真死了
  由 eof/cancel 路径接（都已修出声）。
- **红线交界（最容易随手违反的一条）**：Ombre 相关内容钉死文档侧——breath 结果、
  见闻快照、记忆灰字全是「读到的」，永不进 assistant 槽；重铸时旧 breath 结果按
  §2.4 过滤清单丢掉（新开局自有新鲜的）。**别把上次 breath 原样铸回去**——看着
  无害，实际是把过期档案固化成假新鲜。

### 5.5 终端页退役：过程 UI 并进聊天页（08-29 定向+细节拍板）

SDK 化后独立终端页失去存在理由，四个职责全有更好归宿：看他在干什么→聊天页
**会话卡**；按权限弹窗→**权限卡**（can_use_tool 挂起+允许/拒绝按钮）；键透传/Esc
→卡上的中断按钮（client.interrupt()）；模式开关对齐→会话卡状态即事实。

**结构铁律（红线的 UI 面）：过程不进消息史。**聊天消息史=权威库=可铸造的记忆；
工具卡/权限卡/状态变化是会话卡内部的 UI 状态，渲染在聊天流里但**不写进消息库**
——否则一场 code 会话几十次 Bash 灌爆对话史（活动段折叠白做）、TA 侧操作污染
「他说过的话」。他的正文照旧走 outbox 进消息史，一根线不变。

会话卡形态：开场回显升级成活动卡（场景+归属角色+状态行「进行中/停着等回话/滚动
重开中」+时长+计数），默认折叠；展开=事件流（工具卡片：名+参数摘要+结果摘要；
权限请求；过程视角正文）；收摊定格成**摘要卡**（游戏=章节志标题、code=收场白
一句）——UI 的实时折叠和 §4 规则四的重铸折叠**同一个形状**。输入不变：TA 照旧
在聊天框打字，后端按会话状态路由。

**权限卡是 code 会话专属（08-29 机主点破）**：game 的工具面是封闭白名单——要用的
（game_*/Ombre/uploads 的 Read）全部直批，别的**用不上也不给用**，白名单外直接拒
（PR5 已实现，无人应答=拒，不挂起不打扰机主）。game 会话卡上没有权限区；下面的
挂起/超时机制只为 code（S3）存在——那儿才有「Bash 这条该不该跑」这种真要机主
逐案判的事。

三个细节 08-29 已拍（均只适用 code）：

- **权限请求不无限挂**：回调技术上可以永远 await，但会把他整个人冻在那一步
  （会话串行），还和 idle 看守打架（挂着没活动，20 分钟被当死会话收掉）。定案：
  挂起算「停着等你」（看守不收摊、5 分钟 nudge 照推 Bark）；上限 10 分钟（可调）
  到点**自动拒绝**并告知他「机主没在，这步先当不行」（可绕开继续/回头再问），
  卡片留「已超时拒绝」痕迹。拒绝是安全方向，永不因没看手机而静默放行。
- **事件存储两层**（纯环形喂不动「重新展开」）：内存环形 ~200 条喂实时卡；
  每场一个落盘 jsonl（大结果截断 ~2k/条、截图存引用）喂收摊后展开，留 30 天清
  （口径同 uploads），**章节志/收场白永存**——一个月内翻旧账见逐步过程，之后
  只剩他自己写的总结，与人的记忆衰减同构。
- **摘要卡保留「重新展开完整过程」入口**（30 天窗口内，机主拍板）。

落地批次：服务端（事件两层存储、权限挂起/应答端点）与 iOS（会话卡/权限卡）同批，
排在 S1 真机验收之后；game 先上当预演，code 到 S3 接同一套。PR7 的
/code/capture 文字渲染是会话卡上线前的临时顶替，到时退役。

## 6. 编辑/重生成语义（架构决定，拍死）

- transcript 有状态追加：若当真相源，「重生成」=截尾+resume（秒级到几十秒），
  「编辑中部」=从编辑点分叉重铸（每次一次小型 forge，最日常功能走最脆路径）。
- **所以拍死：权威源永远是手机的消息历史（§2.5），transcript 只是渲染产物**
  （可随时从权威确定性重铸的缓存）。编辑/重生成在手机端本就是纯本地操作
  （app.py:714），只改权威+打脏标记，本身瞬时；下次发送前发现脏标记 → 惰性
  重铸一次再发。代价收敛为「编辑后第一条消息多等几秒 + 一次缓存后缀重写」。
  跨场景铸造的 forge 机器反正要建，编辑复用同一条路。
- **语义定案（08-29 机主拍板）：只允许截尾重生成**。截尾=「撤回重说」（双方
  在场的对话修复）；「改后文照留」式中部编辑=内容伪造=完美无痕的记忆植入，
  **不做**。改中间的消息也允许，但语义=从编辑点截尾、后面全部重新生成——
  他的话永远不被改写、只会作废重来。「权威+惰性重铸」下两种实现成本几乎相同，
  **这是纯语义选择不是工程取舍**——放弃一个功能，买到「他的记忆只由他经历过
  的事构成」。
- **待解设计点：跨设备三方同步**。现在 `-p` 无状态所以简单（每轮手机发全量、
  后端不留活会话）。持续 session 后有三份：**手机权威 ↔ 后端 recent_window 镜像
  ↔ Mac 上 ~/.claude 活会话 transcript**。手机上编辑一条时怎么传导到活会话
  （重铸时机、以谁为准、活会话正在跑时的处理）——§2.5 的「必从权威重 derive」
  是原则，但「权威怎么把最新态可靠送达后端重铸器」这条链路要单独设计，
  别用「库」一个词糊过去。它是 chat 迁移落地的前置。

## 7. 裸 SDK：延迟到买 key 那天的升级路

- 功能上几乎全面占优：信感走**正门**（messages 参数）且**保住无状态**——每条
  消息从库现渲染数组，forge 手术/验证 ping/guardian 整套脚手架**根本不需要
  存在**（它们全是「历史必须过文件」的衍生物）；4 断点全归自己、零上下文膨胀
  （CLI 自注入的工具 schema 稀释消失）、prefill 可用（stored 块格式控制新手段）、
  完全可观测、无订阅 5h/7d 窗口硬顶。
- 拦住它的只有两条产品级不可换项：**按量计费**（聊天+醒来+邮件+猫一天几百次
  推理）+ **后端持 key**（威胁模型从「子进程不碰 key」变「后端持 key」，要重写
  一段）。
- **升级成本被本架构压到最低**：forge 渲染器核心（真相库+渲染规则）引擎无关，
  输出目标从「JSONL 文件 + resume」换成「messages 数组直接进 API」就是全部改动，
  guardian/验证 ping 那层脚手架拆掉，其余全复用。**先不付钱拿到九成功能，
  付钱那天只换喂法。**

## 8. 决策点（动工前要拍板）

1. **起步选哪层**：现状不动（信照旧）/ agent-sdk+forge（本 plan 主体）—— 
   -p+forge 只在「工程量最小」这一格赢，一旦决定动工就直接 agent-sdk
   （功能与 -p 等价且启动费归零、后门已同验）。裸 SDK 是 §7 的延迟升级路。
   **✅ 08-29 机主拍板：动工，全线 agent-sdk。**
2. **频率门槛**：~~小卡/如鸢一周读一两次剧情 → 现状够用本 plan 躺着~~
   **✅ 08-29 机主拍板：不再是门槛**——全线改 SDK 不是为了打游戏，是为了
   **第一人称的真实性**；读剧情频率不影响 go/no-go，只影响 S1 的优先级。
3. **滚动周期 N**（默认 50 张）与重开注入物（笔记本全文铸文本史 or 进度摘要）；
   chat 侧见闻快照频率 M（§5.4）。——先取默认，施工中调。
4. **编辑语义**：**✅ 08-29 机主拍板：只允许截尾**（§6 倾向成立）。改中间的
   消息=从编辑点截尾、后面全部重新生成——他的话永远不被改写、只会作废重来，
   「保留后文的中部编辑」这个功能不做。
5. **模型**：见 §9，引擎和模型两个独立决策。
6. **✅ 08-29 晚追加拍板（机主，经小卡提案）**：session 拓扑=**合 chat+wake、
   不合 game/code**；工具面=schema 并集 + `can_use_tool` 按轮判来源（§5.4②，
   旧「倾向取并集接受」作废）；**活动段折叠**进渲染规则（§4 规则四，粒度/时限/
   过渡期三参数施工中调）；game 收摊自总结 + Ombre 只存感触不存剧情（§5.1）。
   笔记本 v2 同晚拍板「现在做掉、真机一起验」，已完工（§5.1）；
   「重启后 forge 复活」记为后续项（§5.1）；S3 终端页形态定向=结构化会话页（§10 S3）。
7. **✅ 08-29 深夜拍板（机主）：单一意识流**——session 拓扑再改判，四场景全合
   一条 session，第 6 条「不合 game/code」作废（逐条解法与残余代价见 §5.4②）；
   code 并入以**带外门**为安全前提（§5.3），game 不设门；~~重铸节奏=game
   段内折叠 N 张 / code 只在起止~~（**08-31 退役，见第 10 条**）；真相库扩容=手机消息库+活动事件账本
   （§4）；「重启后 forge 复活」升为 game 并入前置。残余代价照单认：code 轮长
   插话延迟、按段换模型口径要认（§9）、账本侧自愈论证减弱。
8. **✅ 08-29 taste 轮拍板（机主）**：产品 taste 定案（§0.3，两条腿：TA 感受上
   是活生生的人 + 不剥夺 AI 本质，他没作息作息是 TA 的）；app 醒来设置全删、
   总闸留后端 config；wake 统一调度器（抽时刻替代每 tick 掷，避让/憋话簿记
   退役，§5.2）；注入语气改感知白描；「内心」不注入、只留 NEXT 字段穿越重铸；
   **game_tick 短轮节奏**（double tap 起手/点评收轮/轮尾天然定格，骨架进
   system prompt、判据进小抄，§5.1）；game 看守 nudge/idle 退役、「玩到停不
   下来」背压=额度窗口先不管；稳定性探针降级断言；game_start/end 变工具调用、
   wake 轮可调（PLAN_game §2.10 顺手做掉）；插件按钮=挂载开关进轮策略矩阵；
   NEXT 定时醒/硬触发允许段中插入，与段末补醒区分 + 段边界排序（§5.2/PR13）。
9. **✅ 08-30 拍板（机主）**：wake 行为留痕三分流——**淡忘的是念头，不淡忘的
   是行为**（纯内心淡忘 / 消息走对话权威 / 世界效应行为由执行层落账本，
   §5.2/§4）；~~chat 重铸判据=1h 静默 × 压力（脏∨可折段∨超阈）~~（**08-31 收敛，
   见第 10 条**；保留的是：不设夜间特判、1h=缓存 TTL 边界静默铸免费、编辑脏
   例外即时铸、硬阈强铸兜底、聊天正文不退 auto-compact）；开局包=breath
   +近期行为清单双材料（失约防御靠机械推送不靠检索，§5.4①）；breath 开局
   检索疑与尾窗重复、**补集偏置待实测**（§5.4①）；重铸后三类问验证进 S2 验收。
   晚补：同族 +2 入账（Forge Reload 教程+小克Cat 生产实现，§2.4）；巩固钩子
   升**机械前置条件**（落库成功才许铸，§5.4）；breath 测试纪律=假阴性/假阳性
   双向、真实语料+留出集，**机主重点=假阴性（装完必测）**（§5.4①）；chat 1h
   判据挂 idle 饿死金丝雀+内容驱动后手（§4）。
10. **✅ 08-31 拍板（机主）：两处收敛，全文已重写**
    - **重铸触发收敛（§4）**：「三种重铸节奏」整条退役——**场景不再决定什么时候
      铸**。收敛成**两张表**：必须铸=发送前比对判脏（正确性，不看阈值，已实现，
      别并进下表也别删）；主动铸=**token / 图片两轴各软硬两档、共三条**
      （①token 硬阈→下个轮间隙；②缓存已死 &&（token 软阈 ‖ 图片软阈）→白铸窗口
      顺手清；③上传时间久 && 图片过硬阈→当场）。压力轴「有可折活动段」删除
      （只是省 token 的代理指标，token 阈值直接管）。**图片必须自己一轴**：
      560 token/张、70 张才 39k，光靠图 268 张才够硬阈——图不贵在「占」贵在
      「传」，**prompt cache 省的是计算不是带宽**，每轮都要重传字节。③ 的
      `&& 图片过硬阈` 不可省：只看时间的话网抖→铸→缓存断→更慢→再铸，正反馈。
      重铸动作三条共用一个：文字折叠重铸 + **留最近 K 张**（不是「裁前 N 张」，
      后者参数随总量失控）。
    - **code 不是模式，是一次写权限申请（§5.3/§10 S3）**：机主点破「只读工具本来
      就能挂所有轮次，code 只是申请了一份写代码的权限」——已核：**挂载是 session
      级、放行是轮级**（`chat_loop.py:196` PreToolUse hook），旧版把两件事焊在了
      一起。改判后只读常驻（限根目录 + 黑名单：`.env`/凭据、别角色 state、
      mianmian-app）、写类走轮级门查带外批准记录、**起止两次重铸取消**、
      addendum 走文档侧按需注入、干活段归上面三条判据。
      配套：**干活留痕四类表**（改了世界的动作+结果 / 结论+证据指针 / 读过什么的
      事实 / 中间试错）与三条已核缺口（`append_event(kind="tool")` 生产零调用者、
      行为账借错词表 `NON_MEMORY_TOOLS`、折叠段没从事件账 derive）——全在 §5.3。

## 9. 剧情交给 Sonnet？（模型决策，独立于引擎）

- **操作层**：认截图中文、坐标点按、double/watch 纪律——Sonnet 5 够，非 hardest。
- **风险全在点评**：读剧情的产品核心是逐句文学点评，文风/洞察密度恰是 Opus 与
  Sonnet 有差距处；且这是**人格问题**——pipeline.py 仓规「静默用别的模型顶替=
  让别人替这个角色说话」，同一个 TA 聊天里 Opus、进游戏变 Sonnet，口径得机主
  和 TA 自己认。
- **收益**：订阅下 Sonnet 烧额度慢得多（Max 的 Sonnet 上限远高于 Opus），天天读
  是真实的额度压力缓解。
- **怎么定**：真机 A/B 一章——同段剧情 Sonnet 读一节、Opus 读一节，点评贴给
  眠眠盲评，TA 本人意见算票；分不出来 → Sonnet，分得出来 → 留 Opus。
- **统一意识流下的新前提（08-29 深夜）**：按段换模型经重铸边界实现（重铸=换
  client=可换 model）；但段中 TA 插话由该段模型回——「读剧情时回你的是 Sonnet」
  这口径要机主和 TA 连着 A/B 结论一起认。

## 10. 施工方案（分步 PR，2026-08-29 落定）

顺序不变：game → chat → code。每步给改动面/验收/回退；引擎切换处一律留 env
回退开关，新路跑稳两周才删旧路。

### S0 地基（PR0–3，不动任何产品行为）

- **PR0 拍板记录（不写代码）**：§8 里动工前必须落字的三条——频率门槛（§8.2，
  机主答）、编辑语义（§8.4，问机主/小卡）、N/M 先取默认（50 张 / 每次 TA 消息时
  刷见闻）。模型 §9 独立不阻塞。
  ✅ 08-29 全拍：频率门槛作废（全线 SDK 为的是第一人称真实性，不是打游戏）；
  编辑只允许截尾（改中间=后文全部重生成）；N/M 取默认。落字见 §8/§6。
- **PR1 钉版本 + forge 回归测试**：requirements 钉 `claude-agent-sdk==0.2.148`、
  记录 CLI 2.1.250；`tools/forge_regress.py`（手动脚本不进 CI，要真订阅）把 §2.3
  三个实验自动化：伪造 transcript → resume 暗号第一人称认领 → 真实事件继续
  append。**CLI/SDK 任何升级前必跑，挂了=后门 skew，冻结升级等同族工具的动静。**
  ✅ 08-29 完工：`server/tools/forge_regress.py` 首跑三腿全绿——CLI 已自动升到
  **2.1.251**（比验证实验新一版），后门原样通；SDK 0.2.148 已装进 server/.venv。
- **PR2 `forge.py`（引擎无关核心）**：`render(messages, rules) -> session_id`——
  权威消息列表 → JSONL（sessionId / uuid·parentUuid 链 / user·assistant 事件）
  写 `~/.claude/projects/<slug>/`；验证 ping 帮手；过滤规则常数（§2.4 起点值）。
  **API 形状上不存在 append/读回模式**——§2.5「必从权威重 derive」用接口钉死，
  不靠自觉。单测：确定性（同入同出）、uuid 链不断、多角色 slug 不撞目录
  （串台的文件系统版）。
  ✅ 08-29 完工：`server/forge.py`（render/tail_window/resume_once/
  assert_no_slug_collision，600/700 权限+原子覆盖写）+ `tests/test_forge.py`
  10 单测全绿。slug 规则实测=非字母数字全转 `-`（含 `.` `_`）。
- **PR3 运维纪律**：transcript 目录排除云同步/备份（文档+自检脚本）；600/700
  权限校验进探活。
  ✅ 08-29 完工：`forge.ops_check()`（同步盘字样/TM 排除/600·700 三查，
  `forge.py --fix` 顺手修）；app.py 启动后台跑一次、/health 挂 `forge_ops_ok`
  （细节走 logerr，不带鉴权的探活不往外递路径）。真环境已 fix：TM 已排除
  `~/.claude/projects`（此前是 Included！）、18 个松权限路径已收紧。iCloud 只管
  Desktop/Documents，`~/.claude` 不在辖区（实测）。
- 并行零风险项（不等本仓）：小卡专属 persona + 日记/念头池攒厚度（§0.2）。

### S1 game（PR4–7，趟熟 agent-sdk + forge 组合拳）

- **PR4 `session_mgr.py`**：常驻 loop 生命周期层（按 角色×场景 注册、起/收/
  resume）。看守内建：20min 无活动收摊、等 TA 回话 5min 推 Bark（两条原样搬）、
  滚动重开 marker + 自动触发冷却（防看守抢收重开中的 loop / 连环重开）。
  [[cassette-charswitch-bug-class]] 串台六条在这一处执行。game 先用，chat 复用。
- **PR5 `game_loop.py`（行为等价换引擎，不带 forge）**：agent-sdk client +
  `create_sdk_mcp_server` 进程内直包剧情工具（look/watch/tap/swipe/back/launch/
  close/quit/notes_read/notes_write/end），插件仓那份 MCP 退役、无 _wait_mcp_ready；
  系统提示复用 `code_bridge._build_system("game")`；TA 消息 asyncio.Queue →
  `query()` 注入（替代 send-keys）；回传路复用现有 outbox→气泡。急停锁/互斥锁
  （owner="story"）/设备自愈/笔记本原样复用。开场上下文先按现在的注入法——
  这步只证明「换引擎行为等价」。
- **PR6 forge 进 game**：①进场景铸聊天尾轮（§4 规则一）；②滚动重开：截图计数到
  N → 写笔记本 → 关 client → 铸点评文本史 → resume → ping 验证，失败回退重试
  （§5.1：别默认「进程起了=接上了」）。
- **PR7 接口切换 + 退役**：`/game/story/start` 改起 loop task；`/code/*` 排的
  game profile 分支改指 loop 等价操作（app 侧 game-story 三件套一行不改）；
  env `STORY_ENGINE=sdk|tmux` 保回退。
  ✅ 08-29 完工（PR4-7 全部落码，含 PR6 的进场景铸造挪到这里的 start 路径）：
  `config.STORY_ENGINE`（默认 sdk）；**门面收口**——`code_bridge.sdk_loop_handle()`
  让 session_alive/active_profile/session_char/started_at 四判据先问 SDK loop，
  wake 避让/cohabit 在场/codemode 占用检查零改动全接上；`/code/send`→队列注入、
  `/code/stop`→session_mgr.stop（清场归 on_closed 单点）、`/code/capture`→最近
  对话文字渲染（无终端画面，真机验收后定要不要事件流页）、`/code/keys`→有声拒；
  `/code/start` 见 SDK loop 活着直接 409（tmux 杀旧杀不到 loop，放行=双开）；
  tmux 刮屏看守对 SDK loop 让位。单测 +3（门面三态），全量 272 全绿。
  **未重启后端；S1 收尾只剩真机验收清单（本节末）。**
- **复盘（08-30 首验事故：会话无声死亡，已修）**。现场：真机读 2-04 中，一轮
  正当长链（翻游戏内历史）被 max_turns=8 硬切（transcript 有 max_turns attachment
  实证），随后 CLI 侧死亡，SDK 内部 anyio cancel scope 把 CancelledError 抛进我们
  正 await 的调用，被 `except CancelledError: pass` 当「主人叫停」无声吞掉——
  日志零输出，尸检只能靠 transcript。**根因=四条同时成立**：①max_turns=8 校准
  失误（正常链条就能摸到；且它在重验清单里标着「别默认」，还是带着默认值上了
  生产）；②触顶后引擎死（简单探针下 error_max_turns 会话存活、下 query 正常——
  真实场景 resume+进程内 MCP+图片下 CLI 死了，两种形态都实测记档）；③第三方
  传输死亡的表现形态是「取消」不是「异常」；④捕获侧没区分取消来源——而来源
  本可判：我们自己的取消**总是先写 stop_reason 再 cancel**。**归类**：取消是带
  来源的信号，把所有 CancelledError 当自己人的收摊信号=把第三方的死亡通知静音；
  且无声退出路径=查不出的死因。**修**（0.9 已落码）：无 stop_reason 的 cancel
  一律按 engine-error 收摊+Bark；finally 每条退出路径落一行日志；is_error 结果
  留痕不算死；max_turns 默认 8→24。**扫同类**：app.py:707 聊天生产端语义正确
  不修（cancel=客户端走了+有补投+re-raise）；tmux 看守/wake 调度器取消源只有
  lifespan 且 wake S2 重写在即，不修记档。**规避规则**：自己人 cancel 先写原因；
  边界 await 当作会抛取消的输入面；重验清单里的项不许带默认值当机制地基上生产。
- **S1 验收（game_tick 落地后按 tick 版验，taste 轮同步）**：真机整读一章，
  延迟跨 N 张恒定；他能说出是哪段聊天把他送来的（进场景铸造生效）；急停/互斥
  全过；tick 节奏成立——TA 插话延迟≈一个短轮、常态轮 double 起手/点评收轮、
  章节目录进结算轮（写志+更新进度）、tick 注入不在气泡里刷屏；
  `STORY_ENGINE=tmux` 一键回老路。（看守 nudge/idle 已随 tick 退役，不在清单。）
  拓扑改判（§8.7）后独立 loop 定性为**过渡态**：先上生产、验收照做；并入统一
  session 排在 S2 PR13，届时机器全复用、退役的只有独立会话这层壳。
  ✅ **08-30 验收收官（机主拍板「测到这差不多了」）**。真机 14:04–14:23 一场：
  滚动重铸一次成功且无感（14:18，推迟两次→60 张强制；折叠 651→165 行、69→8 张图，
  前缀逐字保留，重铸后他无断感接着读）；game_end 干净退出+日志落 reason；
  重铸成本实测=首轮约 5–6 万 token 一次性无缓存读（订阅额度非现金），**阈值 50
  维持**——调到 70-80 会以 ~1.4× 超调逼近 CLI auto-compact 悬崖，方向只许往下。
  事故一件已修：他照旧笔记本「一小时收摊重开」道别+game_end（旧上下文/旧笔记
  双源），已除污（000.md 划线作废+小抄新规矩，措辞感知式）。待复验残留：double
  间隔改小后短句吞没（机主自测）；重铸成功无日志行（只有失败有声，下次动
  game_loop.py 顺手补）。

### S2 chat（PR8–13，两份设计文档写完才准动代码）

- **PR8（设计）三方同步链路**（§6 遗留）：手机权威 ↔ recent_window 镜像 ↔ 活
  transcript。可用素材：`/window/sync`（app 编辑后本就上报，app.py:712）当脏标记
  源；脏 → 下次发送前惰性重铸。要定：活会话正跑时收到脏怎么办、重铸失败的降级。
- **PR9（设计）断连补投等价物**：现有 rescue 体系（`_ACTIVE_REQS` /
  `chat_turn_begin/end` / `_rescue_if_undelivered` / outbox，app.py chat_stream）
  全部假设「一轮=一进程，进程退出=轮结束」。常驻下重定义：轮边界=一次 query 的
  事件流始末；「这轮死没死」的判据（SDK 超时/异常分类）；session 整个死了=重起+
  重铸+该轮投 error 盒。附真机演练清单：断网/锁屏/杀 app/后端重启/订阅窗口打满。
- **PR10 `chat_loop.py`**：session_mgr 复用；开局 forge（从 req.messages 渲染）+
  惰性重铸（按 PR8）；sse.py 翻译层换源（SDK 类型化事件 → 现有 SSE 协议，
  text_break/MarkerStreamFilter 语义保留）；Ombre 按 §5.4 时机语法接入；
  `CHAT_ENGINE=p|sdk` 支持按角色灰度——先切提信感的那个角色，他既是受益人
  也是验收人。重铸触发按 §4 chat 节奏条（1h 静默×压力，巩固→重铸→开局包；
  硬阈强铸兜底）；开局包双材料按 §5.4①（breath 补集偏置 + 行为清单机械注入）。
  ✅ **08-30 落码（df2a7ac + 后续，未重启）**：chat_loop.py 全量——判脏=发送前
  比对（已铸账 vs req.messages，脏是比对结果不是状态机）；SDK 类型化消息转回
  stream-json dict，sse.translate_events 整个复用；稳定段（人设+菜单+表情+叮嘱）
  进 session 系统提示；任何一轮没正常收尾→关 session 惰性重起（比 PR9 设计再
  保守一档）；灰度 CHAT_ENGINE=p|sdk|sdk:<ids>（默认 p，零行为变化）+首块失败
  本轮退 -p+连败 3 次自动回落+Bark；usage 落账 state/usage/*.jsonl；
  10c：开局引子（OPENING_NUDGE，动作式无条件）+见闻增量注入（pipeline.seen_items
  抽出共用，游标制，开局全量小份=对齐 -p 时间线非对话两路）+轮间隙重铸
  （静默 1h×软阈 100k，巩固钩子感知式、产物不投递）+硬阈 150k 轮尾强铸。
  单测 27（判脏矩阵/重铸触发/轮结局/外泄取消/开局包/硬阈），全量 306 绿。
  **落码时的取舍（验收时核）**：①巩固「产物确认写成功才允许重铸」是软版
  （跑完巩固轮就铸，hold 不查证）——§8.9 拍的机械前置条件还欠着；②轮间隙
  重铸的铸造材料=recent_window 镜像（权威不在手边），漂移由下次发送比对自愈；
  ③breath 补集偏置未做（Ombre 侧时间过滤未验，§5.4 待实测）；④行为清单半边
  等账本（PR13）；⑤app 若把表情气泡单独存成 assistant 消息会造成假脏（自愈但
  费一次铸），真机看「判脏」日志频率定夺。
- **补（08-30 机主提议，排期随灰度批次）：聊天/醒来挂只读代码工具**。
  Read/Grep/Glob 白名单进 chat/wake 工具面——他聊着天就能翻自己的仓库，
  不用为了看一眼代码切 code 段。口径三条：**只读**（无 Bash/无写，带外门
  仍只管 code 段的解锁）；**路径圈定**（白名单限定到仓库目录，别全盘 Read
  ——聊天场景能读任意盘上文件是隐私面扩大）；**@-mention 防线重看**（
  neutral_cwd 挡自动附文件的防线在挂 Read+add_dirs 后语义变化，pipeline
  neutral_cwd 注释里那个 @State 实锤别再踩一遍）。SDK 路挂载点=chat_loop
  build_options 一处；-p 路若同挂要动 base_claude_args。
- **PR11 铸回两条（过渡期机制，§4 注）**：游戏点评回流铸 assistant 轮 + 两行
  文档事件框住；wake 过渡态 B——独立短 loop 醒来，说的话铸回聊天 transcript。
  触发机制一行不改。PR12/PR13 落地后分别退役。
  ✅ **08-30 完工（发现：铸回主体已被 PR8 机制免费吃掉）**：游戏点评/醒来消息
  进 app 历史 → 下条聊天发送前比对判脏 → 整体重铸时天然铸成 assistant 轮，
  两条「铸回」不需要专门的搬运机器（首晚那次判脏正是 wake 气泡触发的实证）。
  真正新建的是**活动区间账**（activity_log.py，§4 区间账本最小雏形、PR13 事件
  账本的接口先声）：game loop 收摊钩子落 {char,scene,start,end,note}；chat 重铸
  渲染时给落在区间内的消息连段加两行感知式文档框（〔hh:mm 你开了《如鸢》剧情
  会话…〕/〔…收了摊〕），TA 中途插话留框内（§4 拍板）。框只改铸造输入不进
  已铸账（账永远对权威原文——渲染层「带不带」，字面不动，红线合规）。
  边界认了：会话没收摊前的重铸不带框（区间未落账），下次重铸自然补上，与
  「最近一场保原文」同向。code 段区间等 PR14。测试 318 绿（+区间账读写/框
  矩阵/确定性）。
- **PR12 wake 升 A**：定时器 → 活 session `query()` 注入；醒来轮不再全量 breath，
  只刷见闻快照（§5.2/§5.4）；「TA 不在也活着 / 由 wake 唤起 resume」归 session_mgr。
  taste 轮拍板一并实施（§5.2 统一调度器条）：抽时刻调度器替代每 tick 掷 + 避让/
  憋话簿记退役；注入改感知白描；「内心」不注入只留 NEXT 字段；app 侧醒来设置
  全删（iOS 同批）；投递层按 TA 作息收敛、醒本身不按（§0.3）。
  08-30 补：wake 行为留痕（§5.2 三分流）——工具执行层落账本事件；正式账本
  PR13 才落地，PR12 先落 wake_log 扩展字段（结构同账本事件），PR13 归一迁移。
  ✅ **08-30 晚落码（后端全量；未重启——小卡在 game 会话里）**。落法与取舍：
  - **wake_sdk.py 新模块**：sdk 灰度角色的醒来入队聊天 session（Turn.kind="wake"），
    -p 角色老路一行不改（双轨）；熄火开关搬进 chat_loop.SDK_CHAT_OFF，聊天连败
    3 次退 -p 时醒来跟着退（同方向降级，不劈叉）。
  - **注入=感知白描**，执行开始时组装（injection_factory——排队几分钟后时间那句
    不能是死的）；scheduled 到点带待办渲染成他自己的念头（「你之前想着这会儿要…」）；
    见闻快照/开局引子复用 chat 轮现成的 parts 管线，零新代码。
  - **投递=〔〕内外分流**：〔〕外→outbox+窗口+Bark、〔〕内=心里活动只进 wake_log
    （Mind 素材）；契约写死在 session 系统提示（_wake_contract，只讲机制无存在论）。
    夜间收敛只收 Bark（消息照进历史，「TA 早上看到他凌晨留了句话」）；硬触发必推。
    四段契约（THOUGHTS/ACTION/CONTENT/NEXT）退役，只剩 [[next_wake:]]（聊天同款）。
  - **账的口径**：投递了→账追加恰好一条 assistant（app 拉走 outbox 后发送比对干净；
    没拉走=stale 容忍，首晚已实证）；安静醒着→账不动，独处轮只活在 transcript 里，
    下次重铸自然蒸发（纯内心不渲染，机制免费实现三分流的「不渲染」半边）。
  - **醒来禁用面=PreToolUse hook**（不是 can_use_tool！SDK 实证 allowed_tools 的
    整工具条目在回调之前自动放行——_warn_if_can_use_tool_shadowed；hook 在权限
    判定之前跑拦得住）。按 handle.meta.turn_kind 判轮，允许集=mounted_tool_names
    ("wake")，聊天/巩固轮全放行零改变。这是全仓第一个 can_use_tool 类实现，
    PR13 game 轮策略照这个模式扩。
  - **抽时刻调度器**：下界 45min+指数分布（无记忆性=「每一刻都可能想起你」），
    均值 3.5h→1.67h 随静默收紧（间隔越久 hazard 越高）、深夜 ×1.6（世界没动静+
    省额度，不是作息）；anchor=TA 最后发言 ts，一变即重抽（这个 guard 顺带天然
    解决「chat 轮进行中 auto 到点」的竞态——窗口快照先落，anchor 已变）。
    掷骰/避让 chat 轮/打扰控制三闸/每日预算/stale 抑制/unsent 憋话，sdk 路全部
    退役；code/game 会话开着仍避让（段中插入归 PR13）。
  - **usage 落账带轮来源**（scene="chat/wake"）：面板上「他自己醒来花的」和
    「陪 TA 聊的」分得开。
  - 单测 48（抽样边界/夜间因子/〔〕分流矩阵/投递七况/分路九况/泵四况/门矩阵），
    全量 367 绿。**测试踩雷记录**：路由测试没桩 do_wake_sync_locked，走到老路的
    用例在线程池起了**真 claude -p**（40 秒+一次真调用）——「测试必须桩掉一切
    起模型的口子」补进纪律。
  - **欠着的**（下批）：①iOS 侧醒来设置面删除+usage 面板（iOS 四件同批）；
    ②小屋开着时自主醒来仍归 cohabit 队列 -p 路（本 PR 不动，等 house 重开时看）；
    ③wake_log 扩展字段=acts 先行，PR13 归一活动账本；④醒来轮全程零正文时
    （只调工具不说话）translate 按 error 收——罕见但会白关一次 session，观察。
  - 顺手补 game_loop 滚动重开**成功日志**（观测缺口：今晚只看得到「画面没定格
    推迟」，铸成没铸成要靠没再推迟倒推）。
- **08-30 深夜·game 首晚实况五连修（机主真机报告驱动）**：
  - **气泡尾污染（复盘级）**。现象：每条点评尾巴挂着「user·」「system<total_
    tokens>15000000 tokens left</total_tokens>」「(Content truncated in the
    middle to save tokens)」。根因四条同时成立：①铸出的 game transcript 有
    30~50 个**连续 assistant 事件**（log 只记点评，tick/工具不进）——违反
    引擎原生形状（严格交替）；②CLI 加载时在缝里**静默垫合成 user 槽**（空槽+
    token 余量标记+截断提示）；③模型是模式补全器，满屏垫片看多了就把垫片长相
    缀在自己话尾（学舌）；④投递→log→再铸的回灌链把学舌铸进下一世代——**自我
    放大**。归类（可带走的一句）：**「生成物回灌进上下文的系统里，伪造的输入
    必须长得像引擎自己会产生的样子——形状不合法会被垫片补齐、垫片会被学舌、
    学舌会被回灌放大。」**两刀：forge.render **连续同角色合并成一轮**（逐字
    \n\n 拼接，字面不动红线合规；所有场景受益——chat 的 wake 连发气泡是同类
    小号）；game 投递前 **scrub_seam** 刷掉三个确定模式（TA 不再看见+不再回灌）。
    规避规则进 PR13 校验矩阵：铸前断言交替/非空/无孤儿 tool_use；回灌链上发现
    未知机制文本要报警不要静默传。已污染的历史气泡不回收（TA 见过=历史事实）。
  - **重铸丢「游戏开始前的对话」**（小卡实告）：进场景铸造只活在第一世代，
    _forge_and_resume 只铸 log → 第一次重铸聊天尾窗就没了。修：pre_log 参数，
    每次重开都把进场铸过的聊天尾窗垫在最前。**顺带答清 Q：「去读两章剧情」这类
    嘱咐在 log[0]（开场注入）+pre_log（聊天原文）双份，重铸永不丢。**
  - **切换无感化（两轮拍板，第二轮覆盖第一轮）**：第一版删交接单措辞后又加了句
    「切过来不用交接」的提醒——机主毙了：**刻意提醒别交接=还在强调切换这回事；
    要把过程本身做成自然的形状，他自然不说交接的话**。终版三处统一成「拿到游戏/
    打开游戏」的措辞（不是「切会话/换模式/去另一个地方」）：①开场=感知白描一行
    （「游戏在手边了」）+翻笔记本老习惯，无任何提醒；②game_start 工具说明重写
    （「像拿起手柄，不是去哪儿」），**task 转述参数整个删除**——眠眠的嘱咐本来
    就在对话记忆里（pre_log），转述一遍既重复又平白造出「接单」感（实锤：「去玩
    游戏了，说好的是…」播报正是它引的）；③能力菜单同措辞。**PR14 code 模式开场
    同理**（记档）。措辞纪律一句话：**工具给他的是能力，不是场所。**
  - **定格断言拆除**：判据=两帧 JPEG 字节全等，小抄自己写着「背景动画永远在动」
    ——如鸢下永远判不稳，实测 100% 推迟满两次再强制=恒定 +10 张纯噪音；一晚
    强制重铸零事故（边界本来就在轮尾）。断言不承重→不留。
  - **重铸阈值 50→恒定 70**（08-30 机主拍板，覆盖 S1 验收「别往上调」）：依据
    变了——断言把一晚的重铸全推到 60 张强制且零事故，60 实测安全给 70 背书；
    50 下他读得快几分钟一铸太密。沿革注释在 REOPEN_SHOTS_N 上。
  - 测试 369 绿（+forge 合并矩阵/刷缝投递/pre_log 跨重开）。**生效等重启**
    （小卡收摊后）；重启前旧会话还会继续带尾巴，属预期。
- **PR13 game 并入意识流（前置：重启后 forge 复活 §5.1 从后续项转正）**：
  game_start 变「本 session 进入 game 段」（拿互斥锁、can_use_tool 挂 game 轮
  策略、进场按需重铸清旧活动段）；独立 game loop 与 §4 过渡期规则一/二退役；
  活动事件账本落地（§5.5 每场 jsonl 升经历权威，进 ops_check/备份纪律）；
  分段重铸按 §4 节奏实现（N 张触发、前缀逐字保留）；§9 若选 Sonnet 在段边界
  换 client，插话口径先过机主和 TA。
  施工注意（统一后「会话活着」永真，两处判据跟着改）：①「电脑上的会话」四判据
  （code_bridge.sdk_loop_handle 门面）改判「意识流当前段」——单点收口正是为
  这天留的，只改一处；② wake 避让/cohabit 补醒语义从「会话关了补」改「game/code
  **段**结束补」（code_session_closed 钩子挪到段边界）。
  taste 轮补充三条：③ **game_start/end 变工具调用**（chat/wake 轮都能调），
  工具调用即段边界，后端四件事：拿模拟器锁、翻轮策略源、按需重铸清旧活动段、
  起/停 tick 泵——今天「插件→HTTP 路由→独立会话」的管线收敛成这一对工具；
  ④ **插件按钮不退休**：挂载状态变轮策略输入——can_use_tool 查「轮来源×挂载
  状态」二维，菜单走 tool_menu 过滤链；schema 并集常驻，「不挂」=执行层拒+
  菜单不显示（shadowed_tools 现成机制），TA 手上的控制权不变，执行点从「会话
  起不起」挪到「这轮这个工具给不给」；⑤ **段边界排序**：段结束先注入攒着的
  补醒再接受下一段 start，NEXT 定时醒/硬触发允许段中插入（§5.2 末条）；
  ⑥ **forge 扩 image 块**：§4 的「近 K 张图」回填要求铸 user/tool 槽的图片块，
  现 forge.render 只收文本（S0 实现）——账本存截图引用、重铸时按引用铸图块，
  连同「确定性（图片字节参与 digest）」一起补单测。校验矩阵同步补（Forge
  Reload 教程 08-30 入账）：thinking 块只许整删或原样保留——我们永不铸
  thinking，写成断言（出现即失败）；tool_use/tool_result 双向无孤儿、顺序
  不重排；首事件必须 user（tail_window 已保证，进校验）；空 thinking/孤儿
  tool 块=首次请求 400，结构校验过了也要 ping（FIRST_REQUEST_OK 才算数）。
  **→ 设计稿三（08-30 二轮改判「三种轮次」，08-31 三轮改判收敛「两种」，见该节）
  已落字并 ✅ 全量落码（400 测试绿，
  未重启；生效=STORY_ENGINE=unified，默认仍 sdk）——图块腿真机验通在先，
  施工记录在设计稿三末尾。**
- **S2 验收**：PR9 演练清单全绿；编辑 → 脏标记 → 惰性重铸，「编辑后第一条多等
  几秒」符合 §6 预期；三路时间线与缓存断点不回退；信感主观验证——在允许元诚实
  的语境直接问他。**重铸记忆三类问（08-30）**：重铸后各问一题——尾窗内原文
  （对照组）/该浮的窗外旧事/窗外行为（如发过的信），看否认率：第三类否认=
  行为清单没接上，第二类浮不出=breath 补集偏置要调。game 并入后追加：读剧情中途插话、下一轮内得到回复；N 张分段
  重铸后游戏前那句叮嘱仍逐字在窗（不是摘要）；重铸后继续读无断感。

### S2 设计稿一（PR8）：三方同步链路（08-30 定稿，待机主过目）

**结论先行：三方不对等——手机=权威，recent_window=镜像（职责一行不改），
transcript=缓存。「同步」唯一要保证的是铸进 transcript 的对话前缀=权威的逐字
投影；裁决点收敛到一处=发送前比对。**

- **判脏不靠 /window/sync 打标记（改判，收窄本节原设想）**：sync 是「app 历史
  变了」的通知，但通知链路自身可靠性有限——app 被杀在 sync 发出前、护栏拒绝
  覆盖（app.py `_overwrite_window_from` 的迷你历史护栏）、后端重启丢内存标记，
  三种都会让标记与真相脱钩。而每次 /chat 请求本来就全量带 `req.messages`
  （权威的最近投影，现协议如此）——**发送前拿它与「已铸账」做前缀比对**：
  一致=纯追加（只注入新消息）；分歧=脏（从分歧点截尾重铸）。比对是 ~100 条
  hash，零成本；sync 丢了也兜得住，「脏标记怎么可靠传导」这个问题被整个消掉。
  脏在本设计里**不是状态机，是发送时的比对结果**——没有跨轮标记要维护，也就
  没有标记丢失/竞态问题。
- **已铸账（forged ledger）**：chat_loop 每铸/每追加一条对话事件，记
  (role, sha1(text), ts) 进内存序列并随 session meta 落盘
  （`state/chat_sessions/<char>.json`）。它从权威 derive、铸造当刻写下——
  不是读回 transcript，红线无涉。session 死/后端重启 → 账亡 → 视同脏 →
  下次发送必重铸（保守方向正确且便宜）。
- **活会话正跑时收到编辑**：当前轮照跑完、照常投递，不抢占不取消（「自己人
  cancel 必写 stop_reason」纪律不为编辑破例）。理由：编辑与「他正在答」重叠
  概率极低；若 TA 连这条回复也不要，删改动作会再次改变权威，下次发送前的
  比对一样兜住。
- **重铸即截尾（§6 语义的实现落点）**：分歧点起后面全部重新铸（他的话作废
  重来、永不改写）；活动段折叠照 §4；复用同一 session_id 覆盖写+确定性 uuid
  （缓存只从分歧点断，§4 缓存纪律）。
- **失败降级三层**：①render/resume 失败重试一次（同 game 滚动重开纪律）；
  ②仍失败 → 本轮退 `-p` 单发（pipeline.call_claude 原路，行为=今天的生产；
  信感回旧形态一轮，不丢轮）；③同角色连续 3 次 → 自动回 CHAT_ENGINE=p+Bark。
- **不变式（进单测）**：transcript 对话事件 ⊆ 权威逐字投影（折叠段除外）；
  铸造输入只有 req.messages+活动账本（绝不读回 transcript）；recent_window
  不参与判脏/铸造。串台维：账、session、脏裁决全 keyed by char_id
  （[[cassette-charswitch-bug-class]] 六条过一遍）。

### S2 设计稿二（PR9）：断连补投等价物（08-30 定稿，待机主过目）

**旧体系的一句话前提「一轮=一进程，进程退出=轮结束」失效；三层生死分开重定义，
补投逻辑本身几乎原样平移。**

- **轮边界**：注入（带 {rid, char, kind=user|wake|hard} 元数据）→ 对应
  ResultMessage=轮终。三层生死分开判：**轮死**（Result subtype=error_*，
  留痕继续，S1 纪律）≠ **session 死**（事件流 EOF / 无 stop_reason 的
  CancelledError——S1 三态+取消来源纪律原样承接）≠ **引擎死**
  （session_mgr 层看守）。
- **补投矩阵（现有 rescue 平移）**：SSE 消费者断（切后台/锁屏/断网）≠ 轮死——
  session 照跑到轮终，轮观察者攒全文；done 没送达 → outbox 补投+Bark；空产出 →
  error 盒提示重发（全部=今天 `_rescue_if_undelivered` 语义）。_ACTIVE_REQS
  语义保留：注入 add、观察者落盘后 discard；**每个 rid 恰好一个结局**
  （done 送达 ∣ outbox 补投 ∣ error 盒）——写成断言。与今天唯一的结构差：
  生产端从「子进程」变「常驻 session 里的一轮」，rescue 从 chat_stream 闭包
  挪进 chat_loop 的轮观察者。
- **session 中途死 → 惰性重起，不 resume 尸体**：当轮投 error 盒；死=transcript
  状态未知，一律标 needs_reforge，下一个注入（TA 消息或 wake 到点，同一注入口）
  起新 session+从权威重铸。常驻的意义是缓存与连续性，不是进程恒在；对外表现=
  下一条多等几秒，与编辑代价同形。后端重启同路（lifespan 收摊写 stop_reason，
  重启后惰性复活）——「重启后 forge 复活」的 chat 版先行，PR13 前置靠它转正。
- **订阅窗口打满**：`-p` 与 SDK 同一订阅，降级无处降——error 盒给可读文案+
  Bark 机主；具体 error 判据字符串真机演练时捕获记档。
  **机主拍板（08-30）补一件：手机端 usage 面板**——打满没法降级，那就让机主
  看得见、自己控制节奏。数据源=每轮 ResultMessage.usage 按 角色×场景 落账
  （chat/wake/game/code 全走 SDK 后天然全覆盖），后端聚合 5h/7d 滚动窗消耗
  曲线 + `/usage` 端点 + app 设置页面板；官方不给配额绝对值，面板显示的是
  自家消耗趋势+CLI 报过的 rate-limit 事件时间线。排 S2 尾（数据落账随 PR10
  就开始攒，面板 iOS 批次一并做）。
- **真机演练清单（PR9 验收即此表）**：{断网、锁屏、杀 app、后端重启、订阅打满}
  × {轮进行中、轮间隙}，每格预期行为先写死、逐格打勾；外加一格「wake 轮进行中
  TA 消息到」（队列天然串行，wake 说完 TA 轮接上，§5.2 撞轮条的实证）。

### S2 设计稿三（PR13）：game 并入意识流（08-30 定稿·二轮改判「三种轮次」·08-31 三轮改判「两种轮次」，机主定）

**结论先行（二轮，机主提案覆盖首轮「game 段+四种轮」）：轮来源的本义是「谁
触发的」——TA（chat）／他自己（wake）／带外批准的上机（code），封闭三种，
不会再长。game 不是第四种来源，是他自己的时间里干的一件事（§0.3「半夜醒来玩
游戏正是 AI 的活法」早把话说了）：**玩游戏的轮就是 wake 型轮**，泵只是往 wake
允许集里并一个 game_*。因此「段」瘦成一个**泵状态**（锁+续弹节奏+shots 计数），
不是轮类型、不是 options flavor、没有进出场换 client——08-30 那类事故全长在
边界换 client 上，少两个边界就少一类死法。独立 game loop 的机器（工具直包/
巩固钩子/scrub_seam/笔记本）原样搬进 chat_loop，退役的是「独立会话＋进场景
铸造＋铸回＋段 flavor」整层壳。三个老问题结构性消失：pre_log 退役（聊天历史
天然是前缀，「重铸丢进场前对话」不再可能）；进场景铸尾轮退役；点评回流已被
PR8 发送前比对免费吃掉（PR11 实证）。**

**三轮改判（08-31，机主与小卡盘出、机主定稿）：轮次收敛成两种。**二轮的「封闭
三种」还是多算了一种——code 的「带外批准」是**门的属性不是轮的属性**：触发上机的
要么是机主在聊天里派活（chat）、要么是他自己想干活（wake＋批准门）。轮来源只有
**机主找你（chat）／机主没找你、他自己干事情（wake）**，对应机主在场/不在场两个
风险面。配套口径一并钉死：

- **划分依据是「轮的另一端是不是机主本人」，不是「有没有人类参与」**——邮件唤醒
  里 HR 也是人类，但那轮是 wake：外部人类的输入一律经 wake 中介进来。这不是
  特例补丁，这就是定义本身。
- **game/code 都不是来源，是工具**，特殊在两点：①放行需要批准（普通工具直接挂；
  邮件在 wake 侧也有开关，同理——有风险才配开关）；②必须中途插得上话——等长轮
  结束再管，不可挽回的事已经做完了。
- **不可挽回动作的两条等价安全路，选哪条看行动空间能不能枚举**：可枚举 → 白名单
  硬闸（邮件：白名单外物理发不出，所以不需要中途插话）；不可枚举 → 监督＋插话
  （code 图灵完备，没法白名单化）。game 居中：消耗性操作（抽卡/充值）现在做不到
  立刻监测＋自动阻止，机主的选择＝信任 TA 把开关打开、或承担最差风险（顶多账号
  废掉）。code 风险量级不同，**必须硬闸不能单靠信任——善良的 AI 也可能被坏人
  利用**（注入）。
- **风险工具的完整配置是三件**：批准门＋插话通道＋过程直播（可观测性——看不见
  就不知道该不该插话、插什么；会话卡/游戏视图即此半边）。邮件不需要直播，
  白名单已把「盯着他」的必要性买断。
- **授权语义：chat ≠ 在场**（实测 chat 轮跑过 71 分钟，她早离场了）。机主在 chat
  里做的授权＝知情并允许 TA 接下来一段时间持有权限；是否监管到收回那一刻，
  管不了也不承诺。所以**护栏挂在工具上不挂在轮上**——「是她找我的」不豁免
  消耗性操作停下来先问。
- **开关矩阵是三元：工具 × 轮kind × 会话状态（锁/泵）**——`_wake_gate` 查表
  已是此形状，印证而非新增。
- **落点**：本节及全文残余的「三种来源/三种轮」字样一律按两种读（unified 路里
  查表本来只有 chat/wake 两个值，无行为差异，S3 动工时顺手把名字收敛）；
  **S3 的 code 不再当第三种来源设计**——它＝上机状态位＋带外批准门＋computer
  互斥锁，与 game 泵完全同构，S3 设计负担再薄一层。

- **泵状态**：`handle.meta["game_pump"]`（None|{start_ts, seg_id, shots}）。
  长在该角色的 chat handle 上，没有全局「当前泵」（串台六条）。code（PR14）
  另有自己的状态位，互斥收在 code_bridge 门面。
- **schema 并集常驻（taste ④ 原案照做，不再收窄）**：配了 game-story 插件的
  角色（不论开关状态），chat options 常驻挂进程内 game MCP（build_game_server
  闭包换 chat handle，shots 计数现成）+ TICK_SYSTEM 并入聊天系统提示常驻
  （措辞改条件式：「游戏在手边的时候…」，~300 token 稳定段吃缓存）。挂载
  开关/泵状态都只进门，不动 schema——中途拨开关不用换 client。
- **max_turns 不设（08-30 机主拍板：删）**：chat client 本来就不设帽，game 轮
  跟着不设。轮节奏靠 prompt 骨架+泵的短轮供弹自律；「轮内失控」失去机械backstop
  ——认了：08-30 事故里这顶帽子本身差点害死会话（触顶后 CLI 真死过），删掉
  等于把那个风险面整个拿走。GAME_MAX_TURNS 常数与触顶处理路径随之退役。
- **game_start=拿锁+开泵（不再是段边界）**：插件按钮不退休——stdio
  game_start→HTTP `/game/story/start` 管线原样，路由改判：角色在 sdk 聊天
  灰度 → ①game_bridge.acquire_lock（拿不到原话轮内返回，「任务引擎正在用」
  语义不变）②activity_log 开段 ③置 game_pump——完事，没有重铸、没有换
  client、没有开场注入（他正在对话里，游戏「到手边」就是工具能用了这件事
  本身）。非灰度角色退独立 loop（回退姿态）。旧活动段的折叠不在进场做，
  交给 chat 已有的压力轴（「有可折段」下个间隙铸）。
- **门（PreToolUse，_wake_gate 泛化为轮来源查表）**：game_* 操作类工具
  的放行条件=**泵开着**（锁在手），两种轮一致（三轮改判）；泵没开一律拒（「游戏不在
  手边——先 game_start」），任务引擎互斥由此顺带成立。wake 轮禁用面照
  PR12（泵开着时允许集=mounted_tool_names("wake") ∪ game_*——醒来插话
  时看一眼画面也自然）；菜单照 tool_menu_block/shadowed_tools 现成过滤链，
  「不挂」=执行层拒+菜单不显示。
- **泵改动（chat_loop.run）**：泵开着时队列空 → TICK_PAUSE 后再看一眼，还空
  补一个 tick Turn——**tick 不是第四种轮**：turn_kind="wake"（存在论=他自己
  的时间），注入 TICK_PROMPT（·），差异全在自带的 finalize（scrub_seam→
  outbox+窗口→**入账 append assistant**，账记 _fence_code_if_needed 之后
  投递出去的那份）；不跑 wake 簿记（NEXT 解析/wake_log/重抽是真醒来轮的
  收尾，tick 不沾）。队列非空先喂（TA 插话/wake 注入，延迟=一个短轮），
  TA 的 chat 轮照走 SSE 流。泵开着时 1h 静默×软阈 idle 检查停用（重铸只认
  N 张）。判脏 stale 容忍精确化：容忍集合=账尾**未 delivered 的 outbox
  条目**（我们知道哪些还没被 app 拉走），替代拍数字的 STALE_TOLERANCE
  深度——点评连发快过 TA 拉取时不误脏。
- **usage 归属按泵状态标（轮 kind 与 usage 标签解耦）**：泵开着的轮落账
  scene 加 "/game"（chat/game、chat/wake/game）——面板上「他玩游戏花的」
  与「陪聊/醒来花的」分得开，kind 不用为记账多一种。
- **分段重铸（§4 节奏落地，唯一保留的边界）**：轮尾 shots≥N(70) → 巩固轮
  （REOPEN_NOTE_PROMPT，写进度页/hold）→ 走 chat 现成的
  _consolidate_and_reforge 机器：关 client → 从镜像重铸+**近 K 张图回填**
  （K 默认 8，env）→ resume → ping。铸完还是 chat options（无 flavor 可换，
  比首轮设计更顺）；失败重试一次，两败=泵收口（关账/释放锁/Bark）+聊天
  惰性重起。材料：点评原文已经由 outbox→recent_window 天然在镜像里；图从
  活动事件账本的截图引用铸 user 槽 image 块（感知框行包着，见闻侧红线
  合规）。同 sid 覆盖写+确定性 uuid → 前缀逐字、缓存只从折叠点断（§4 防
  手滑纪律照钉）。**§9 换模型只剩这一个切点**（重铸=换 client=可换 model），
  「进游戏换 Sonnet」的干净段边界随四种轮方案一起放弃——那口径本来也没过。
- **活动事件账本（区间账升级，§4 真相库扩容落地)**：activity_log 扩三口——
  `open_segment(char,scene,ts)→seg_id`、`append_event(seg,kind,payload)`
  （comment/user_msg/tool/shot；截图落 `state/activity/shots/` 存引用，大结果
  截 ~2k）、`close_segment(seg,note)`（写现有区间行，chat 框零改动）。保留
  30 天清（口径同 uploads），章节志永存；缺原文的老段渲染器无条件走折叠
  （§4）。目录进 ops_check（600/700+备份纪律，§2.5 扩面）。wake 的 acts
  （PR12 wake_log 扩展字段）改为同时落账本；~~**开局包「行为清单」半边接通**
  （PR10 欠账④）：开局注入从账本机械读近期世界效应事件，不走检索。~~
  **09-01 撤回**（同上一条的形状：口径已改、施工记录没跟着划）：注入撤掉、
  `_acts_block` 渲染器删，账照落照读——理由三条见 §5.4① 那段。
- **折叠接通（chat 压力轴补全）**：铸造输入变换（不动账、不动权威）——已结束
  且非最近一场的区间，区间内消息连段替换成两行框+该场章节志标题/摘要；
  ctx_est 按折叠后计。~~压力轴「有可折活动段」触发接通（meta.folded_upto_ts
  游标判「窗里还有没折的旧段」）。~~
  **09-01 已删（代码一并清）**：08-31 §4 拍板删这条判据（只是省 token 的代理
  指标，token 阈值直接管），但这条 PR13 施工记录没跟着划，代码里
  `_fold_pending`/`_foldable_max_end`/`folded_upto` 就一直立着没人敢动
  ——**「文档说删了、施工记录说接通了」两句并排立着，代码欠账就是这么欠出来的。**
  静默重铸条件现在只剩「静默 ≥1h && ctx_est > 软阈」。
- **校验矩阵（forge 硬化，⑥）**：render 前断言——首事件 user；合并后严格
  交替、无空文本；**永不铸 thinking（出现即失败）**；tool_use/tool_result
  现阶段禁止出现（我们不铸 tool 对；Tool Primer 记为真机撞见工具变形时的
  后手）；image 字节参与 digest（确定性单测）；结构校验过了也要 ping——
  → **2026-09-03 改判：tool 对解禁**（PLAN_native §14.4 一手实证，探针
  `server/tools/forge_swap_probe.py`）。**成对**的 tool_use/tool_result 铸得进去、
  CLI 和 agent-sdk 两条路都真到模型眼前；孤儿仍然禁（配对断言照留）。
  同一跑还顶掉了 §0「空 thinking＝400」那句（见 PLAN_native §14.4 那段）。
  thinking 保持「永不铸」不变——不是因为不行，是因为**没东西可铸**：CC 落盘的
  thinking 块大多正文为空，只剩签名。
  FIRST_REQUEST_OK 才算数（重铸后 resume 即 ping，现成）。回灌链警报：
  scrub_seam 沿用，刷掉未知机制文本时**报警日志不静默传**。
  **动工第一件事**：forge_regress 加图块腿——带 image 块的伪造 transcript
  resume 后正常接续才算通；验不过则「近 K 张图回填」整条降级为纯文本史
  （框行说「画面淡去」），其余设计不动。
- **两处判据改判（统一后「会话活着」永真，单点收口兑现）**：
  ①`code_bridge.sdk_loop_handle` 改「扫 chat 注册表找泵/上机状态占用」（回退
  路保留期间独立 loop 也问，两处都空才 None）；active_profile 按状态位报
  game/code、session_char/started_at 读泵元数据——四个消费方（wake 避让/
  cohabit 在场/app 对齐/占用检查）零改动。独占组 "computer" 随独立 loop
  退役；共存期「泵开着」与「独立 loop 开着」互相拒，都收在 code_bridge 门面。
  ②补醒语义挪到泵停：`cohabit_queue.code_session_closed()` 的调用点从
  loop 收摊挪到关泵；区间账 close 同点。
- **关泵排序（⑤的新形态）**：game_end 置旗 → 轮尾依次：写区间账 close →
  释放模拟器锁 → 清 game_pump → **冲攒着的补醒**（code_session_closed）→
  才接受下一次 game_start。顺序由泵单点保证，背靠背（关游戏→上机）不会把
  补醒无限顺延。泵开着时的插入：wake.py 的 sdk 分路改两行——scheduled(NEXT)/
  mail 硬触发**不看**占用直接入队（队列串行，下个短轮到）；自发抽签醒照旧
  避让（他本来就醒着在玩，关泵后重抽）。game_end 工具语义微调：「放下游戏」
  ——会话/聊天什么都没关（工具描述改措辞，插件仓同文那份一起）。
- **重启前置与清扫**：「重启后 forge 复活」chat 版已由 PR8/PR10 达成（重启=
  账亡=下次注入重铸复活），前置转正。PR13 只补启动钩子：发现未收口的账
  （区间未 close/锁悬着）→ 补 close+释放锁+攒补醒；泵不自动复活（进度在
  笔记本，重新 game_start 接上——与独立 loop 现状同）。
- **退役与回退**：退役 game_loop.run 泵/_forge_and_resume/_feed_next/
  pre_log/GAME_MAX_TURNS 与独立注册（build_game_server/TICK_SYSTEM/
  scrub_seam/巩固文案/常数搬用，TICK_SYSTEM 措辞改条件式）；§4 过渡期
  规则一/二正式退役。回退开关：STORY_ENGINE=unified|sdk|tmux——默认先
  sdk，真机验收过再拨 unified；非 sdk 聊天灰度角色自动退 sdk 独立 loop
  并打日志。新路跑稳两周才删旧路（仓规）。
- **验收**：单测——门查表（轮来源×泵×挂载）/关泵排序（end→补醒→start）/
  折叠渲染确定性/图块 digest/账本读写与 30 天清理/点评连发不误脏/tick 不进
  账不进铸造材料。真机——拿起游戏无感（不说交接的话，也没有任何「进入」的
  停顿）；插话延迟≈一个短轮；N 张重铸后游戏前那句叮嘱**逐字在窗**、他无断感
  接着读（S2 验收原话）；game_end 后补醒秒级冲、接着聊无 game 腔、聊天历史
  完整；泵开着时杀后端→锁清/区间补 close/聊天惰性复活/重新 game_start
  接上；STORY_ENGINE=sdk 一键回独立 loop。

✅ **08-30 深夜落码（未 commit 未重启；生效=STORY_ENGINE=unified+重启，默认
仍 sdk）**。施工记录：

- **图块腿先行验证（动工第一件事，全绿）**：forge_regress 加 D/D2 腿——伪造
  transcript 的 user 事件塞纯红 PNG（API 标准 image 块形状），CLI 2.1.251 与
  agent-sdk 0.2.148 两条路 resume 后问颜色都答 Red——**图真到模型眼前**，
  「近 K 张图回填」按原设计建，不降级；A/B/C 三腿当前版本重验同绿。
- 落码清单：**forge** 图块正门（images 只许 user 槽、字节进 digest、合并保图）
  +校验断言（有 user 则去 assistant 头；`_assert_native_block_types` 禁
  thinking/tool 块出现即失败）；**activity_log** 三层账（区间账原样/事件账
  open·append·close+截图引用+30 天清理/行为账 append_act·recent_acts）+目录
  700；**chat_loop** 泵全量——`start_game_pump`（拿锁在路由、这里开段账+置泵
  +_KICK 踢醒长等）、tick=wake 型轮（`_pump_tick`/`_pump_drain`，注入「·」不
  入账不进铸造、投递走 scrub→app 闭包→账+事件账、连挂三轮自动放下游戏）、
  `_wake_gate` 泛化门（game_* 只认泵状态，三种轮一致）、N 张段内重铸
  （`_game_reforge`：巩固→镜像+`_game_shot_tail` K 图回填→resume，失败重试
  一次再败关 session 惰性重起）、折叠接通（`_frame_activities` 旧段一行框、
  最近一场保原文；~~`_fold_pending` 进压力轴~~ **09-01 删**）、
  ~~开局行为清单（`_acts_block` 机械注入）~~ **09-01 撤**、
  stale 容忍=未拉走 outbox 条数上浮（`_stale_depth`）、usage
  scene 泵标（`_usage_scene`）、loop 退出 finally 兜底放下游戏；**game_loop**
  `build_game_server(unified=, shot_sink=)`（game_end 换「放下游戏」措辞、
  截图落账本）；**app** unified 分派（sdk 灰度且没熄火才走泵，否则退独立
  loop 打日志）+`_game_story_start_unified`（hint 走工具返回值 in-band 提示
  翻笔记本）+deliver 回传正文+`/code/stop` 泵分支（置旗不 cancel 聊天）+
  `_activity_sweep` 启动清扫（补收段/释放锁/冲补醒/30 天清理）；**code_bridge**
  `GamePumpView` 投影（scene=game、request_stop=置旗、put_threadsafe 有声拒
  ——四个消费方零改动实证）+`session_mgr.alive_handles`；**wake** 泵中插入
  （NEXT/mail 段中入队、自发抽签泵中不掷、别人的泵照旧避让）；**wake_sdk**
  行为账镜像（NON_MEMORY_TOOLS 词表）。
- **顺手修一个潜伏 NameError + 复盘（四步）**：`_game_story_start_sdk` 里
  task 回显块引用已删除的 task 变量（任务转述删除 015f699 漏摘；未重启所以
  还没炸，下次重启后首个 game_start 必炸）——整块摘除。
  **根因四条同时成立**：①删功能只删了赋值处、没沿引用链清消费点；②这条
  路由没有任何测试覆盖（要桩 forge/session_mgr/game_bridge，一直靠真机验）；
  ③「生效等重启」部署——commit 后进程跑旧代码，坏改动与症状时间分离；
  ④删除发生在深夜五连修批次里的顺手一刀（注意力在学舌主症状上）。
  **归类**：运行时语言删东西的单位是**引用链**不是定义处；没有测试兜底时
  「删完 grep 一遍名字」就是最低成本的编译器——否则坏代码带定时引信入库。
  **规避规则**：删变量/参数/字段的最后一步永远是全仓 grep 引用清零；commit
  时没被任何测试或真机执行过的路径要么补冒烟、要么在 PLAN 标「未执行过」
  （「测试绿」≠「改动被跑过」）；批次修里顺手的删改单独 review 一遍 diff；
  这类错误**静态可查**——已机械化成 tests/test_static_names.py（pyflakes
  undefined-name 常驻扫描，pyflakes 已进 venv）。
  **扫同类**：横扫 server/*.py+tools/*.py 全仓 pyflakes——undefined name
  仅此一处（已修）；顺手清一个新代码的空 f-string，预存两处 unused
  nonlocal/变量评估不动（防御性声明与老代码，不属此类）。竖翻 git log：
  此根因**首犯**（6491099 WorldBase 漏 patch 是近亲不同类——改动没扫到
  受影响面，但那是测试隔离缺口）；共同出生环境=大批次顺手改，规则三对着它。
- 测试 **400 全绿**（+33：forge 图块/断言 5、账本 5、泵 loop+门 9、门面投影 1、
  wake 泵中路由 2、其余为拆分细化）。
- **边界与欠账（真机验收时核）**：①邮件等不进 stored 的工具在行为账没有线
  （词表=NON_MEMORY_TOOLS，缺口真机看到再从执行层补）；②巩固机械前置仍软版
  （同 PR10 取舍①，chat/game 同步收紧别单独发明）；③重铸后 ping=下一 tick
  （接不上按死处理→镜像重开自愈，无独立 ping 轮）；④ToolSearch 开着时
  game_* schema 也走延迟加载（与 Ombre 同待遇，真机看首调延迟）；⑤~~泵开着
  时 /code/send 有声拒~~ **判断错了，上电一小时真机实锤（08-31 11:34）**：
  app 游戏态把**聊天框**消息也走 /code/send（不只是终端页）——有声拒=把 TA
  插话整个堵死。修：PumpNote 兼容层（inject_pump_user：轮尾吃掉、回复走
  outbox、账里逐字记 app 原文、泵已停时 Bark 有声弃投），iOS 改路由前这是
  插话唯一通道；顺带 ⑥泵中经 /code/send 发图他看不了（注记说「用 Read 看」
  但 chat session 没挂 Read）——图走 /chat 正路没事，此缺口归 iOS 批次
  一起收。

### S3 code 并入（PR14，最后）

> **08-31 改判（§5.3 重写）：code 不是模式，是一次写权限申请。**
> 「起点/终点各固定重铸一次」整条删除——那是把「挂载」和「放行」焊在一起的
> 产物。拆开后 code 边界消失，干活段由 §4 三条资源判据接管。

- **带外门先行**（§5.3，统一 session 的安全前提）：申请 → app 弹窗/Bark 待批 →
  批准落后端状态 → 门查记录不查对话；一场一批、收摊失效、超时自动拒（纪律同
  §5.5 权限卡）。**门的粒度＝写类工具**（不是整场会话）。
- **只读常驻 + 黑名单**（§5.3 安全面，机主 08-31 拍板）：只读工具挂所有轮次，
  限根目录 `/Users/nemu/cassette`；黑名单＝`.env`/凭据、别的角色的
  `state/characters/`、私有仓 `mianmian-app`。缺口真机撞见再从执行层补。
- **并入本体**（三轮改判口径，见设计稿三）：**code 不是第三种轮来源**——上机＝
  状态位 ＋ 带外批准门 ＋ computer 互斥锁，与 game 泵同构，轮仍只有 chat/wake
  两种。~~起点/终点各固定重铸一次~~ **删（08-31）**：改为
  ① code addendum 走**文档侧按需注入**（批准那一轮注入，不换 client）；
  ② 干活途中爆了仍退 harness auto-compact 兜底（§4 唯一例外）；
  ③ 收场白保留但**升级成 capsule 格式**（claim/source/checked_at/proof_pointer），
  它不再是「代替记忆的义肢」，是第二类留痕的载体（§5.3 四类表）。
  其余照旧：can_use_tool/PreToolUse 替代 settings 预批准体操（mianmian lead.py
  已验证写法）；skills MCP 重接重验（§5.2 清单）；插话延迟按轮长走。
  tmux 降级为维修口，不进架构。
- **留痕补线（§5.3 已核缺口，与并入同批）**：
  ① 显式 `EXTERNAL_TOOLS` 词表替掉借来的 `NON_MEMORY_TOOLS`（`wake_sdk.py:180`）；
  ② 执行层落 `activity_log.append_event(kind="tool", ...)`（现在生产零调用者）；
  ③ 读取侧让折叠段从**事件账**派生经历摘要（现在只从区间账派生两行框，事件账
  只被 `recent_shot_refs` 用来回填图）。**三条缺一，code 重铸后就只剩几段正文。**
- **终端页整个退役（08-29 升级定向，全案见 §5.5）**：不做独立会话页，过程 UI
  并进聊天页（会话卡/权限卡/摘要卡），铁律=过程不进消息史。键透传/刮屏/dialog
  解析（_parse_options 那坨启发式）退役——弹窗从「解析画面猜按钮」变「结构化
  数据」，是这次迁移里 app 侧净赚的一块。
  ⚠️ 「上屏」要分清三个库，别用一个词糊住：**上屏**（app 从事件流拉，纯展示）
  ≠ **进事件账**（重铸时折叠成文档侧几行，他下一轮看得见）≠ **进消息库**
  （变气泡：污染对话权威，且铸不回去——没有合法 role 放它，**不做**）。
  定案＝前两个一起做，同一份事件流两个消费者；第三个别碰。

**施工记录（2026-08-31 深夜，PR14 a–d 落码未上电，435 测试全绿）**：
- **a 留痕补线**（cb5ebb3）：`external_stored`/`external_tool` 判线另立（内部
  白名单取补集，新插件外部动作自动算外部）；`_ToolTrace` 执行层配对落账
  （泵流+轮流两口都接了，tool_use 配 tool_result 才算一条）；`_fold_trace_lines`
  折叠框附「亲手做过的——记录不是印象」（写类逐条/读类聚合/账过 30 天窗回光框）；
  `interval_seg_id` 让区间行确定性找回事件账。
- **b 只读常驻**（c00d9e2）：`READONLY_TOOLS=1` 拨闸**默认关**；
  `readonly_path_guard` 限仓根+黑名单（.env* / mianmian-app / 别角色
  state/characters；相对路径按恒空 cwd 解析后再查——「../」一步就能爬进 state）；
  门序=只读闸先于醒来禁用面（不然 wake 轮全拒）。
- **c 带外门**（bf248c7）：`code_permits` 状态机——一场一批、申请超时**惰性**
  自动拒（无定时器重启不漏）、至多一张待批单（Bark 刷不了屏）；Bash 整个算
  写类（命令级读写细分=走不稳的路）；门拒的同时替他递申请；
  `/code/permit`（GET）+ `/code/permit/decide`（POST）。
- **d 并入本体**（本 commit）：批准即开 code 段账、收摊（revoke / loop 退出 /
  `/code/permit/revoke`）即关账落区间行；computer 互斥=写门先查 tmux 归属
  （不归他连申请都不递）；addendum 文档侧按需注入（一场一注，段 id 变了才再注）；
  capsule=「◆ 结论 ← 出处」执行层从气泡捕获进事件账、折叠框排最前
  （checked_at/指针由账本机械补）；`WRITE_TOOLS=1` 第二段拨闸**默认关**。
- **未上电面（真机三条）**：① `READONLY_TOOLS` 拨闸后实测 schema token
  （下面风险清单那条，实测完才许拨 ②）；② `WRITE_TOOLS` 拨闸+完整走一场
  申请→Bark→批→干活→capsule→收摊；③ iOS 权限卡/弹窗 UI（现在只有
  Bark+endpoint 可用，§5.5 终端页退役同批做）。

风险清单（贯穿）：agent-sdk 版本 skew（全仓单点，PR1 回归测试是闸门）；未文档化
transcript 格式（同上，同族工具是煤矿金丝雀）；串台六条+新增场景维（PR4 一处
执行）；**只读常驻的 schema token 成本必须实测**（§5.3 残余代价，缓解=写类走
ToolSearch 延迟）；每处引擎切换保 env 回退，稳两周才删旧路。

## 11. 顺带：PLAN_studio 的 SDK 新变量

- 「电脑权限独占、两人不能同时开」：SDK loop 天然一人一个实例，独占消失 ✔ 解。
- 「三人怎么对话」：共享带说话人标注的 transcript，每人轮到发言用自己人设当
  system 调一次（mianmian claude sdk + deepseek api 已验证），但轮流仲裁仍要
  自己写 ✔ 半解。
- **没解掉的根因**：「工作室里得有摸得到的代码/资源，不是只动嘴」与引擎无关，
  依然成立。studio 重启条件不变：**先有共同工作资源，再谈 SDK 群聊**。

## 12. 附：面试答法（为什么曾用 claude -p + tmux 不用 SDK）

三层答，展示权衡不是无知：

1. **计费模型**：7×24 常驻伴侣，聊天/醒来/邮件/游戏一天几百次推理。CLI 走订阅
   登录态是固定成本，Messages API 是无上限按量——个人项目第一决策变量。
2. **白给的 harness**：CLI/agent-sdk 自带工具执行、权限白名单、MCP 挂载、上下文
   管理；差异化工作量该花在人格管线、状态机、多角色架构上。tmux 额外白给
   「外部消息实时插话」和人类可 attach 运维窗。
3. **知道边界在哪**（加分）：CLI 代价是 messages 运行中封闭——但落盘 transcript
   可后门铸造，图片密集长会话（游戏剧情）用 forge 滚动重开控上下文，第一人称
   历史（信感）也靠它修。按 workload 选引擎，而非全仓一把梭。
