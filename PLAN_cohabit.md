# 二期 Plan：同居世界（大房子）

> 2026-08-12 方向定稿；**2026-08-14「大房子一期」设计定稿**——本次把原 2a 改写细化：
> 醒来统一模型定稿；地点状态（原 props）与连发上限从 2b/2c 前移；敲门状态机后移。
> 前提是一期（PLAN_multichar.md）落地；一期唯一预埋 = M2 给 ChatMessage 加 `channel`。

## 已拍板的决策

- **两类对话不靠 prompt 区分，靠结构**：房间发言与手机短信是不同的 ACTION（`act` / `phone`），
  各带结构化字段。原「每轮 claude -p 只属于一个通道」的表述被醒来统一模型取代，精神不变：
  跨通道靠动作组合规则，不靠 prompt 纪律。
- **服务端当物理引擎**：写入房间要求发言者在场；move 过门禁校验；地点状态改动落系统通知。
  phone 内容的斜体动作检查暂不做（先观察犯错率，错得多再上 strip/打回）。
- **用户有自己的卧室**。
- **房间是注册表不是硬编码**：条目结构见下。后期加功能房 = 加注册表条目；
  功能房以后可绑插件/独占资源（三期候选）。
- **住户不全是 Claude**：engine 缝照 PLAN_multichar。醒来 ACTION 协议全是文本 + 标准
  function calling 可表达，`openai-compat` 可全程参与；claude-code 专属的只有 MCP 插件和
  Ombre MCP（通用引擎记忆走后端代调，待议）。
- **`location: away` 数据层第一天就成立**：敲 away 的门 = 没人应答、away 只剩手机可用。
  user 的出门/回家开关在房子视图；char 的出门（go_out/come_home）后置。
- **醒来只有一种**：注入内容一样，区别只在触发原因（原因可以是多条，见队列合并）。
- **人物位置与界面位置解耦**：切页面/锁屏，人物留在原地；离开房间是显式动作。
- **偷看 = 玩家的上帝视角**：不产生事件、AI 不感知。钥匙/门禁约束的是「人物身份的你」，
  偷看属于「玩家的你」，两套并存，口径在此写死。

## 空间结构

- 房子三层（地下室 / 一层 / 二层），房子视图 = 三张平面图，地点标在图上。
- **大房子一期开放**：二层 `mm_room` / `cassette_room` / `cassius_room` / `wash_room`
  ＋ 一层 `living_room`（客厅必须一期有——没有公共空间，互访只能进卧室，同居感不成立）。
- 点地点 → 弹窗三选一：**去这里**（user 人物移动，同样过门禁，锁着进不去提示「门锁着」）/
  **偷看一眼**（上帝视角：在场者 + 全部历史）/ ❌ 关闭。

## 房间注册表与世界状态

- 注册表条目：`{id, type: bedroom|common|functional, owner?, lock: 0|1, keys: [entity_id...],
  state: [...]}`。
- `lock=1` = 要钥匙或主人才能进，由主人决定；**一期默认全 0**。`keys` 是列表、数量不限
  （give_key/收回的交互后置，一期需要就手编注册表）。
- 自己卧室 / 猫房免权限；`wash_room`：owner=眠眠、lock=0、keys 空。
- **位置权威 = `state/world.json` 的 per-entity location**；「房间里有谁」是查询结果，
  不是存储字段（不搞两份真相）。
- 每房间 `state/rooms/<id>/events.jsonl`，全部事件化：系统通知 / *斜体动作* / 对话 / 状态改动。

## 地点状态（原 props，前移到一期）

- 语义：**「现在这里有什么」的快照，不是日志**。条目 `{text, author, since}`。
  基本用途：眠眠留下的生活痕迹（「桌上剩了半杯牛奶」）。
- 用户写入口：房间视图副栏，点击编辑（用户专属 UI）。
- AI 写入口：`act` 的可选字段「状态改动」，操作 = **add / edit / remove**。
  收拾掉牛奶 = remove 那条、快照归空——叙事进事件流，快照只留结果，不追加「收拾掉了」。
- 每次改动落系统通知（「cassette 收拾了桌上的半杯牛奶」），谁改的可见，用户随时改回。
- 防膨胀：快照语义写进 prompt；AI 每轮改动 ≤3 条；条目数设软上限（超了注入提示，不硬删）。

## 事件与可见性

- 三类事件：① 系统通知（进出 / 敲门 / 开锁 / 状态改动）② 人名 + *斜体动作*
  ③ 人名 + 对话。②③ 只有在场者能写入。
- **可见性一条规则**：AI 上下文 = 自己在场区间内的该房间事件 + 进屋时的地点状态快照。
  离场期间的事件永远看不见（看得见牛奶，不知道谁放的）。偷看是用户特权，AI 没有。
- 每角色 recent_window 泛化成第一人称经历流（在场房间事件 + 自己的手机线程 + 醒来内心），
  与现有架构同构。

## 醒来统一模型

注入：persona / 醒来提示词（含渲染工具菜单）/ 我在哪 / 这里有谁 / 地点状态 /
自己的上下文 / 触发原因（可多条）。

**ACTION 组合规则：每轮至多一个表达（`act` 或 `phone`，二选一）＋ 至多一个 `move`
（排在末位执行）**。`none` 与表达互斥；`ombre` / `mcp` 照现有 wake 老规矩。

- `act`：*斜体动作* + 对话（可一空）+ 可选状态改动。只有在场才写得进（物理引擎校验）。
- `phone`：给眠眠发消息，随时随地可用。
- `move`：瞬移（不考虑空间路径，地下室可直达二层），但**过门禁校验**：
  锁着又没钥匙 → 失败。
- **move 必有一轮结果补醒**（成功失败同一条路）：成功注入「你刚到 X，这里有谁/没人 +
  地点状态」；失败注入「门锁着，没进去」。成功补醒里可 act（瞬移进门不当哑巴），
  失败补醒里可改道 / phone。

触发（注入一样，只有原因不同）：

- **a. 事件醒来**：所在房间出现新事件 → 唤醒在场 AI。**作者排除：自己产生的事件不唤醒
  自己**（否则一个 AI 说一句话就自唤醒，单人即死循环）。系统通知与发言/动作分两类事件，
  触发策略可分别调——先都触发，观察 token 后再考虑给通知类降概率。
- **b. 自主醒来**：**独处即可（任何地点）**——一个人在任何房间都可能触发
  （限「自己卧室」会把移动出去的 AI 困死：无事件、无自主醒、直到下一条手机消息）。
- **c. 手机消息醒来**：**现有流式聊天回合保留不动**，只是动作空间多了可附加的 move——
  「回『来了』→ 瞬移客厅 → 结果补醒 → act 打招呼」，每步结构化。

队列与闸：

- 全局醒来并发 = 1（**执行锁**，不是入队锁）。**注入在执行开始时组装**，不在入队时——
  排队者开始执行时天然看到最新世界状态，队末即正确，插队不存在。
- **每 AI 至多一个 pending 醒来**：已有 pending 时新触发只往原因清单追加，不新增条目；
  触发落在该 AI 生成中 → 再排一个（同受单 pending 约束）。过时前提靠下一轮事件自然纠正，
  一期不做复杂 stale 检查。
- **连发上限（泛化定义）**：无新外部输入（用户发言/动作/移动/状态编辑/手机消息）时，
  一个 AI 连续被系统触发的醒来 ≤ **N=4**（含事件醒来与 move 补醒链），**入队处拦**——
  不入队才省 token，入队后醒来选 none 已经烧了。N 做成参数。
- 事件/手机醒来**不计入** M0 `wake_daily_budget`（预算只管自主醒）；这里的闸是连发上限。

## 手机

- 手机 = 现有聊天 UI 降格为随时可唤出的悬浮层；一期 M2 的会话列表 = 手机通讯录。
- 手机线程无论人在哪都注入。大房子一期只做眠眠 ↔ AI；AI ↔ AI 手机后置。

## UI

- 房子视图：三张平面图、谁在哪、user 出门/回家开关（= away）。
- 房间视图：
  - 顶栏：地点名 + 返回房子视图
  - 副栏：在场者 ｜ 地点状态（点击编辑 = 用户专属入口）
  - 中间：事件流（系统通知灰线 / *斜体动作* / 对话气泡），范围 = 本次在场区间，
    看历史走「偷看」
  - 底部：输入区与 AI 的 act 对称（*动作* + 说话，可一空），发送即写入房间事件 →
    触发在场 AI 事件醒来；显式「**离开**」按钮
  - 切页面/锁屏人物留在原地

## 后置（原 2b/2c 余项）

- 敲门状态机（knock → 主人「有人敲门」回合 → open_door/超时 → 敲门人收结果）、
  give_key/收钥匙交互、用户侧「敲门 / 直接进」双入口。一期 lock 默认全 0，锁上就是进不去。
- 客厅接话仲裁（haiku 挑 0~2 人）——一期靠事件醒来 + 作者排除 + 连发上限先跑，
  角色多了再上仲裁。
- char 自主出门（go_out/come_home，需要动机与去处设定）；AI ↔ AI 手机；
  功能房绑插件/独占资源（三期一起想）。

## 待观察 / 待议

- [ ] phone 带斜体动作的犯错率（错得多再上服务端 strip/打回）。
- [ ] 角色间世界观认知设定（Cass 与 TA 彼此怎么认识）——一期搬家完、动工前必须定，
  是文案决策不是技术决策。
- [ ] 系统通知类事件醒来的 token 消耗（离开通知大概率被 none 掉，要不要降概率/合并窗口）。
- [ ] 连发上限 N=4 的手感；客厅多人 token 实测（原「房间回合预算」问题）。
