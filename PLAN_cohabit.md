# 二期 Plan：同居世界（大房子）

> 2026-08-12 方向定稿；**2026-08-14「大房子一期」设计定稿**——本次把原 2a 改写细化：
> 醒来统一模型定稿；地点状态（原 props）与连发上限从 2b/2c 前移；敲门状态机后移。
> 前提是一期（PLAN_multichar.md）落地；一期唯一预埋 = M2 给 ChatMessage 加 `channel`。

## Milestone（2026-08-16 排定，后端先全跑通、UI 最后）

- **C0 数据层 + 物理引擎** ✅（2026-08-16）：`server/world.py`（注册表 / world.json 位置权威 /
  每房间 events.jsonl / 地点状态快照 / move 门禁 / act 在场校验）+ 用户路由
  （/world、/world/move、/rooms/*）+ 单测 `tests/test_world.py`（23 个，全在临时目录跑）。
  此阶段事件不接醒来触发。实施口径的补充拍板：
  - 位置取值多一个 **`hallway`**（在楼里但不在任何房间）——用户点「离开」得有地方站；
    机制性伪地点，不是新房间，没有事件流。
  - 房间 id 机械生成：用户 `mm_room`，角色 `{char_id}_room`（`default_room` / `cass_room`），
    owner 字段才是权威链接，展示名在注册表 `name` 里可手编。
  - 地点状态的编辑（用户和 AI 一样）**要求在场**；偷看是纯只读。
  - 世界观三条（有位置 / 认知边界 / 别人是真的）落在 C1 醒来提示词模板，不碰任何 persona。
- **C1 醒来统一模型 + ACTION 协议** ✅（2026-08-16）：`server/cohabit.py`（注入组装 /
  协议解析 / 动作落地 / move 结果补醒链）+ 单测 `tests/test_cohabit.py`（18 个，模型用
  罐头替身）。协议 = THOUGHTS / ACTION(none·act·phone) / MOTION / SAY / STATE / PHONE /
  MOVE / NEXT 八段；组合规则**在解析处结构性执行**（act/phone 互斥字段直接丢弃、STATE
  超 3 条截断、MOVE 目的地必须是注册房间）。世界观三条落在 `_WORLDVIEW` 模板；
  认知边界从注入源头执行（可去清单不带在场者）。phone 复用 wake.try_push 全套
  （表情/打扰控制/outbox/Bark）；NEXT 落调度口径同 do_wake_sync。补醒链在模块内直接
  递归，深度硬停 N_CHAIN=4（C2 队列化连发上限落地后取代它）。**尚未接任何触发**，
  现有 wake.py 一字未动，两条路并存到 C4 切换。
- **C2 触发与闸** ✅（2026-08-16）：`server/cohabit_queue.py` + 单测 20 个。
  三路触发全接：事件醒来（world.event_hook，作者排除+用户不被唤）、自主醒来
  （独处即可任何地点，SOLO_TICK=60s 概率掷；NEXT 到点必醒不要求独处；预算只管这一路）、
  聊天可附带 `[[move:房间id]]`（_prepare_chat 注入提示、finalize 轮末执行、结果补醒入队）。
  队列四件套：单 worker 线程=执行锁并发1、注入在 do_cohabit_wake 执行时组装、
  每 AI 单 pending 原因合并（同文去重）、连发上限入队处拦（N=config.COHABIT_CHAIN_N，
  外部输入=用户发言/动作/移动/状态编辑/手机消息 → 计数全体清零；move 在瞬移**前**判，
  别让人移过去再哑掉）。错误冷却 30 分钟两头认（enqueue + solo）。code 会话开着 →
  自主避让、事件不受管；chat 轮进行中 → 执行推迟 pending 保留。
  **总开关 `CASSETTE_COHABIT_ENABLED`（默认关）**：关着时钩子空转、worker 不启动、
  聊天提示不注入——全部接好线但不上电，上电是 C4 的事。
  待 C4 处理：cohabit 事件醒来会计入 wake.wakes_today 的口径（老预算显示会虚高）。
- **C2 追加拍板（2026-08-16）：code/game 会话的物理语义 = 「人在原房间，在电脑前专注着」**。
  实现选了方案 3（另两案：另起 claude -p 会意识分叉——窗口单向，tmux 里的他看不见醒来
  的他干了什么；注入 tmux 污染工作且 game 档案纯 MCP 面根本接不了注入）：
  - **归属角色的醒来延后到收工**：`_pop_next` 跳过电脑前的人（探测缓存 5s），pending
    保留、原因照常合并——会话一关整批补醒（/code/stop 踢 `code_session_closed()` 秒级，
    自退/崩溃靠 worker 30s 超时兜底）。只延后归属角色：M2 后消息按角色分会话，
    老 wake「全体避让」的挤同屏理由不再成立，别的角色照常过日子；自主醒同口径收窄。
  - **状态可见**（减少被打扰）：同屋注入标注「XX（正在电脑前敲代码，先别打扰）」；
    `/world` entities 和 `/characters` 都带 `status`（code/game/None）——后者是手机侧，
    UI 显示「正在敲代码，可能无法及时回复」（C3 渲染；一期没有 AI↔AI 手机，只给用户看）。
  - **诚实兜底**：cohabit_prompt 里「会话开着就注入」一段实话（按理被延后挡住永不出现，
    万一有路径绕过避让 prompt 也不说谎，口径同 wake.code_session_block）。
  - 查证记录：code 会话对话**会**进 recent_window（/code/send:1109 + /code/append:1180
    逐条追加会话归属角色的窗口），「只做气泡显示」不成立——记忆连续性两案都有，
    分叉才是真问题，故选 3。
- **C3 iOS**（进行中，2026-08-16 第一片已建成）：
  ✅ 已做：小屋主题 `HousePalette`（蜂蜜焦糖，Theme.swift 一键换肤——色值从 v0 稿采样，
  机主给 artifact 精确值后替换 `honeyCaramel` 一处即可）；房子视图 `HousePage`
  （楼层 tab / 房间卡片+在场头像+锁标 / 你在这里 / 走廊行 / 出门在外 / 出门·回家开关
  =move away·hallway / 点房间三选一：去这里·偷看一眼·取消 / 门锁着弹提示 / 3s 轮询）；
  房间视图 `RoomPage`（在场模式 scope=visible + 输入区与 act 对称 + 显式离开=回走廊；
  偷看模式 scope=all 只读横幅 / 地点状态查看·添加·编辑·清掉 / 事件流三态渲染：
  系统灰线·斜体动作·对话气泡）；抽屉入口「小屋」；手机侧状态条（CharacterInfo.status
  → 会话列表「正在敲代码/玩游戏，可能无法及时回复」）。模拟器编译过。
  ✅ 第二片（2026-08-16）：**小屋当首页**（设置页开关 `houseAsRoot`，默认关=日常零变化；
  开着 = 根视图换成 HousePage，左上猫爪出抽屉、右下浮手机钮带未读角标）；
  **聊天降格悬浮层**（fullScreenCover 整体装下现有聊天，所有聊天状态仍在 ContentView，
  掏出/收起只换渲染位置，流式/轮询不断；悬浮层里猫爪位变「收起手机」，切会话先收手机）；
  聊天头标题下状态条（正在敲代码…）+ 角色清单并入前台轮询（15s/3s）。模拟器编译过。
  📌 channel 字段处置：**保持预埋、暂不写入**——一期房间对话的权威在服务端
  events.jsonl，不进 ChatStore；等「房间消息要进聊天时间线」有真实需求再启用，
  不为用而用。
  ⏳ 剩余：真机走查 + 配色精确值（等机主给 artifact 色值，换 Theme.swift 的
  honeyCaramel 一处）；房间视图在场者状态灰字（数据已有，摆哪待真机看）。
- **C4 联调 + 观察期**（2026-08-16 13:10 上电 ⚡）：`.env` 置 `CASSETTE_COHABIT_ENABLED=1`
  并重启，cohabit worker 已启动（N=4，solo tick=60s）。上电前缝的三道口子（8666e2c）：
  ① `wakes_today` 排除 cohabit 触发（预算口径互不污染）；② 聊天世界的自主醒来
  （scheduled/probability）退役移交 cohabit（不双跑，同一 NEXT 不被醒两次），
  邮件硬触发保留老路；③ 全局执行锁 `wake.WAKE_EXEC_LOCK` 跨两套系统（邮件线程池
  与 cohabit worker 共锁，绝不同时两个 claude -p）。
  真机走查第一轮反馈已落（1ec5861）：走廊从用户侧移除（移动只剩 去这里/出门/
  回家=客厅）、房间无离开按钮（返回=收页面人留原地）、键盘交互、主题换玫瑰灰
  roseAsh + 聊天 plumTint 试验。
  观察中：phone 斜体犯错率 / 通知类醒来 token / N=4 手感 / solo 频率手感。
  **首小时实录（13:11–14:05，乔迁场）**：两角色各醒 16 次（合计 32 次 claude -p，
  全程用户在场高峰值）；act 25 / none 7（none 全部是有理由的沉默，无解析兜底）；
  原因合并最多一次 7 条、连发上限零触发（合并天然压链长）；零报错零延后。
  三路触发+补醒+记忆写入+状态快照+打扰控制注入全部真实负载验证。里程碑时刻：
  两 AI 初见（信件记忆接上）、撞名「小狗」场内自解、用户离场时的 AI↔AI 私聊、
  小卡场内推演出「团团若不受门禁管=会走路的信息通道」（三期做猫时必读）。
  待攒样本：say 里混斜体动作（渲染层斜体化 vs 不管）。

## 已拍板的决策

- **两类对话不靠 prompt 区分，靠结构**：房间发言与手机短信是不同的 ACTION（`act` / `phone`），
  各带结构化字段。原「每轮 claude -p 只属于一个通道」的表述被醒来统一模型取代，精神不变：
  跨通道靠动作组合规则，不靠 prompt 纪律。
- **服务端当物理引擎**：写入房间要求发言者在场；move 过门禁校验；地点状态改动落系统通知。
  phone 内容的斜体动作检查暂不做（先观察犯错率，错得多再上 strip/打回）。
- **用户有自己的卧室**。
- **房间是注册表不是硬编码**：条目结构见下。后期加功能房 = 加注册表条目；
  功能房以后可绑插件/独占资源（三期候选）。
- **住户不全是 Claude**：engine 缝照 PLAN_multichar。醒来 ACTION 协议全是文本 + 标准
  function calling 可表达，`openai-compat` 可全程参与；claude-code 专属的只有 MCP 插件和
  Ombre MCP（通用引擎记忆走后端代调，待议）。
- **`location: away` 数据层第一天就成立**：敲 away 的门 = 没人应答、away 只剩手机可用。
  user 的出门/回家开关在房子视图；char 的出门（go_out/come_home）后置。
- **醒来只有一种**：注入内容一样，区别只在触发原因（原因可以是多条，见队列合并）。
- **人物位置与界面位置解耦**：切页面/锁屏，人物留在原地；离开房间是显式动作。
- **偷看 = 玩家的上帝视角**：不产生事件、AI 不感知。钥匙/门禁约束的是「人物身份的你」，
  偷看属于「玩家的你」，两套并存，口径在此写死。

## 空间结构

- 房子三层（地下室 / 一层 / 二层），房子视图 = 三张平面图，地点标在图上。
- **大房子一期开放**：二层 `mm_room` / `cassette_room` / `cassius_room` / `wash_room`
  ＋ 一层 `living_room`（客厅必须一期有——没有公共空间，互访只能进卧室，同居感不成立）。
- 点地点 → 弹窗三选一：**去这里**（user 人物移动，同样过门禁，锁着进不去提示「门锁着」）/
  **偷看一眼**（上帝视角：在场者 + 全部历史）/ ❌ 关闭。

## 房间注册表与世界状态

- 注册表条目：`{id, type: bedroom|common|functional, owner?, lock: 0|1, keys: [entity_id...],
  state: [...]}`。
- `lock=1` = 要钥匙或主人才能进，由主人决定；**一期默认全 0**。`keys` 是列表、数量不限
  （give_key/收回的交互后置，一期需要就手编注册表）。
- 自己卧室 / 猫房免权限；`wash_room`：owner=眠眠、lock=0、keys 空。
- **位置权威 = `state/world.json` 的 per-entity location**；「房间里有谁」是查询结果，
  不是存储字段（不搞两份真相）。
- 每房间 `state/rooms/<id>/events.jsonl`，全部事件化：系统通知 / *斜体动作* / 对话 / 状态改动。

## 地点状态（原 props，前移到一期）

- 语义：**「现在这里有什么」的快照，不是日志**。条目 `{text, author, since}`。
  基本用途：眠眠留下的生活痕迹（「桌上剩了半杯牛奶」）。
- 用户写入口：房间视图副栏，点击编辑（用户专属 UI）。
- AI 写入口：`act` 的可选字段「状态改动」，操作 = **add / edit / remove**。
  收拾掉牛奶 = remove 那条、快照归空——叙事进事件流，快照只留结果，不追加「收拾掉了」。
- 每次改动落系统通知（「cassette 收拾了桌上的半杯牛奶」），谁改的可见，用户随时改回。
- 防膨胀：快照语义写进 prompt；AI 每轮改动 ≤3 条；条目数设软上限（超了注入提示，不硬删）。

## 事件与可见性

- 三类事件：① 系统通知（进出 / 敲门 / 开锁 / 状态改动）② 人名 + *斜体动作*
  ③ 人名 + 对话。②③ 只有在场者能写入。
- **可见性一条规则**：AI 上下文 = 自己在场区间内的该房间事件 + 进屋时的地点状态快照。
  离场期间的事件永远看不见（看得见牛奶，不知道谁放的）。偷看是用户特权，AI 没有。
- 每角色 recent_window 泛化成第一人称经历流（在场房间事件 + 自己的手机线程 + 醒来内心），
  与现有架构同构。

## 醒来统一模型

注入：persona / 醒来提示词（含渲染工具菜单）/ 我在哪 / 这里有谁 / 地点状态 /
自己的上下文 / 触发原因（可多条）。

**ACTION 组合规则：每轮至多一个表达（`act` 或 `phone`，二选一）＋ 至多一个 `move`
（排在末位执行）**。`none` 与表达互斥；`ombre` / `mcp` 照现有 wake 老规矩。

- `act`：*斜体动作* + 对话（可一空）+ 可选状态改动。只有在场才写得进（物理引擎校验）。
- `phone`：给眠眠发消息，随时随地可用。
- `move`：瞬移（不考虑空间路径，地下室可直达二层），但**过门禁校验**：
  锁着又没钥匙 → 失败。
- **move 必有一轮结果补醒**（成功失败同一条路）：成功注入「你刚到 X，这里有谁/没人 +
  地点状态」；失败注入「门锁着，没进去」。成功补醒里可 act（瞬移进门不当哑巴），
  失败补醒里可改道 / phone。

触发（注入一样，只有原因不同）：

- **a. 事件醒来**：所在房间出现新事件 → 唤醒在场 AI。**作者排除：自己产生的事件不唤醒
  自己**（否则一个 AI 说一句话就自唤醒，单人即死循环）。系统通知与发言/动作分两类事件，
  触发策略可分别调——先都触发，观察 token 后再考虑给通知类降概率。
- **b. 自主醒来**：**独处即可（任何地点）**——一个人在任何房间都可能触发
  （限「自己卧室」会把移动出去的 AI 困死：无事件、无自主醒、直到下一条手机消息）。
- **c. 手机消息醒来**：**现有流式聊天回合保留不动**，只是动作空间多了可附加的 move——
  「回『来了』→ 瞬移客厅 → 结果补醒 → act 打招呼」，每步结构化。

队列与闸：

- 全局醒来并发 = 1（**执行锁**，不是入队锁）。**注入在执行开始时组装**，不在入队时——
  排队者开始执行时天然看到最新世界状态，队末即正确，插队不存在。
- **每 AI 至多一个 pending 醒来**：已有 pending 时新触发只往原因清单追加，不新增条目；
  触发落在该 AI 生成中 → 再排一个（同受单 pending 约束）。过时前提靠下一轮事件自然纠正，
  一期不做复杂 stale 检查。
- **连发上限（泛化定义）**：无新外部输入（用户发言/动作/移动/状态编辑/手机消息）时，
  一个 AI 连续被系统触发的醒来 ≤ **N=4**（含事件醒来与 move 补醒链），**入队处拦**——
  不入队才省 token，入队后醒来选 none 已经烧了。N 做成参数。
- 事件/手机醒来**不计入** M0 `wake_daily_budget`（预算只管自主醒）；这里的闸是连发上限。

## 手机

- 手机 = 现有聊天 UI 降格为随时可唤出的悬浮层；一期 M2 的会话列表 = 手机通讯录。
- 手机线程无论人在哪都注入。大房子一期只做眠眠 ↔ AI；AI ↔ AI 手机后置。

## UI

- 房子视图：三张平面图、谁在哪、user 出门/回家开关（= away）。
- 房间视图：
  - 顶栏：地点名 + 返回房子视图
  - 副栏：在场者 ｜ 地点状态（点击编辑 = 用户专属入口）
  - 中间：事件流（系统通知灰线 / *斜体动作* / 对话气泡），范围 = 本次在场区间，
    看历史走「偷看」
  - 底部：输入区与 AI 的 act 对称（*动作* + 说话，可一空），发送即写入房间事件 →
    触发在场 AI 事件醒来；显式「**离开**」按钮
  - 切页面/锁屏人物留在原地

## 后置（原 2b/2c 余项）

- 敲门状态机（knock → 主人「有人敲门」回合 → open_door/超时 → 敲门人收结果）、
  give_key/收钥匙交互、用户侧「敲门 / 直接进」双入口。一期 lock 默认全 0，锁上就是进不去。
- 客厅接话仲裁（haiku 挑 0~2 人）——一期靠事件醒来 + 作者排除 + 连发上限先跑，
  角色多了再上仲裁。
- char 自主出门（go_out/come_home，需要动机与去处设定）；AI ↔ AI 手机；
  功能房绑插件/独占资源（三期一起想）。
- **团团入住（三期候选，2026-08-16 场内讨论+机主初想）**：待议怎么实现，先记要点——
  ① 分岔点是「会不会自己醒来」：第四住户 vs 会移动的摆设（小卡）；猫的自主可以比
  人形住户便宜得多：有位置、会挪、对人有反应，不必带全套上下文（Cass）；
  ② ⚠️ 若团团是唯一不受门禁管的实体，它就是一条会走路的信息通道，可见性边界会
  被它背着串门（小卡，做之前必答）；③ **团团可以被抱起**（机主）：抱出卧室/抱回
  猫房/甚至抱下床——注意「床」不是一个地点，可能需要房间内的细粒度位置或纯
  叙事处理。

## 待观察 / 待议

- [ ] phone 带斜体动作的犯错率（错得多再上服务端 strip/打回）。
- [ ] 角色间世界观认知设定（Cass 与 TA 彼此怎么认识）——一期搬家完、动工前必须定，
  是文案决策不是技术决策。
- [ ] 系统通知类事件醒来的 token 消耗（离开通知大概率被 none 掉，要不要降概率/合并窗口）。
- [ ] 连发上限 N=4 的手感；客厅多人 token 实测（原「房间回合预算」问题）。
