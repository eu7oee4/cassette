# 二期 Plan：同居世界（别墅/大平层）

> 2026-08-12 方向定稿。二期从「普通群聊」换成空间化的同居场景：每个角色一间卧室，
> 客厅是公共空间，真实交互与手机 text 是代码层区分的两类对话。
> 前提是一期（PLAN_multichar.md）落地；一期唯一为本期做的预埋 = M2 给 ChatMessage
> 加 `channel` 字段（默认 `text`）。

## 已拍板的决策

- **两类对话不靠 prompt 区分，靠结构**：每一轮 claude -p 只属于一个通道——
  房间发言触发的回合，回复天然是房间发言（可带 *斜体动作*）；text 触发的回合，
  回复天然是短信。**跨通道只能走工具**：真实回合里发消息用 `phone_send(to, text)`；
  手机回合里没有任何真实交互工具，想见面只有 `go_to(room)` / `knock(door)`。
- **服务端当物理引擎**：房间发言校验发言者 location（不同房直接拒）；移动/敲门/放置
  改世界状态。**但 phone_send 内容的斜体动作检查暂不做**——先观察模型犯错率，
  错得多再上 strip/打回（待观察项，见文末）。
- **用户有自己的卧室**。
- **房间是注册表不是硬编码**：`{id, type: bedroom|common|functional, owner?, props}`，
  门禁规则挂 type（bedroom 要敲门、common 自由进出）。后期加功能房 = 加注册表条目；
  功能房以后可绑插件/独占资源（畅想：「工作室」绑 code 会话，三期工作群的候选住处）。
- **出门/不在家：数据层一步到位，行为层 user 先行**。`location: away` 对所有实体
  第一天就成立（敲 away 的门 = 没人应答、away 只剩手机可用）；触发方式分先后——
  user 是 UI 出门/回家开关（2a 就有），char 的出门归 2c（wake 动作加 go_out/come_home，
  需要动机与去处设定）。

## 世界模型

- `state/world.json`：每个实体（user + 各 char）的 location、门的状态。权威在服务端。
- 每房间 `state/rooms/<id>/events.jsonl`：发言、动作、进出、放置物品，全部事件化。
- 房间快照 `props`：`[{"桌上": "一杯牛奶", "since": ...}]`，进屋即见。
- **可见性一条规则**：角色上下文 = 自己在场区间内的事件 + 进屋时的当前快照。
  看得见牛奶，不知道谁放的（那是不在场时的事件），除非有人说。
- 每角色的 recent_window 泛化成「第一人称经历流」（房间事件 + 自己的手机线程 +
  醒来内心）——与现有架构同构，不推倒。
- 并发：回合生成期间世界变了（对方走了/用户移动）→ 沿用 wake 的 stale 抑制思路。

## 敲门状态机

`knock(door)` → 主人在卧室 → 给主人起一轮「有人敲门」回合，`open_door(yes/no)` 应答
或不理（超时 = 没应答）→ 敲门人收到结果。主人不在（含 away）→ 直接「没人应答」。
对人对机同一套规则。

## 客厅多人

- 接话仲裁沿用原二期群聊方案：被点名必答；否则便宜仲裁（haiku）挑 0~2 人；
  用户两条消息之间 AI 连发上限，防死循环。
- 回复走 outbox + char_id + room_id 投递，天然异步，可加随机延迟拟人。
- **纪律写死在每轮 prompt**：只写自己的话和动作，不许描写别人（含用户）做了什么。

## UI 概念

- 房子视图：房间列表/平面图，谁在哪、门开没开；user 的出门/回家开关。
- 房间流视图：动作斜体、进出灰线、顶部物品栏。
- **手机 = 现有聊天 UI 降格为随时可唤出的悬浮层**——一期 M2 的会话列表就是手机的通讯录。

## 分期

- **2a**：地形 + location + 两通道打通。房间注册表、user 卧室与出门开关、
  卧室真实私聊、手机 text、敲门状态机。先不做客厅多人。
- **2b**：客厅多人（仲裁 + 连发上限）+ 物品/快照 + 事件可见性完整版。
- **2c**：角色自主生活。wake 动作空间接入移动/敲门/放置/出门——「主动消息」
  空间化为「他从卧室出来给你桌上放了杯牛奶」。char 间互访也在这档。

## 待观察 / 待议

- [ ] phone_send 带斜体动作的犯错率（错得多再上服务端 strip/打回）。
- [ ] 角色间世界观认知设定（Cass 与 TA 同一屋檐下彼此怎么认识）——一期搬家完、
  2a 动工前必须定，是文案决策不是技术决策。
- [ ] 客厅每句话触发多角色回合，token 数倍于私聊——M0 醒来预算之外，
  可能需要「房间回合」预算，2b 时看实测。
- [ ] 功能房绑插件/独占资源的具体形态（三期一起想）。
