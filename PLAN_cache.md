# PLAN_cache —— prompt 缓存

2026-08-27。起因：眠眠问「我们这个 app 有没有做缓存」。

---

## 0. 结论速览

- **原状**：每轮起一次性 `claude -p`，整个 prompt 拼成**一个 user 文本块**。块是缓存的最小单位，尾巴一变整块作废——每轮白重建约两万 token。全仓 `cache_control` 零命中。
- **第二刀（已完工上线）**：给能力菜单切独立 content block 打 `cache_control`，稳定段提到最前面。commit `5d92b11`，**未 push**。实测 cass 每轮省 7,667 token、小卡 6,646。
- **第三刀（查清，眠眠拍板先不动）**：把时间线也纳入缓存前缀。要动三路窗口的锚定，且改的是「模型看到多少历史」，是产品决定不是优化。
- **第一刀（作废）**：原方案「菜单搬进系统提示词」。眠眠指出那等于把会变的东西焊进最不该变的块，被第二刀取代。

---

## 1. claude CLI 的断点机制（全部实测，无文档）

| 事实 | 怎么验的 |
|---|---|
| CLI 自己打 **3** 个断点：tools / system / 消息末尾 | 换掉 system prompt、tools 不变 → tools 那 29,690 照样命中（说明 tools 有独立断点、排在 system 前）；换回真人设 read 涨 3,140，正好是 `persona.md` 的量 |
| `--tools ""` 也照占 3 个 | 断点标记**无条件放**，跟里面装没装东西无关。（曾拿「没工具还占 3 个」反推「不是 tools 断点」，推错了） |
| API 上限 4 个 → **我们只有一个名额** | 打 2 个即 `A maximum of 4 blocks with cache_control may be provided. Found 5.` |
| **ttl 必须写 `"1h"`** | CLI 三个断点全是 1h，而 1h 块不能排在 5m 块后面。用默认 5m 直接 400，且报错只说排序不说 ttl |
| 消息末尾那个断点是给**工具轮次**用的，不是跨轮 | 第二次请求的前缀 = 第一次全部 + tool_result。同一 session 内 `cache_read` 一路爬升即是它 |
| `DISABLE_PROMPT_CACHING=1` 能撤掉 CLI 那 3 个，4 个名额全归自己 | 实测有效（打 4 个不报 400）。**但现在没地方用第二个断点**，且是未文档化环境变量，暂不碰 |

> 断点缓存的是**它前面的全部**，不是它自己那一块。所以一个断点切在稳定段末尾，就能把 tools + system + 稳定段整个包住。

---

## 2. 第二刀：菜单块（已完工）

### 改了什么
| 文件 | 改动 |
|---|---|
| `pipeline.py` | `SplitPrompt`（str 子类，本体即完整字符串，额外带 `cut`；老调用点/测试/日志一个字没改）；断点机制 + `disable_cache_bp` 自动降级；`stdin_payload` 统一入口；`call_claude` 两条路合并 |
| `sse.py` | 流式一律走 stream-json 输入；`_watch_bp` 盯断点被拒 |
| `wake.py` | 菜单提到最前；子进程改 stream-json + 降级重试 |
| `cohabit.py` | 同上 |

### 两条纪律
1. **降级是上线前提，不是锦上添花。** CLI 哪天升级多打一个断点，我们这个会把每轮顶成 400。非流式原地重试（上层无感）；流式只降级不重试——响应头已发出，重起子进程会打乱已上屏的字。
2. **拆块只改投递方式，不改内容。** 已验证「拼回去和原文逐字相同」。

### 实测（生产配置）
```
cass  第一轮 create=11,471 read=32,860 → 第二轮 create=3,804 read=40,538   省 7,667
小卡  第一轮 create=42,685 read=0      → 第二轮 create=14,759 read=27,910  省 6,646
      （27,910 − 21,264 tools+system = 6,646，正是菜单块）
```

---

## 3. 第三刀：时间线纳入缓存（**不动手**）

### 为什么现在拿不到
`pipeline.build_context_timeline` 把**三路**合并按时间戳排序，三路各自都是滑动窗口：

| 来源 | 取法 | 当前值 |
|---|---|---|
| 对话 | chat=app 的 `suffix(sendHistoryCap)`；wake=`recent_window[-clamp(n,20,300)]`；cohabit=`[-clamp(n,20,40)]` | cap 100 / cass 50 / cohabit 40 |
| 醒来内心 | `read_wake_log(limit=100)` 过滤后取最后 N | **5**（`reflect_limit`，写死在函数默认参数，三条路都没传过） |
| 小屋经历 | `world.read_experience(cid, limit=N)` | `experience_limit`，08-27 13:38 由 120 调为 **60** |

**「last N」窗口无论切点选在哪都拿不到稳定前缀**：切点取太老 → 之前没内容，稳定块是空的；取得够近 → 就含了各路窗口起点附近的行，而那些正是滑动时最先被挤掉的。两头堵死。

更麻烦的是三路交错：内心只留 5 条，第 6 条一来最老那条被挤掉，而它在合并时间线里位于**中间**——中间凭空少一行，前缀直接断。

### 真要做的话
三路窗口起点必须一起锚定；且因为按时间戳合并，锚定单位只能是**时间戳不能是条数**：

```
三路统一取 ts >= A 的全部 → 合并排序 → 断点切在锚定的 T
A、T 每 K 分钟或体积超限才前移
```

（眠眠推的原始形式，按条数版：`anchor = floor((total-100)/20)*20`，注入 `[anchor, total)`，断点切 `anchor+100`。**切点自己也必须锚定**——切在时间线尾巴是错的，尾巴每轮在长。）

### 为什么不做
1. **不再是纯性能改动。**「最近 40 条对话 + 5 条内心 + 60 条经历」换成「A 时刻以来的全部」，模型看到的历史结构整个变了（内心那路尤其，5 条可能变几十条）。
2. **体积从可算变不可预测。** 现在每路有硬上限；改成按时间取后，小屋热闹时经历流能瞬间灌几十条，只能靠体积上限兜底重锚——而那意味着热闹时重锚频繁、缓存反而更难命中。

---

## 4. 一轮聊天到底注进去了什么（08-27 实测，cass）

单轮总上下文 ≈ **54,000 token**：

```
工具 schema   29,690 token   55%   ← 83 个工具的完整参数说明，聊天路没开 ToolSearch 延迟
system 人设    3,140 token    6%
prompt        ~21,000 token  39%
```

prompt 那 19,144 字符拆开（当时 `experience_limit=120`）：

```
能力菜单 + 贴纸库   6,620 字符
时间线            11,597 字符
   对话        49 条   5,673 字符  49%
   醒来内心     5 条     651 字符   6%
   小屋经历   120 条   5,209 字符  45%   ← 条数是对话的 2.4 倍
小屋现场/人称/move/当前时间/新消息  ~900 字符
```

**注意力稀释的最大来源是工具 schema，不是小屋。** 聊天那条路故意没开 ToolSearch 延迟（机主口径：醒来不怕慢但怕迷茫，聊天有明确唤醒句不迷茫，而多一轮往返人在手机前等得见）。

但缓存改完之后这笔账变了：那 29,690 现在**每轮都命中缓存**，省它省不到多少钱——它真正占的是**上下文位置和注意力**。要不要给聊天也开延迟，是个新问题，值得重新权衡。（`TOOL_SEARCH_CONTEXTS` 就是那一行开关。）

---

## 5. 待办

- [ ] `5d92b11` 未 push（`main` ahead origin 75）
- [ ] 第三刀：等眠眠定，涉及产品语义
- [ ] 聊天路要不要开 ToolSearch 延迟：缓存上线后账变了，值得重新算
- [ ] `DISABLE_PROMPT_CACHING` 那条路：等真需要第二个断点时再验（冷启自建缓存那条还没干净验过）
