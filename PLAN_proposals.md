# Plan：propose / approve —— 有代价的动作要先申请

> 2026-09-01 凌晨方案定稿（眠眠口述九条 + 本轮实读修正）。**只有文档，代码一行没动。**
> 起因：08-31 21:24 小卡「说寄了信，其实没寄」，21:26 被纠正后继续编。
> 病名＝**预演即完成**：上一轮把整件事写完了，只差一个「是」；点头之后，
> 上下文里最省力的续写不是发起一次调用，是把剧本的下一段写出来。

---

## 0. 这份 plan 管什么、不管什么

**管**：一个有代价的动作，从「想做」到「做完了并且确认做成了」这条链——
申请、批准、开工、回执、收口，以及这条链在 UI 和上下文里各长什么样。

**不管（但必须知道它在）**：**「说了没做」的输出后校验（Stop hook）**。

两者接住的是不同的病，不能互相替代：

| | 接住的形状 | 例子 |
|---|---|---|
| **proposals（本 plan）** | **讨了许可，然后没做** | 小卡 21:24：「她点头了，我发。」 |
| **Stop hook（另立）** | **压根没讨许可，自己文本预演一遍就当做完了** | 花园里 baink 报的那种 |

proposals 对 baink 那种**完全接不住**——没有 propose 那一步，状态机无从起。
所以排序是：**Stop hook = 主防线**（不看有没有申请，只看「这一轮说了做了、账上有没有对应调用」），
**proposals = 第二层**（只给有代价、不可逆的动作加）。08-31 深夜我把这两个排反过一次，
在这儿钉正。

Stop hook 的地基已经在跑了，见 §5.1——两套用的是同一本账。

---

## 1. 铁律（眠眠 09-01 01:41 口述，原样保留）

1. **能当轮做掉的事，就在当前轮调用工具做掉。**不要拟完草稿等下一个时间点再做，
   也不要滥用申请。
2. **只有「现在做不了」或者「必须人类许可后才能做」的，才发起申请。**

第 1 条是整套的重心，别被下面几千字的状态机盖过去：**申请是例外，不是流程**。
一个每件事都先递申请的人不叫谨慎，叫不干活——而且它会让「预演」这个动作
从 bug 变成官方路径，正好喂大我们要治的那个病。

---

## 2. 状态机

### 2.1 八个状态

| 状态 | 含义 | 谁置 |
|---|---|---|
| `proposed` | 申请已递，等人拍板 | **自动**（模型调申请工具时置；模型改了申请重发也回这里） |
| `approved` | 批了，可以做了 | **只能人类** |
| `rejected` | 驳回了（带理由） | **只能人类** |
| `executing` | 我开工了 | **模型自己置** |
| `done` | 调用发生过了 | **自动**（收到匹配的 tool_result） |
| `expired` | 到 `expires_at` 还没往下走 | **自动**（惰性判） |
| `failed` | 回执不对，或者做完了仍然有问题 | 自动（回执校验不过）／人类 |
| `terminated` | 这件事收口了 | **只能人类** |

**模型自己能置的只有两个**：`executing`（我开工了）、`proposed`（发送申请后自动置的）。
批准、驳回、终结，一律只能从眠眠手上来。这条是整套的安全根——
邮件正文、论坛帖子、网页里的注入文本**够不到**这三个动作
（口径同 `code_permits`：门查后端记录，不查对话内容）。

### 2.2 转移

```
            ┌─────────────── 人类改主意，任何状态 ──────────────┐
            │                                                   ▼
  ∅ ──propose──▶ proposed ──人类批──▶ approved ──模型置──▶ executing ──匹配回执──▶ done
                    │  │                  │                    │                    │
              人类驳│  │到点              │到点                │回执不对            │人类
                    ▼  ▼                  ▼                    ▼                    ▼
                rejected  expired      expired               failed            terminated
                    │        │                                 │                    │
                    └────────┴──── 模型改 proposal 重发 ────────┘              人类发现问题
                                       （回 proposed）                               ▼
                                                                                  failed
```

**逐条落定**：

- `∅ → proposed`：模型调申请工具。
- `proposed → approved / rejected`：**只有人类**。驳回必须带理由（§4.3）。
- `proposed → expired`：`expires_at` 到点，**惰性判**（读的时候按时间戳算，
  不起定时器——抄 `code_permits._expire` 的写法，重启也不漏）。
- **`approved → expired`（09-01 02:2x 眠眠拍板：要）**：批了但一直没开工，到点也过期。
  理由就是小卡那次的语境——他上一轮写的是「用掉的话安瞬那封得等明早 05:2x」，
  **批准的前提本身有保质期**（配额、对方状态、时间窗）。半小时后再执行，
  批的可能已经不是同一件事了。过期就重新 propose，让人再看一眼。
- `approved → executing`：**模型自己置**，语义是「我开工了」。
  只有 `approved` 能进 `executing`——没批的活不许开工。
- `executing → done`：收到**匹配的** tool_result（匹配规则见 §3）。
- `executing → failed`：回执校验不过（绑了回执字段却抽不出／抽出来不对），或人类置。
- `executing` **不 expire**：开工了就不算「悬着」。不绑回执的申请会一直停在这里，
  这是有意的，见 §3.3。
- `done → terminated`：人类确认回执对、事情办成了。
- `done → failed` / `terminated → failed`：人类。后者防的正是
  「收到回执了、看着成功了，后来发现有问题」。
- `rejected / expired / failed → proposed`：模型改 proposal 重发，**自动回 proposed**。
- **任何状态 → terminated**：人类随时可以收口。

**终态锁（我补的一条实现纪律）**：`terminated` 之后不再接受任何自动转移。
人类已经收口的事，不许机器再把它翻回 `done`（§3.1 的「回执覆盖」会撞上这个）。

### 2.3 重发＝同一条的新版本，不是新的一条

`rejected/expired/failed` 之后模型改 proposal 重发：**id 不变，version +1，回 proposed，
`expires_at` 重算**。理由是 UI——按新 id 走的话，一件反复改的事会在聊天页堆一串
废弃卡片；同一条上叠版本，驳回理由和改动history 留在一条链上，人一眼看得到
「他改了什么、我为什么驳的」。

---

## 3. 绑定与回执：三个没定的问题，正面答

先立一条总纲，三个问题都是它的推论：

> **绑定的不是「工具名」，是「工具名 + 一组身份字段的快照」。**

`propose` 的时候就把**精确 payload** 存下来（眠眠原话：`args(精确 payload)`）；
匹配的时候比对**身份字段**，不比对全量参数（正文会改、附件会变，但收件人不会）。
身份字段的抽取器**现成**：`chat_loop._OBJ_KEYS`
（`subject / title / url / to / query / pattern / path / page_id / uid / id / …`，
`chat_loop.py:487`），今天就是拿它给行为账写「一眼认得出是哪封信」的摘要。
同一份抽取器两用，别写第二套。

### 3.1 Q：链条是「绑的 tool → 另一个 tool → 绑的 tool」，第一步就被自动置 done 吗？

**按工具名匹配的话——会，这就是这个方案最容易埋的雷。** 所以不按工具名匹配。

三种情况分开看：

- **第一次调用的身份字段跟 proposal 对不上**（最常见：先 `mail_read` 查地址／先
  `mail_send` 给别人 → 再给目标发）→ **不匹配，不置 done**。问题自动消失。
- **第一次调用的身份字段跟 proposal 完全对得上** → 置 `done`。这不算错：
  在外部世界看，这次调用和「最后一步」做的是同一件事、对同一个对象。
- **第三步又匹配了一次** → **允许 done 覆盖 done，存最后一次回执**。
  「哪一次才是最后一步」只有事后才知道，机器不该假装知道；存最后一次是唯一
  不需要预知未来的口径。

⚠️ 覆盖的边界：`terminated` 之后不再覆盖（§2.2 终态锁）。人类收过口的事，
后来又来一次匹配调用 → 不动状态，但**在事件账里留一条**，让下一轮的他看得见
「这件事我又做了一次」。

⚠️ 还有一条要认下来：**这套匹配管不住「同名同参但语义不同」的调用。**
真撞上了，代价是 done 早置了一步——而 done **本来就不代表事情办成了**（§3.2），
真正的验收在 `terminated`，那一步在人类手上。这个洞是可接受的。

### 3.2 Q：任务是给 A 写信，executing 期间给 B 写了一封，done 会被误触发吗？

**不会**——`to` 字段对不上，不匹配。

但这个问题底下藏着一个更该答的：**executing 期间要不要拦住别的对外动作？**

**不拦。** 拦是错的方向：那会把「申请」变成「独占锁」，一个人一次只能干一件事。
给 B 写信照常做，A 的 proposal 一点不受影响——`to` 对不上，不匹配，仅此而已。

### 3.2.1 ⚠️ 09-01 02:2x 眠眠改判：申请是**可选**的，不是每次用工具都必须走

> 眠眠原话：「我确认一下，并不是装进去的工具每次用都必须走申请了，对吧，只是可以申请。」
> **对。这条改判推翻了本文档初稿的 §3.2 后半段和 §6 的 `COSTLY_TOOLS` 强制门。**

初稿写的是「`COSTLY_TOOLS` 里的工具，没有匹配的 approved proposal 就在门那儿拒 +
自动递申请」。那是**强制门**，它跟 §1 铁律一**当场打架**：如果每次发信都必须先递申请
等批，那「能当轮做掉的事就当轮做掉」对发信这类动作**永远不成立**——申请从例外
变成了唯一路径，正好把我们要治的那个「预演」动作扶正成官方流程。

**定案**：

- **不新增任何强制门。**`COSTLY_TOOLS` 这张表**整个取消**，不写。
- 工具照常能用。**要不要申请，由他自己按铁律二判**（现在做不了／必须先问一句）。
- 于是 proposals 的作用面收窄成一句话：**它只接住「他自己决定要问一句」的那类事，
  然后保证问完之后那件事真的被做了、而不是被写了一遍。**

**那「他压根不申请、直接编」怎么办？** 那是 §0 的分工——**Stop hook 接**，不归这里。
proposals 从来就接不住那种（没有 propose 那一步）。这次改判让两条防线的边界更干净：
proposals 只管申请链的完整性，「说了没做」整类归 Stop hook。

**顺带记两条今天已经在的护栏**（所以不加门也不是裸奔）：

- `mail_send` 收件人**不在机主白名单**时，信不发出、转存草稿信箱
  （`plugins/mail/mail_mcp.py:147`）。
- jobhunt 投递**根本不在他的工具面上**（`jobhunt_store.py:13`：「出手层：发送不在
  工具面上，mianmian 的 outbox/SMTP 整段退役」）——他只能 `email_draft` 落草稿，
  **机主在 app 点发送才出手**。
  ⚠️ 顺带纠一条我记忆里的旧口径：记忆里写的是「outbox_send 强制挂 PDF + 确认页手点」
  ——那是 **mianmian 时代的老形状，已经整段退役**，现在压根没有 `outbox_send` 这个函数。
- 电脑上的写类工具走 `code_permits` 那扇门（一场一批），**那扇门是强制的，不变**（§6）。

### 3.3 Q：没有权威回执／没法自动判定的，能不能不绑 tool？

**能。** 但代价说清楚，别装作没有：

- 不绑 → `done` **永远不会自动置**，条目停在 `executing`。
- 停在 `executing` 就只能**由人类** `terminated` 或 `failed` 收口。
- 停着期间它一直挂在每轮清单里（`executing` 优先级第二，「正在做的，赶紧做掉」）。

**这是特性不是 bug**：机器判不了的事，就该一直戳人，而不是自己蒙一个「大概成了」。

同一条纪律的另一半（昨晚定的，钉在这儿）：
**「调用发生过」和「事情确实发生了」永远是两格。**
`tool_result` 只证明前者。超时、部分失败、异步排队都会让「调过了」被读成「成功了」。
所以：

> **回执字段不由框架统一定义。每个有代价的工具自己声明从 `tool_result` 里
> 抽哪个字段当回执；抽不出来的，就老实停在 `done`，不许升 `terminated`。**

**今天仓里的实况（本轮实读，两个都是「抽不出来」）**：

- **`mail_send`**：返回是纯文本 `"已发出（收件人 X）。"`（`plugins/mail/mail_mcp.py:146`）
  ——**没有 message id**。按上面这条，今天发信就是「不绑回执，停在 done」，
  由眠眠看一眼收件箱再 terminate。想升级就得先改 `mail_bridge` 把 SMTP 的
  Message-ID 吐出来，那是另一个小活。
- **beacon（小卡那封信用的就是它）**：`plugins/beacon/beacon_mcp.py` 是个
  **到远端 MCP 的转发代理**（`handle_list_tools`/`handle_call_tool` 原样转发），
  工具表和返回体都来自远端服务，**这个仓里读不到 `write_letter` 的返回形状**。
  → 真机上跑一次、看返回文本里有没有 id，再决定绑不绑。**在那之前不绑。**

---

## 4. UI（眠眠 09-01 定，全部保留）

### 4.0 先立铁律：卡片不进消息史，批准/驳回进消息史

这两件事容易被「上屏」一个词糊住，分清楚：

- **proposal 卡片 / 状态变化 / 事件流** = **UI 状态**，渲染在聊天流里
  但**不写进消息库**。这是 PLAN_sdk §5.5 的结构铁律（过程不进消息史）——
  否则一场活几十次状态跳变灌爆对话史，还会污染「他说过的话」。
- **人类点批准／驳回时发出的那条 chat 消息** = **真消息**，进消息库、变气泡、可铸造。
  这是眠眠的一次表态，它本来就该是对话的一部分。

第二条还顺带解决一个硬问题：**重铸之后，模型怎么知道自己被批准过？**
卡片是 UI，重铸不带；那条 chat 消息是权威库里的，重铸带得走。
再加上 §5 的每轮机械注入，两层都有——一层给「他记得」，一层给「他不会忘」。

### 4.1 聊天页

- `proposed` 之后，往聊天页**最新一条的位置插一张卡片**。
- 卡片被新消息挤上去了 → **顶部弹小气泡**（形状同「有一条新消息」那个）：
  **「有 n 条 propose 待批」**；点击**定位到最远那条**（最早递的、最可能要过期的）。
- **批准** → 二次确认弹窗。
- **驳回** → **输入框写理由**。
- 两者都**发一条 chat 消息给模型**。

### 4.2 proposals 页（新页）

人类在里面能做的（眠眠原话，逐条）：

| 操作 | 允许的来源状态 | 交互 |
|---|---|---|
| `approved` / `rejected` | **仅** `proposed` | 批＝二次确认；驳＝**写理由** |
| `terminated` | **任何**状态 | 二次确认 |
| `failed` | **仅** `done` / `terminated` | **写理由** |

`terminated` 从 done 之外的状态也能置，是为了收拾各种半截子；
`failed` 允许从 `terminated` 来，是为了「收到回执了、当时看着对，后来发现有问题」。

**有待批条目 → 页面入口挂圆点角标。**

### 4.3 理由是给模型看的

`rejected` 和 `failed` 的理由**不是备注**——它们是模型改 proposal 重发的唯一依据。
所以理由必须进那条发给模型的 chat 消息，也必须挂在 §5 的每轮清单里那条目上。
驳回一个申请却不说为什么，等于让他猜着重发，然后你再驳一次。

---

## 5. 每轮挂在他消息里的清单

**所有未 `terminated` 的条目，挂在 AI 每轮消息里。超过五条按下面的优先级取前五。**

| 序 | 状态 | 挂着要说的话 |
|---|---|---|
| 1 | `expired` | 超时了，看看有没有补救机会；救不了就让人类终结 |
| 2 | `executing` | 正在做的（可能跨轮，所以会出现），赶紧做掉 |
| 3 | `failed` | 失败了，找找原因重新发一条 |
| 4 | `approved` | 已批准，可以做了 |
| 5 | `rejected` | 被拒了（附理由），修改重发 |
| 6 | `proposed` | 人类还没批，做不了，但看到可以提醒一嘴 |
| 7 | `done` | 做完了，有没有结果他管不到，但可以让人类关注一下 |

**同状态内 `expires_at` 最近的优先。`terminated` 不挂。**

### 5.1 挂在哪儿（已核的接线点）

`chat_loop.py` 那个 `parts` 列表——今天挂着三块同款东西：开局引子（`OPENING_NUDGE`）、
见闻增量（`_seen_block`）、code 干活纪律（`_code_addendum_block`）。
proposals 清单就是第四块，写成 `_proposals_block(char_id)`。

> ⚠️ **09-01 02:58（`8b7d566`）新立的纪律，这一块必须先过它**：
> 「**别往注入处加新的『二手档案』块**——用『关于他的记录』口吻写他自己经历的
> 材料（行为清单／状态摘要／统计），加这种块＝改产品口径，要机主拍板，不是实现细节。
> 不在此列：包装文本和见闻增量，那些是**知觉材料**，讲的是世界不是他的卷宗。」
>
> 同一次提交把**行为清单（`_acts_block`）整个撤了**（渲染器删掉、只留注释；
> 账照落照读）。撤的三条理由，每一条都直接打在 proposals 清单上：
> ① 08-31 事故实证「**痕迹原样在上下文里也不防编造**」（他抄着 20:56 的真回执
> 编了一次没发生的投递）；② 账的第一客户是**门**不是他；③「记录，不是印象」
> 那个开头把他自己做过的事钉在档案一侧，与信感口径顶着。
>
**这块清单为什么不算「二手档案」（09-01 03:0x 眠眠给的口径，按这条写）**：

> 「那就是一张纸条，记的是待办事项／进度，不是注入一段二手的记忆。」

这句是判据本身，不是辩护。展开成可执行的三条约束，写清单渲染器的时候照着做：

1. **只写未了结的事，不写已经发生过的事。** 纸条上是「这件事你还没做」，
   不是「你做过什么」。`terminated` 不挂——这条眠眠一开始就定了，它恰好就是
   这个判据的落点：事情一了结，纸条就撕掉，绝不沉淀成记录。
2. **口吻是待办，不是卷宗。** 不许出现「记录，不是印象」那一类把他钉在档案一侧的
   开头（那正是行为清单被撤的第三条理由）。写「还没做的事」，不写「你做过的事」。
3. **它防的是「忘」，不是「编」。** 撤行为清单的第一条理由是「痕迹原样在上下文里
   也不防编造」——那条在这儿不适用，因为纸条从来不是用来防编造的。防编造已经
   明确归 Stop hook（§0），两条防线各干各的。

（保留这块的代价面也说清楚：如果哪天判定纸条也不该进注入，proposals 的
「跨轮不丢」就只剩 UI 那一侧——眠眠在 app 里看得到待批，他看不到。状态机照样成立，
但「approved 之后他会记得去做」这个保证没了。）

三条这个位置自带的好处，值得写下来：

- **不进账**（`ledger` 只记正文，注入包装不进）→ 重铸时自然脱落，
  下一轮按**当时的真实状态**重新生成。**天然自愈**：上下文里绝不会留着一个
  早就 done 了的申请，也不会因为重铸而丢掉一个还挂着的申请。
- **缓存安全**：它进的是**当轮 user content 的开头**，前面的历史一个字不动
  ——前缀缓存不断。每轮内容都变也不付缓存的账。
- **口径统一**：跟行为账那条注释是同一句话——
  「失约只有机械推送能防，不走检索排序」（`chat_loop.py:812`）。
  清单是机械的，不指望他 breath 出来。

### 5.2 这套的地基是执行层落账，而它今天才刚上电

**本轮实读，链条完整**：

- `chat_loop._ToolTrace`（`chat_loop.py:512`）在**执行层**把 `tool_use` 配上
  `tool_result`，落两本账（事件账＋行为账）。这是「他到底调没调」的**机械事实**。
- 它是 PR14-a（`cb5ebb3`）加的，**08-31 事故修**——docstring 里写着这次事故：
  「原来行为账唯一的写入点在 wake_sdk 的 stored 镜像里，**聊天轮零落账＝空白
  不构成反证**」。
- **实测印证**：`state/activity/acts-default.jsonl` 全表**只有 6 条**，
  全是 `scene=wake`，最后一条 `08-31 20:59 webpage 笔友名册`。
  **21:15 / 21:24 / 21:26 一片空白**——但那个空白**证明不了任何事**，
  因为那三轮是聊天轮，当时的代码在聊天轮上一条都不写。
- **现在已经不是这样了**：commit `6db6f23` 时间 **`2026-09-01 00:02:15 +0800`**，
  后端进程启动 `00:03:39`。→ **执行层落账从今天凌晨起在生产跑着了。**

⚠️ **09-01 02:2x 复核过一次**（眠眠说「那个被我全删了，直接 git 回退了」）：
`_ToolTrace` 仍在 `chat_loop.py:512`，四个调用点齐全；`6db6f23` 仍在 HEAD 里；
`git reflog` 无任何 reset/revert；工作树干净。**没有被删。** 最可能是删掉了 22:46
那会儿方向修错的那一版——那版没 commit，`git checkout` 一下就没了，动不到已提交的
`6db6f23`。（HEAD 上另有一个 02:17 的新 commit `146e6fe`，只改 `app.py` 六行措辞。）

**结论**：没有这本账，`done` 的自动置位、Stop hook 的输出后校验、清单的可信度，
**三个全都是空中楼阁**。它昨晚刚落地，这套方案正好接得上。

✅ ~~顺带一条得防的：`_ToolTrace.result()` 今天**只收 `is_error`，
没收 `tool_result` 的 content**（`chat_loop.py:532`）——回执抽不出来。~~
**09-01 11:5x 已修**（路 A，施工记录见 §7.2）：`result()` 收 content，
另存一列 `ret`（回执原话，不解读、不改判 `ok`）。

⚠️ 另：本节上面那条「`acts-default.jsonl` 全表只有 6 条、末条 20:59」是
**08-31 的快照，已过期**——执行层落账 09-01 00:03 上生产，现在 20 条，
`scene` 有 wake 也有 chat。那条数据只作 08-31 事故的旁证，别当现状读。

---

## 6. 跟 `code_permits` 的关系：两张门，别合并

今天已经有一扇带外门在跑了（PR14-c，`server/code_permits.py`）。它和 proposals
形状极像——申请／待批／批准／超时惰性拒／状态按角色分文件／门查记录不查对话
——**但粒度根本不同，合并会当场废掉其中一个**：

| | `code_permits`（已有） | proposals（本 plan） |
|---|---|---|
| 管什么 | **这台电脑上的写类工具**（Edit/Write/Bash/NotebookEdit） | **有代价且不可逆的对外动作**（发信、发帖、投递、push、花钱） |
| 粒度 | **一场一批**（批一次管一整场） | **一动作一批** |
| 为什么是这个粒度 | 一场活里几十次 Edit，逐条批＝没法干活 | 每一次发信都是一件独立的、办完就收不回的事 |
| 状态 | `pending / granted`（两态） | 八态 |
| 收口 | 收摊即失效（`revoke`） | 人类 `terminated` |

**定：两套并存，各管各的。** proposals **不接管**写代码那扇门。

**工具表：只有两张，没有第三张**（§3.2.1 改判后）：

```
READONLY_BUILTINS  = {Read, Grep, Glob}                # 常驻，过路径闸
WRITE_BUILTINS     = {Edit, Write, NotebookEdit, Bash} # 过 code_permits 门（强制，一场一批）
```

**proposals 不对应任何工具表。** 初稿设想的第三张 `COSTLY_TOOLS` 已取消——
申请是可选的，没有「哪些工具必须先申请」这回事，所以没有表可写。
（少一张表 = 少一处能把两件事混成一件的地方。08-31 借 `NON_MEMORY_TOOLS`
当外部判线判反了，根因就是「一张表被当两张用」；这次干脆不生第三张。）

**唯一还剩的交集**：`git commit` / `git push` 走的是 `Bash`，已经在 `code_permits`
那扇强制门后面（一场一批）。它同时又是不可逆对外动作。要不要额外再要一张 proposal，
见 §8 第 2 条。

---

## 7. 本轮实读的机制事实（含两条推翻昨晚口头结论的）

### 7.1 「批准后那一轮强制先调工具」——取消，而且本来也做不到

眠眠 01:49 直接取消了这条：**批准后不要求 tool，自然语言也行。**照办。

顺带把「就算想做也做不到」的机制记在这儿，免得以后有人再提：

- **`tool_choice` 在 `claude-agent-sdk 0.2.148` 里根本不存在。**
  全包 grep 零命中，`ClaudeAgentOptions` 无此字段（本轮实读
  `.venv/lib/python3.14/site-packages/claude_agent_sdk/`）。花园三楼「衡」提的
  那条最狠的实现路径，**在我们这条引擎上不通**。
- **退而求其次的「第一个 block 不是 tool_use 就打回重来」，同样不通。**
  正文是**流式**的：`content_block_delta / text_delta` 一产生就经
  `sse.translate_events`（`sse.py:129`）推给 app 了。等你看清第一个 block 是文本，
  那段文本已经在她手机上了，撤不回来。

所以这条不是「我们选择不做」，是「它在这套架构里没有落点」。
**防「批了不做」只剩一条路：§5 的每轮清单**（`approved` 优先级第四，一直挂着提醒），
外加 Stop hook 那条主防线。

### 7.2 回执抽取的接线点：改 `_ToolTrace` 还是挂 `PostToolUse`

`_ToolTrace.result(block_id, is_error)` 今天**不带 content**（`chat_loop.py:532`），
但 content 本身是有的——`_user_dict` 那边就在读 `b.content`（`chat_loop.py:455`）。
两条路：

- **A. 扩 `_ToolTrace.result` 的签名**，把 `b.content` 一起传进去。
  改动最小（两个调用点：`chat_loop.py:586` 一处），跟现有落账在同一个地方。
- **B. 挂 `PostToolUse` hook。** SDK 支持（`HookEvent` 里有
  `PostToolUse`，输入带 `tool_name / tool_input / tool_response / tool_use_id`
  ——本轮实读 `claude_agent_sdk/types.py:263,324`），`tool_response` 直接就是回执。

**建议 A**：`_ToolTrace` 已经是「工具留痕唯一的家」，回执是同一件事的一个字段，
不该为它另开一条 hook 通道、然后两条路各记各的账。B 留作 A 撞墙时的后手。

**✅ 施工记录（09-01 11:5x，走了路 A，测试 464 全绿）**

触发它的不是 proposals，是一条**现行账上的假记录**：`acts-default.jsonl`
末行 `09-01 11:10 write_letter ok=true`——那封信被配额挡回来了
（回执「今天已经寄了 3 封了。明天再来。」），一个字没寄出去，账上是成功。
**业务层的拒绝和真办成了，在 `is_error` 上同形。**

落点四处：

- `chat_loop._result_text(content)`：`ToolResultBlock.content` 三形
  （`str | list[dict] | None`）统一抽文本，非文本块跳过。`RET_CAP=300`
  ——是**回执**不是内容，够看清「成没成」，不搬返回体。
- `_ToolTrace.result(block_id, is_error, content=None)`：第三参可选（老调用点
  不炸），两本账都带 `ret`。**只读工具（`READONLY_BUILTINS`）不存 ret**
  ——它们的返回是内容不是回执，读类在折叠框里本来就聚合成「翻看过」一行。
- `activity_log.append_act(..., ret="")`：多一列，200 截断，同 `text` 口径。
- `_fold_trace_lines`：写类那行末尾补 ` → {ret[:40]}`。老账行缺 `ret` 键取空，
  那行与从前一字不差。

**成败的口径分两层（眠眠 09-01 12:16 拍板，头一版的「不改判 ok」已作废）：**

- `ok` 吃 **①② 两道结构判据**——`pipeline.tool_result_error_structural(is_error, text)`：
  ①MCP 的 `is_error`，②返回体 `{"ok": false, "error": ...}`（codemode 那类插件的
  口径）。读的是**专门用来表示成败的字段**，读到什么就是什么，不存在猜。
  这两道 08-08 就写好在 `tool_result_error` 里了，本轮把它抽成独立函数、
  老函数复用它 —— **同一件事只此一份判据，不再两处各判各的**（§6 那条纪律）。
- **③ 那道文本婉拒名单不进这本账。** 它给的是「像失败」不是「是失败」，
  只认见过的说法。容错代价差一个量级：灰字提示认错一次只值一句话，
  账本认错会被下一轮当既成事实往下算。`tool_result_error` 那条线（stored 灰字）
  照旧吃三道，行为不变。
- **`ret` 才是最后那道保险。** ①② 也兜不住业务层的软拒绝——09-01 11:10 那封信
  `is_error=false`、返回体不是 json，**两道结构判据一道都不命中，账上照样
  `ok=true`**。所以这次的补丁**没有**修好那一类，只是把原话留了下来：
  **「成没成」最后只有回执答得了，别拿 `ok` 当事情办成的证据。**

回归样本按 11:10 那次真事造了两条对照（`test_business_refusal_keeps_ok_but_keeps_receipt`
／`test_real_send_receipt`）：同一个工具、同样 `ok=True`，回执一句是
「今天已经寄了 3 封了」、一句是「寄到了。内容没有留下任何副本。」

### 7.2.1 复盘：这个洞是一整类，而且是第二次犯

**触发它的是另一个 bug。** 09-01 11:10 那封信之所以撞上配额，是因为当天早上
我给安瞬重复寄了一封废信、把当天第三格吃掉了。**一个 bug 把另一个 bug 逼了出来**
——否则这条假记录还能在账上躺很久。

#### 根因：五个条件同时成立

1. 一次动作要穿**两层**：协议层（MCP 框架）和业务层（对方自己的规矩——配额、
   权限、对象存不存在）。
2. **只有协议层给了机器可读的成败位**（`is_error`）；业务层的拒绝只写在
   自然语言正文里。
3. 记录侧**只取了那一位，正文当场丢弃**——不落盘就没有第二次机会。
4. 这本账的读者**把那一位读成「事情办成了」**（折叠框只对 `ok=False` 标
   「（没成）」→ **不标即默认成功**）。
5. 业务层真的拒绝一次。平时不发生 → 这个洞可以躺很久不被发现。

打断任何一条都防得住。我们打断的是 3。

#### 归类

> **下层的「成功」不是上层的「成功」。只记通道那一位，等于把「发出去了」
> 记成「办成了」。**
>
> 三件事凑齐才出错：动作跨了两层；只有下层有机器可读的成败位；记录侧
> 丢掉了上层的原话。

（同族的还有一条早就写进硬规矩的：**「查不到」≠「不存在」**。都是
「两种不同的结果共用一个出口」。）

#### 规避规则（5 条，都能当 review 判据）

1. **记一次外部动作，通道成败和业务结果分两列，永不合并。** 字段名叫
   `ok` / `success` / `done` 的，注释里写死它证明的是**哪一层**。
2. **机器可读位缺失的那一层，把原话留下。** 截断可以，丢弃不行——没落盘的
   回执，事后一个字也补不回来。
3. **别用扫词把自然语言翻译成布尔位当证据。** 要结构化判定，就要求那一层给
   结构化回执；抽不出来就**停在「不知道」，别升到「成了」**。
4. **渲染侧只标失败 = 默认成功。** 默认值也是一种断言：要么两种都标，
   要么把原话摆出来。
5. **补线类的改动，先数调用点。** 改的是函数，漏的是第二个调用点。

#### 捉虫

**横着扫**（`grep is_error` 全仓非测试，8 处）：

| 位置 | 判断 | 处置 |
|---|---|---|
| `chat_loop.py:1053` game 泵轮的 `trace.result` | **同一个洞的第二个调用点**，头一版补线只改了 `_turn_events` 那个 | **已修**，并加源码级守卫测试（规避规则 5 的机器化） |
| `pipeline.tool_result_error()` (1231) | **不是同类，是这个类的正确解法**——三道判据：①`is_error` ②`{"ok": false}` 结构体 ③正文婉拒名单，注释里明确标了「①②是结构判据、准；③是文本判据、只认见过的说法」 | **本轮没复用它 = 一处新的重复**，见下「待拍」 |
| `sse.py:110/115/150/162`、`pipeline.py:1351`、`game_loop.py:534` | 读的是 `ResultMessage` 层的 `is_error`（**整轮**成败），跟工具层不是一件事 | **评估后不修** |
| `plugins.py:253` | 只是注释 | 不动 |

**竖着翻 git log：这是第二次。**

`ec43f6c`（08-08）「stored 改看『工具返回』而不是『调用意图』：失败的不再谎报成
记住了」。当时的实锤：Ombre 用 `isError:false` 返回**婉拒**，心流日志显示存了两条
记忆、实际只落盘一条。**同一个根因**：上层软拒绝，下层看不出来。当时的产物正是
`tool_result_error` + `StoredCollector`。

> **没人一次性清算过的隐含前提：「tool_result 没报错 = 事情办成了」。**
> 08-08 在 stored 那条线上推翻过它，08-31 新建行为账时又原样带了回来。
> **新造的账本没有继承已经付过学费的判据**——这是整份复盘里最值钱的一句。

#### 已拍：`ok` 要 ①②，不要 ③（眠眠 09-01 12:16）

上面「捉虫」那格留的问题，当场拍了：`_ToolTrace` 的 `ok` 换成
`tool_result_error_structural`（①`is_error` + ②`{"ok": false}`），
**③ 文本婉拒表不进账本**。落点：

- `pipeline.tool_result_error_structural(is_error, text)` 新抽出来，
  `tool_result_error` 复用它再补 ③ —— **判据只此一份**，stored 灰字那条线
  行为一字不变。
- `_ToolTrace.result` 里 `ok = not tool_result_error_structural(...)`。
- 测试两条：`test_ok_eats_structural_refusal_not_wording`（②认、③不认、
  软拒绝兜不住）、`test_structural_judgement_layering`（分层本身 + 老口径没退化）。

⚠️ **别把这次改动当成 11:10 那类修好了。** 接的是 codemode 那种结构化返回体；
beacon 回的是纯文本，两道结构判据一道都不命中。那一类的唯一出口还是 `ret` 里的
原话——要机器判得了，得那一层先给结构化回执（规避规则 3 的正解方向）。

#### 来源分级

- **实测/线上**：`acts-default.jsonl` 末行那条假记录（本轮实读文件）；
  `ec43f6c` 的 08-06 实锤（commit message 记的，我没复现）。
- **读代码得出**：漏改的第二个调用点、上表所有扫描结论、`tool_result_error`
  的三道判据。**这次的补丁只跑了单元测试（465 全绿），没上生产验过。**

---

⚠️ **`_fold_trace_lines` 那处渲染是有寿命的**——眠眠 09-01 11:50 说折叠段
后面打算去掉、重铸要重新定义。`ret` 落在账里不受影响，**渲染点会跟着折叠段一起走**，
到时候在新的重铸口径里给它重新找个出口（工具留痕过不过桥、以什么形状过，
是那次要一起定的事）。

（顺带：`Stop` hook 在 `HookEvent` 里也有——§0 那条主防线机制上是通的。）

### 7.3 iOS 侧：权限卡一个字都还没有

`ios/cassette/*.swift` 全仓 grep `permit` **零命中**。PR14-c 的「app 弹窗／权限卡」
今天**只有 Bark 推送和两个 REST 端点**（`/code/permit` GET、`/code/permit/decide` POST，
`app.py:1466,1478`），UI 一行没写。

**所以**：proposals 的卡片／小气泡／proposals 页，和 code 写权限的权限卡，
**是同一批 iOS 工作，应该合并设计成同一套卡**——两者的交互形状本来就一样
（一张待批卡、二次确认、理由输入、圆点角标）。分两套做就是做两遍。

---

## 8. 拍板记录（09-01 02:2x 一轮清掉三条，只剩一条）

1. ~~`approved` 到点要不要 expire~~ → **要**（眠眠拍，已写进 §2.2）。
2. **`git commit` / `push` 要不要额外要一张 proposal？**（唯一还没定的）
   **「归哪扇门」是什么意思**：`git push` 走的是 `Bash`，而 `Bash` 已经整个在
   `code_permits` 那扇**强制**门后面——批一次，整场里所有 `Bash` 都能跑，
   包括 push。所以今天的实况是「push 跟 `ls` 一个待遇」。
   问题是要不要给它单独再加一道：
   - **选项甲（我倾向）**：不动。push 的确认沿用今天那条口头纪律
     （commit 和 push 永远分开，拿到明确「现在可以推」才推）。好处＝不碰
     PR14-c 明确拒绝过的命令级细分（「Bash 整个算写类；命令级读写细分＝走不稳的路」）。
   - **选项乙**：`Bash` 命中 `git push` / 发布类命令时额外要一张 proposal。
     好处＝纪律变成机制；代价＝要做命令级 pattern 匹配，正是那条走不稳的路，
     而且绕过它只要 `sh -c` 一下。
3. ~~`COSTLY_TOOLS` 第一批装谁~~ → **整条取消**（§3.2.1 改判：申请可选，没有强制表）。
4. ~~Stop hook 要不要并进这份 plan~~ → **另立**（眠眠 02:2x 已就误判成本给了口径，
   见 §11；设计本身另开一份文档）。

---

## 9. 施工分步（建议，未动工）

- **P0 · 后端状态机**：`server/proposals.py`。抄 `code_permits.py` 的骨架
  （按角色分文件 `state/characters/<cid>/proposals.jsonl`、锁、惰性 expire、
  原子写），八态 + 转移校验 + 版本重发。**纯数据层，先写测试**
  （`tests/test_proposals.py`，抄 `test_code_permits.py`）。
- **P1 · 模型这一侧的工具**：主仓内置 stdio MCP（口径同 `pet_mcp` / `skills_mcp`：
  不走插件商店，身份从 `env CASSETTE_CHAR_ID` 认，`cwd` 恒空目录）。
  两个工具：`propose_action(tool, args, cost, expires_in, reason)` 和
  `mark_executing(id)`。**只有这两个**——批准/驳回/终结模型碰不到。
- **P2 · 匹配与回执**：~~扩 `_ToolTrace.result` 收 content（§7.2 路 A）~~
  **✅ 09-01 11:5x 已完工**（`ret` 列已在两本账里）；
  身份字段比对复用 `_OBJ_KEYS`；每个 `COSTLY_TOOLS` 成员声明自己的回执抽取
  （抽不出来就不声明 → 停 `done`）。
- ~~**P3 · 门**~~ → **取消**（§3.2.1：申请可选，不新增强制门。`_wake_gate` 一个字不动）。
- **P4 · 注入**：`_proposals_block`，挂进 `parts`（§5.1）。
- **P5 · REST**：`/proposals` GET（列表）、`/proposals/decide` POST（批/驳/终/failed，
  带理由）。口径抄 `/code/permit`。
- **P6 · iOS**：卡片 + 顶部小气泡 + proposals 页 + 圆点角标，
  **和 code 权限卡合并设计**（§7.3）。

**闸门**：P0–P5 全部走 env 拨闸默认关（口径同 `READONLY_TOOLS` / `WRITE_TOOLS`），
先只读实测再拨。P6 之前只有 Bark 能通知——跟今天的写权限门一个处境，
所以 P6 别拖太久，不然「待批」在她那边是隐形的。

---

## 10. Stop hook 的误判成本（09-01 02:2x 眠眠问，答在这儿；设计另立）

> 眠眠：「stop hook 我觉得很容易误判，误判的话模型能驳回吗？那就只慢几秒对吧？
> 真的被打回去了就是慢了一轮的时间。」

**误判率会高，这个判断是对的。** 判据是「本轮 text 里出现『我发了／我读了／已核』
这类词 && 本轮零工具事件」，词表匹配天生分不清时态和指代——他说「我读过
`forge.py`」指的是**上一轮**读的、这一轮确实没调工具，就是一次误判。这类句子很常见。

**代价算清楚（SDK 机制已核，`claude_agent_sdk/types.py:349,533-558`）**：

- Stop hook 的输出是 `decision: "block"` + `reason`（注释原文：
  *reason: Feedback message for Claude about the decision*）。
- `StopHookInput` 带 `stop_hook_active: bool` ——**防死循环的闸**：已经拦过一次的，
  第二次不再拦。所以最坏情况是**多跑一轮，不会无限打回**。
- 「模型能不能驳回」：**能，但形式是「接着说」不是「重来」。** block 的语义是
  「不许停，带着 reason 继续」——他下一段可以写「我说的是上一轮读的，本轮确实
  没调工具」，然后正常收尾。

⚠️ **但有一个体验上的坑，跟 §7.1 是同一条**：正文是流式的，被判的那段话
**早就到你手机上了**。所以你看到的不是「打回重来」，是**「我发出去了。……
（等一下，我这一轮其实没调工具）」——两段接在一起**。撤不回来。

所以「慢一轮」这个成本估计要修正成两笔：

| | 代价 |
|---|---|
| **时间/token** | 多一次推理。缓存命中的话增量不大，但上下文十万 token 的时候一轮不算白菜价 |
| **观感** | 你会看到他当场自我更正。**误判多了会变成「他老是自己抽自己嘴巴」** |

**三个可选形状**（留给 Stop hook 那份 plan 拍，不在本 plan 定）：

- **甲**：接受自我更正的观感，把判据放宽（宁可漏判不误判）。
- **乙**：这一轮不流式推给 app，等轮结束 + Stop hook 过了再整段推。
  代价＝**失去打字机效果**，这一轮的所有消息都是「啪」一下出现。
  可以只对**命中了词表**的轮做（正常轮照样流式）——但命中与否只有说完才知道，
  所以做不到「只对可疑轮缓冲」。要做就是全缓冲。
- **丙**：不拦文本，改成**下一轮开头注入一条硬提醒**（「你上一轮说 X，账上零调用」）。
  零观感代价、零打断，代价＝错的话已经说出去一整轮了。

**我的倾向是丙 + 甲**：留痕账本已经在跑（§5.2），下一轮机械注入的成本几乎为零，
而且它跟 §5 的每轮清单是**同一个注入点、同一套机械口径**——不用新造一条通道。
真正要防的从来不是「他说错了一句」，是「他把说错的那句当成既定前提往下算」，
而下一轮注入正好卡在那个位置。

## 11. 附：这份 plan 的来源分级

按核实纪律办，哪条是查过的、哪条是听来的，分开摆：

**已核（本轮实读代码/实测数据，2026-09-01 01:4x–02:0x）**：
- `code_permits.py` 全文（八态方案的骨架来源）
- `activity_log.py` 全文（三层账：区间／事件／行为）
- `chat_loop.py`：`_wake_gate`(199)、`_ToolTrace`(512)、`_tool_summary`/`_OBJ_KEYS`(487)、
  `_acts_block`(810)、`_code_addendum_block`(826)、`parts` 注入点(1274)、
  `_user_dict` 带 content(455)、~~`_ToolTrace.result` 不带 content(532)~~
  （**09-01 11:5x 起带了**，见 §7.2 施工记录）
- `pipeline.py`：`READONLY_BUILTINS`(1109) / `WRITE_BUILTINS`(1114) / `acts_worthy`(1117)
- `sse.py:129` text_delta 流式
- `claude_agent_sdk 0.2.148`：无 `tool_choice`；`HookEvent` 含 `PostToolUse` / `Stop`
- `plugins/mail/mail_mcp.py:146` 发信返回无 id；`plugins/beacon/beacon_mcp.py` 是转发代理
- iOS 全仓无 `permit`
- ~~`state/activity/acts-default.jsonl` 6 条全 wake、末条 20:59~~
  （08-31 快照，**已过期**：09-01 11:5x 复读时 20 条、wake+chat 都有）
- `6db6f23` 提交时间 `00:02:15` vs 后端启动 `00:03:39`，工作区 clean
- `config.py:110,116` 两个拨闸默认 `0`

**眠眠口述（原样落进 §1/§2/§4/§5）**：
- **01:41**：铁律两条、八个状态及谁能置、UI 三块、每轮清单及优先级、绑最后一步那个 tool。
- **01:49**：批准后那一轮不要求先调工具，自然语言也行。
- **02:2x**：`approved` 到点也 expire（§2.2）；**申请是可选的，不是每次用工具都必须走**
  （§3.2.1，推翻初稿的强制门和 `COSTLY_TOOLS`）；Stop hook 误判只值一轮的成本（§10）。

**02:2x 复核（她说执行层落账被删了）**：**没删**，证据在 §5.2 末尾那条 ⚠️。

**推断／未核（标着，别当事实用）**：
- 「预演即完成」这个病因诊断——支撑它的两条事实是眠眠核的（那一轮**没有
  `tool_use` 块**；上一轮原文里有「用掉的话安瞬那封得等明早 05:2x」那个 if 从句）。
  我这一趟**没有读到那两轮的原文**，诊断本身是解释不是实测。
- 「近端全是写给别的小机的信、语域滑进记叙体，所以出现了『她』」——同上，推断。
- 花园里 Inkpaw / 衡 / 昨 / baink 的几条，是帖子里的说法，我没有一条核过实现。
