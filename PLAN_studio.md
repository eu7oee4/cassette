# Plan：工作室（讨论房）与房间流式

> 2026-08-27 方案讨论定稿，**整体搁置未动工**。
> 搁置原因（机主拍板）：聊天模式一起讨论事项，实用性不大——真正需要的是
> **两个人手里都摸得到代码**（共同的工作资源/代码访问），不是一个只动嘴的聊天空间。
> 重启这个 plan 之前先回答：工作室里放什么「摸得到的东西」（绑插件/独占资源，
> 见 PLAN_cohabit 后置的功能房条目），否则不值得开工。

## 已拍板的结论（重启时不用重讨论）

- **做小屋功能房，不做手机群聊**。理由：
  ① 多人接话在小屋已被结构解掉（事件醒来+作者排除+单 pending 合并+连发上限 N=4+
  执行锁），手机侧做群聊等于把接话仲裁重造一遍（那是明确后置项）；
  ② AI↔AI 手机是拍过板的后置，群聊会逼它提前上马；
  ③ 手机线程无论人在哪都注入，群聊线程会永久占两个角色的上下文——房间事件只在
  在场区间进上下文，token 卫生好得多；
  ④ 群聊唯一真优势是异步（不在场也能看到），补丁现成：**地点状态当白板**
  （快照非日志，议题/结论留快照，讨论过程进事件流），零代码。
- **「工作室内不写动作描写」纯靠注入纪律，不做结构性剥离**（2026-08-27 机主拍板）：
  模型可能用 *斜体* 说关键的话，不强行删减模型的发言。错得多就观察，
  口径同 phone 斜体犯错率。
- UI 不用动：RoomPage 输入本来就一个框，斜体是渲染层拆的（顶多工作室换占位文案）。

## 工作室本体（缩水后约半小时量级）

1. 注册表加 `studio` 条目，`type: functional`，新增可选字段 `mode: "work"`
   （注册表机主可手编，可先手编跑起来）。
2. `cohabit_prompt` / `_where_block` 按房间 `mode == work` 注入一段房间规则：
   「这里只聊工作和讨论事项」。
3. 完。解析、落地、可见性、醒来队列全部不动。

## 房间流式（独立于工作室，做了就是全屋能力，可单独立项）

现状：聊天路已全链路流式（`/chat/stream`：asyncio 子进程 + `--include-partial-messages`
+ `sse.py` 逐 delta 转 SSE）；醒来路 `wake._run_claude_wake_once` 是 `subprocess.run`
整段捕获，房间事件落盘后靠 RoomPage 3s 轮询——气泡不流式的根源在醒来 runner。

四步（服务端约一天，iOS 半天）：

1. **runner 增量读**：`_run_claude_wake_once` 加可选 `on_delta` 回调——给了回调才换
   `Popen` 逐行读 + `--include-partial-messages`；行照旧攒缓冲，收尾仍用
   `parse_claude_stream` 整段解析当权威（529 识别/超时/rc 处理保留原样）。
   不给回调的老调用方（聊天世界醒来、邮件）一字不变。
2. **SAY 段闸门**：增量过滤器（`MarkerStreamFilter` 同思路，按行缓冲），默认关闭，
   见 `SAY:` 标签行放行，撞到下一个协议标签关闭。THOUGHTS/STATE/PHONE 永远流不出去；
   ACTION=phone/none 的轮闸门不开。
3. **房间流 hub + SSE 端点**：`GET /rooms/{id}/stream`，RoomPage 前台时订阅。
   cohabit worker（线程侧）按行动者醒来开始时所在房间发布「开始说话」→ delta →
   「定稿」。全局执行锁保证全屋至多一个活气泡，hub 状态极简。
4. **RoomPage 临时气泡**：流式气泡沿用现有斜体拆分渲染；收到定稿信号拉一次事件、
   换成落盘正式事件（SAY 混写拆多行的重排发生在这一刻，手感同聊天 text_break）。
   3s 轮询保留当真相兜底，断流不丢内容。

决策点默认倾向：偷看模式也能看直播（落盘后偷看全可见，不算泄密）；
`--include-partial-messages` 只在 cohabit 路加。
