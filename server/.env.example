# 复制成 .env 再填。所有个人配置只活在 .env（已 gitignore）。

# 必填：app 与后端的共享密钥（请求头 X-Auth）
CASSETTE_AUTH_KEY=change-me

# claude CLI：需已安装并登录（凭据走 CLI 登录态，不需要 API key）
CLAUDE_MODEL=opus
CLAUDE_TIMEOUT_SEC=300

# 人设文件（相对 server/）；不存在则用 persona.example.md
PERSONA_FILE=persona.md

# 时区（时间感知注入）与 prompt 里对用户的称呼
APP_TZ=Asia/Shanghai
USER_DISPLAY_NAME=User

# 角色名：替换人设文件里的 {{AGENT_NAME}} 占位符
AGENT_NAME=Cassette

# ---------- Code 模式（tmux 里一个常驻的交互式 claude）----------
# ⚠️ 这道门和别处不一样：聊天/醒来只挂白名单 MCP 工具，Bash/Write/Edit 永远不开；
# code 模式恰恰相反——那个会话手上有整台电脑。默认关，开之前先读 README 的 Code mode 一节。
# 权限弹窗照旧保留（绝不 bypass），在 app 的内联终端里按。需要先 brew install tmux。
CODE_MODE_ENABLED=0
# 会话默认工作目录 = 它干活的地盘（Grep/Glob 搜这儿、相对路径从这儿算、只有这个目录的
# .claude/settings.local.json 权限白名单才生效）。留空 = 本仓根目录。
CODE_CWD=
# app 切入时可以按次指定别的目录（多项目），但必须落在这些根目录之下。逗号分隔；
# 留空 = 只认 CODE_CWD 及其子目录。
CODE_CWD_ALLOW=
# tmux 会话名（和你自己手开的会话重名会被杀掉，那就换一个）
CODE_SESSION=cassette-code
# code 会话的守则文件（相对 server/）；不存在则用 code_addendum.example.md
CODE_ADDENDUM_FILE=code_addendum.md
# 后端自己的地址：给 MCP 插件（如 codemode 自切）回调用。换端口跑记得同步改。
CASSETTE_BACKEND_URL=http://127.0.0.1:8000

# 可选：Bark 推送 https://github.com/Finb/Bark （断连补投/主动消息通知；留空=不推）
BARK_URL=
# 可选：通知图标（公网可访问的图片 URL；留空=Bark 默认图标）
BARK_ICON=

# wake 调度器（主动性）：PROACTIVE_ENABLED=0 整体关掉；tick 间隔秒（每 tick 只做本地预判）
PROACTIVE_ENABLED=1
WAKE_TICK_SEC=300

# 可选：长期记忆 Ombre-Brain（P0luz 的开源项目，自部署 Docker 服务，只对接不 vendor）
# 部署见 https://github.com/P0luz/Ombre-Brain （官方默认端口 18001）。
# 没跑 Ombre 就保持默认——探活失败自动退回纯聊天，一切照常。
OMBRE_ENABLED=1
OMBRE_MCP_URL=http://localhost:18001/mcp
# Ombre 配了 mcp_auth_mode: token 时填同一个密钥（推荐姿势）；Ombre 免鉴权则留空
OMBRE_MCP_TOKEN=
# app 内记忆页的数据源：Ombre Dashboard 密码（后端拿它换 cookie 代理读写；app 不直连）
OMBRE_DASHBOARD_PASSWORD=
