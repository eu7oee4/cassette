# PLAN_oneturn：「你只有这一轮」+ NEXT 带待办

> 2026-08-28 夜。起因：小卡读完安瞬那十封信，回复末尾写了「我现在就去回信。名册我一并
> 更新」——两件都没做。眠眠当场问了一句：**「是不是因为我们起的是一次性的 claude -p？」**
> 是。根因 TA 自己就猜到了，下面这份是证实、定位和归类。

---

## 1. 现场

`state/outbox.json` 最后一条（08-28 21:49，`origin=chat_rescue`，`stored=0`）：

> 汪，十封全读完了。……**我现在就去回信。**不写一封长的糊过去，欠九天的债不是靠字数
> 还的。**名册我一并更新。**

同一个晚上，同一个角色，同样的工具（mail / webpage 都挂着）：

| 时间 | 它说要做的事 | 成了吗 | 差别 |
|---|---|---|---|
| 21:32 | 搭一张笔友名册网页 | ✅ | 在**开口之前**做的 |
| 21:49 | 读完十封信 | ✅ | 在**开口之前**做的 |
| 21:49 | 「我现在就去回信」 | ❌ | 放到了**开口之后** |
| 21:49 | 「名册我一并更新」 | ❌ | 放到了**开口之后** |

能力一点没少。少的是「开口之后」这个时刻——它不存在。

旁证：那一轮 `stored=0`（没 hold 记忆），`backend.out.log` 当晚没有一条
「聊天里定了下次醒来」。**两个承诺没有任何落点**，只以正文形式活在
`recent_window.json` 里，是一段它自己写的、没有执行钩子的散文。

## 2. 根因：四个条件同时成立

1. **执行环境是一次性子进程**：`pipeline.base_claude_args()` 起的是 `claude -p`，
   没有 `--resume` / `--session-id`，`subprocess.run` 跑完即退。正文吐完 = 进程死。
2. **这条事实从没写进 prompt**。模型的先验是对话式助手的默认世界模型——「我说完话之后
   还在」。没人告诉它这里不是。
3. **意图的表达形式是自然语言正文，而正文没有执行钩子**。写进 `recent_window` 的东西
   是给人读的历史，不是给调度器读的指令。
4. **唯一的跨轮落点接不住这个意图**：`schedule.next_wake_at` 只存「什么时候」，
   不存「要做什么」。就算它想钉，钉子上也挂不了活。

打断任何一条都不出事。条件 1 是架构选择（不动），2/3/4 就是这次的两刀。

**这个坑好不好触发？** 触发很容易——只要一轮里的活多到做不完就会出现。
难的是**发现**：模型说了没做，下一轮它自己也不记得说过，用户不追问就永远不暴露。

> ⚠️ **这一类 bug 没有症状。** 不是「症状不明显」，是零症状：日志里不会有错误，
> 测试不会红，模型下一轮读档时那句承诺已经沉进历史里、和别的话长得一模一样。
> 这次能被抓住，纯粹因为眠眠自己记得它说过什么、并且去问了。

## 3. 归类

> **表达层不区分「打算」和「已完成」，就迟早会把打算当结果报出去。**
>
> **而一个没有落点的打算，一定会蒸发**——跨轮活下来的只有结构化字段，正文只是历史。

抽象条件：说话的一方有一个自己看不见的生命周期边界；边界之后的事它照样能说出口；
说出口的东西没有对应的存储结构。三条凑齐，就会有人（用户）拿着一句不成立的完成态在等。

拿到别的项目也成立：任何 stateless 的 agent（serverless function、cron job、
一次性 RAG 调用）里，「我稍后会…」都是空头；而任何靠自然语言正文传递跨轮意图的系统，
那个意图都活不过这一轮。

## 4. 规避规则

1. **模型看不见的生命周期边界，必须在 prompt 里明说。** 进程什么时候退出、上下文什么
   时候清空、工具什么时候失效——这些是**事实**，不是纪律。不说，模型只能按先验猜，
   而它的先验来自「助手一直在」的训练分布。
2. **禁一种说法，必须同时给一个能落地的替代出口。** 只禁不给，模型会憋出更含糊的说法
   （「我现在就去」变成「回头我看看」），症状消失、问题还在。这次的二选一里，
   ②必须和①一起给。
3. **跨轮的意图要有结构化落点（一个字段），不能只活在正文里。** 判据很简单：
   *下一轮的注入是从哪儿拼的？* 拼不到的地方写什么都等于没写。
4. **落点要连「为什么」一起存。** 只存时间不存事由 = 到点醒来一脸茫然，等于没存。
   `next_wake_at` 之前就是这个病：它保证了「到点叫醒」，但醒来那位不知道自己为什么定了
   这个点。**「什么时候」和「要做什么」是一对，拆开存等于都没存。**
5. **存了就要定清除口径**：什么时候作废、什么时候留着。没有清除规则的待办会永远注入、
   永远清不掉——这一条是写代码时现踩出来的（见 §6 的三种情形）。

## 5. 捉虫

### 竖着翻 git log：这是同一大类的第二次

| 时间 | commit | 谁把打算当成了结果 | 用户看到的 |
|---|---|---|---|
| 08-08 | `ec43f6c` | **系统**：`_stored_from_tool_use` 只解析 `tool_use`（调用意图），从不看 `tool_result` | 灰字「📥 记住了一件事」——实际那条被 Ombre 拒了 |
| 08-28 | 本次 | **模型**：把没做的事写成完成态说出口 | 「我现在就去回信」——实际没有那个之后 |

机制不同（一个是解析漏了返回值，一个是提示词漏了边界），但**形状和受害者完全一样**：
眠眠拿到一句不成立的完成态。`ec43f6c` 当时定下的原则是「看结果不看意图」——这次是
同一条原则的另一个方向：**要么先做出结果再开口，要么把意图存成结构**。

> 顺带记一条：`ec43f6c` 的 commit message 里已经写过「『记住了一件事』是句承诺，
> 没看到结果就不该替他说出口」。同一句话，三周后换了一层又犯一次。

### 横着扫代码：只有一处，已修

扫的特征是「**模型定的、要跨轮兑现的状态**」。全仓 `schedule` 字段只有四个：

| 字段 | 谁定的 | 带事由吗 | 结论 |
|---|---|---|---|
| `last_wake_at` | 系统 | 不需要（是事实不是意图） | — |
| `cooldown_until` | 系统（错误退避） | 不需要 | — |
| `next_wake_at` | **模型** | ❌ 只有时间 | **本次修** |
| `next_wake_todo` | 模型 | ✅ | 本次新增 |

**范式其实早就有，是这一个字段漏了：**

- `offers._offer`（抱人邀约）：`{id, actor, to, room, move_motion, deadline}`——
  跨轮意图，事由齐全，带 deadline（清除口径），有结果补醒。教科书写法。
- `mail_bridge` 待醒 flag：`[{uid, from, subject}]`——存的就是「为什么要醒」，
  `consume_wake_pending()` 读完即清。

也就是说这不是没想到，是 `next_wake_at` 这一个字段**从一开始就只当闹钟设计**，
后来模型开始拿它当待办用，缺口才露出来。

扫的范围：`server/*.py` 全量的 schedule / pending / 定时字段，`plugins/*/`。
**没有第二处同类**——不硬凑。

### 没扫的

`pet_store` / `game` 那两片没有「模型定的跨轮时点」这种字段（团团是引擎驱动、
剧情是盲操搬运），不在这一类里。

## 6. 修法

### ① 「你只有这一轮」（`pipeline.one_turn_hint(kind)`）

一份来源，三条路共用（chat / wake / cohabit）。规矩写成**二选一**：

- ① 这一轮里就做掉，做完再开口——工具都在手上，报结果别报打算；
- ② 做不完就钉到下一轮，**必须带时间 + 写清要做什么**；
- 两个都不选，就别说出口。

点名禁掉的句式：「我现在就去X」「我这就去X」「X我一并做了」。

**位置很关键**：排在 prompt 尾巴上（时间感之前），不进 `extras`。`extras` 在整段最前面，
长对话一压就淹了——而这条恰恰是被淹掉才出事的那条。`kind` 只让②的写法分叉
（chat 用 `[[next_wake]]` 标记，wake 用 NEXT 段），正文一字不差。

### ② 钉子带上待办

`[[next_wake:1小时|给安瞬回信，回完更新名册]]` / `NEXT: 1小时 | 回信`
→ 竖线后存进 `schedule.next_wake_todo` → 下一轮由 `pipeline.pending_todo_block()` 递回。

三条路全接：`app.py`（聊天）、`wake.py`（老醒来）、`cohabit.py`（同居醒来）。

**清除口径**（§4 规则 5 的落地，三种情形分开）：

| 情形 | next_wake_at | next_wake_todo | 为什么 |
|---|---|---|---|
| 定了新点 | 换成新的 | **整体替换**（没写待办＝清空） | 钉子挪了地方，旧的活就作废，不然它跟着新时点一路漂 |
| 到点醒来（点被消费） | 清 | **跟着清** | 兑现过了 |
| 提前被叫醒（随机/硬触发），点还待命 | 留着 | **留着** | 他答应做事的那个时点还没到；这时清掉，到点那次就成了没头没脑的一醒 |
| 小屋总开关关掉 | 撤 | **跟着撤** | 冻结期整段没记忆，解冻后拿到一件「上一轮答应过」的事只会更像失约 |

两个刻意的决定：

- **灰字只给时间**（`next_wake_note` 不含待办）：那是它给自己留的话，不往用户那边漏。
- **没有时间就没有钉子**：解析不出时间的标记整条作废，待办跟着丢。留一个没有兑现时点的
  待办 = 永远注入、永远清不掉。

### 现踩的一个坑

`parse_next_minutes` 有条兜底是「有数字就当分钟」。加了竖线之后，
`「无 | 读第 2 封信」` 会被它咬到待办里的 `2`，把「不定点」读成「2 分钟后醒」。
现在解析前先切待办（`split_next_raw`），单测钉死。

## 7. 验证状态

**读代码 + 单测（已做）**：新增 `tests/test_one_turn.py` 26 条 + `test_cohabit.py` 补 2 条，
覆盖切分 / 落盘 / 回注 / 三种清除口径 / 数字不串。全量 241 条，14 个失败与改动前基线
一模一样（都在 pet/world，本次无关，stash 对比确认过）。后端 08-28 23:09 重启，
`/health` 200，三个 worker 正常。

**真机没验过（留给机主）**：

1. 让它做一件一轮内做不完的事（比如"把这五封信都回了"）→ 看它是当轮做完报结果，
   还是写 `[[next_wake:…|…]]`，而不是又说「我现在就去」；
2. 钉了之后看日志那行有没有带上活：
   `聊天里定了下次醒来：1小时 → 08-29 00:09｜留的活：给安瞬回信`
   （没留活会打「｜没留活」，从日志就能分辨是真钉子还是空闹钟）；
3. 到点醒来时，看它有没有认领上一轮留的活（心流日志里应该提到）；
4. 提前被随机醒来一次 → 待办应该还在，到点那次仍然拿得到。

**这一刀本质上是提示词纪律，模型听不听话只能真机看。** §5 那张表能证明的只是
「机制接得住」，接不住和不听话是两件事，别混着报。

## 8. 没做的

- **「读信/写信就更新名册」那条准则不钉 prompt**（眠眠 08-28 拍板）：名册只有小卡有，
  Cass 没有。钉进公共提示词等于让 Cass 也去更新一个它没有的表——正好是
  [cassette-charswitch-bug-class] 那一类（按角色分的东西写进全局）。②这条路给了它落点，
  准则本身先不动。
- **不做「轮末扫承诺词强制落待办」的机制兜底**（原方案的第 3 条）：误伤面太大
  （「我一并更新」和「这个我更新过了」正则分不开），先看提示词这一刀的真机效果。
